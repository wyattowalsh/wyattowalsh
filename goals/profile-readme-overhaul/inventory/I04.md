# I04 — Trace metrics_svg validate/recover stub markers

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/metrics_svg.py`:

- Error signal `"will-be-regenerated"` matches `will be regenerated` (case-insensitive) → `ERROR_PAYLOAD`.
- `PLACEHOLDER_TEXT_MARKERS`: “Metrics temporarily unavailable” / “Check workflow logs for details”; aria `metrics unavailable`.
- `_is_tiny_placeholder_bar`: root height ≤ `STUB_BAR_MAX_HEIGHT_PX` (24px) with a rendered `<rect>` → `PLACEHOLDER`.
- `STUB_SVG_MAX_BYTES = 512` (tiny-file heuristic lives beside the bar check).
- `recover_svg_file` accepts current if valid, else copies `--previous` if that previous is valid, else **REJECTED**.

Workflow production:

- Backup + `recover` for `metrics.svg`, `metrics.additional.svg`, **and** `metrics.extra.svg`.
- Final `metrics_svg validate` on all three; extra is uploaded and committed (`metrics.extra.svg` in artifact + finalize `metrics_files`).

On-disk `.github/assets/img/metrics.extra.svg` is ~18 KB, height 225, followup title “Overall issues and pull requests status” — **not** a 401-byte / black-bar stub.

## Finding

Validate/recover reject regenerated-bar and tiny-placeholder stubs. Extra has a real recover path and is a real card.

## Gap

None for this leaf. Isolated lines/achievements/gists retries stay off until they pass this gate.
