# I09 — Map Waka API ranges actually called today

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/wakatime_readme.py`:

- `WAKA_STATS_RANGES = ("last_7_days", "last_year", "all_time")`.
- `collect_wakatime_stats` GETs `/users/current/stats/{range}` for each; `last_7_days` is required, year/all-time optional-skip.
- Also GETs `/users/current/all_time_since_today` (lifetime total fallback).
- Heatmap via `fetch_wakatime_daily_totals` (`include_daily=True`):
  1. `/users/current/insights/days/last_year`
  2. fallback `/users/current/summaries?range=Last 30 Days` then `Last 7 Days`

`scripts/wakatime_svg.py` renders week / year / all-time totals plus `_render_heatmap` from those daily totals. README markers wrap an open `.github/assets/img/wakatime.svg` (not `<details>`).

## Finding

Ranges actually called: **last_7_days**, **last_year**, **all_time**, plus all_time_since_today and heatmap insights/summaries endpoints.

## Gap

Call map is complete. Committed `.github/assets/img/wakatime.svg` Platforms row is **Mac only** (no iOS / watchOS text). Renderer + range-union tests can draw Mac/iOS/watchOS when those OS rows exist in the collection; the shipped card does not. Privacy filters belong to WAKA write nodes.
