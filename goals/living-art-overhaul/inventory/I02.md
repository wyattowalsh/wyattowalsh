# I02 — Map ALL_STYLES / LIVING_ART_STYLE_KEYS / STYLE_DIALECTS / CLI style picker

**Date:** 2026-08-18  
**Status:** complete  
**Locks (later):** `L-ROS` → **R1** (`scripts/art/roster.py`); `L-ARTF` → **R2** (`artifacts.py`); `L-TL` → **R3** (`timelapse.py`); `L-CLI` → **R4** (`_common.py` + `art.py`); `L-T-MEDIA` → **R5** (three equality-test modules)  
**Depends (later):** I99 merge, then R1–R5. Until K2, shipped == candidates == six.

There is **no** `scripts/art/roster.py` today. Four independent six-lists must stay in lockstep. R1 becomes the SSOT; this leaf maps what each list actually gates.

---

## Files read

| Path | Why |
|---|---|
| `goals/living-art-overhaul/{goal,facts,grill-notes,plan}.md` | Goal facts + R1–R5 contract |
| `goals/living-art-overhaul/task-graph.json` | R1–R5 verify strings |
| `AGENTS.md`, `scripts/art/AGENTS.md` | Documented SSOT (`_STYLE_REGISTRY` / `ALL_STYLES`) |
| `scripts/art/timelapse.py` | `_STYLE_REGISTRY`, `ALL_STYLES`, `render_timelapse`, module `--only` |
| `scripts/art/artifacts.py` | `LIVING_ART_STYLE_LABELS` / `KEYS`, budgets, regex, inventory, `__all__` |
| `scripts/art/shared/accretion.py` | `STYLE_DIALECTS`, `_DEFAULT_ORIGIN`, `_knobs_for` |
| `scripts/art/shared/__init__.py` | Re-export of `STYLE_DIALECTS` |
| `scripts/art/__init__.py` | Package docstring only — **no** style-key re-export |
| `scripts/art/animate.py` | Duplicate `all_generators` + `--only` |
| `scripts/art/shared/seeds.py` | `parse_cli_args` `--only` (no membership check) |
| `scripts/cli/generate/_common.py` | `_selected_living_art_styles`, default list, index-refresh gate |
| `scripts/cli/generate/art.py` | Typer `--only` on `living-art` / `timelapse` / `animated` |
| `scripts/cli/generate/all_cmd.py` | `generate all` → full `living_art()` (no `--only`) |
| `scripts/cli/generate/__init__.py` | Re-exports commands, not the key tuples |
| `scripts/readme_sections.py` | Hardcoded six-row Living Art list (not imported keys) |
| `.github/workflows/profile-updater.yml` | Matrix from `LIVING_ART_STYLE_KEYS`; shard `--only` |
| `docs/content/docs/scripts/living-art-modes.mdx` | Human matrix + `--only` copy |
| `docs/content/docs/cli/generate.mdx` | `--only` help duplicate |
| Tests listed under [Equality assertions](#equality-assertions-pathline) | Freeze the coincidence of the four lists |
| Sibling inventory `I05.md`, `I15.md`, `I16.md` | Avoid re-mapping inventory/render/docs; this leaf owns picker SSOT |

Not production-edited. `roster.py` does not exist.

---

## Current SSOT (today: one roster, four copies)

Docs still say the style map SSOT is `timelapse.py` (`_STYLE_REGISTRY` / `ALL_STYLES`). That is true for **generators and GIF stems**. Published inventory / CI / CLI default / README do **not** import `ALL_STYLES`. They copy the same six slugs.

| Symbol | File | Type | Role today |
|---|---|---|---|
| `_STYLE_REGISTRY` | `scripts/art/timelapse.py:58-65` | `dict[str, tuple[str, str]]` | Key → `(module_path, "generate")`. Private. |
| `ALL_STYLES` | `timelapse.py:67` | `list[str]` | `list(_STYLE_REGISTRY.keys())`. Default `render_timelapse` loop. |
| `LIVING_ART_STYLE_LABELS` | `artifacts.py:46-53` | `dict[str, str]` | Slug → display title. Insertion order = published order. |
| `LIVING_ART_STYLE_KEYS` | `artifacts.py:54` | `tuple[str, ...]` | `tuple(LIVING_ART_STYLE_LABELS.keys())`. CI matrix + Typer picker + most tests. |
| `_STYLE_KEYS` / `_TIMELAPSE_RE` | `artifacts.py:64-65` | regex | Filename parser. Not a second roster. |
| `_canonical_living_art_names()` | `artifacts.py:291-293` | `tuple[str, ...]` | `tuple(sorted(LIVING_ART_BYTE_BUDGETS))` — **GIF names**, alpha-sorted, not slug order. |
| `STYLE_DIALECTS` | `shared/accretion.py:26-33` | `dict[str, str]` | Slug → family. Re-exported from `scripts.art.shared`. |
| `animate.all_generators` | `animate.py:361-368` | local dict | **Fourth** six-list. Legacy growth CLI only. |
| README `items` | `readme_sections.py:1096-1127` | hardcoded tuples | **Fifth** six-list. Titles + GIF names + alt metaphors. |

There is **no** Python alias such as `LIVING_ART_STYLES = LIVING_ART_STYLE_KEYS`. Closest “aliases”:

- `LIVING_ART_STYLE_LABELS` (human titles for the same slugs)
- `_STYLE_KEYS` (regex join of those slugs)
- `_TIMELAPSE_RE` = `^living-({keys})(-dark)?\.gif$` (optional `-dark` stem; **not** in budgets)
- GIF/MP4 filenames derived as `living-{slug}.gif` / `.mp4`
- Typer commands `generate living-art` and `generate timelapse` share `_generate_living_art_timelapse` (command aliases)
- `--frames` is a Typer alias of `--max-frames`

`scripts.art` `__init__.py` does not re-export any of these names. `artifacts.__all__` exports `LIVING_ART_STYLE_KEYS` and `LIVING_ART_STYLE_LABELS`. `shared.__all__` exports `STYLE_DIALECTS`. `ALL_STYLES` is imported only by tests (and documented in AGENTS / living-art-modes).

---

## Exact current six keys and filenames

Canonical slug order (registry / labels / dialects / CLI default / README wrap):

`inkgarden`, `topo`, `genetic`, `physarum`, `lenia`, `ferrofluid`

| Slug | Label | GIF | MP4 | Generator | Dialect family |
|---|---|---|---|---|---|
| `inkgarden` | Ink Garden | `living-inkgarden.gif` | `living-inkgarden.mp4` | `scripts.art.ink_garden.generate` | botanical |
| `topo` | Topography | `living-topo.gif` | `living-topo.mp4` | `scripts.art.topography.generate` | cartographic |
| `genetic` | Genetic Landscape | `living-genetic.gif` | `living-genetic.mp4` | `scripts.art.genetic_landscape.generate` | fitness |
| `physarum` | Physarum | `living-physarum.gif` | `living-physarum.mp4` | `scripts.art.physarum.generate` | mycelial |
| `lenia` | Lenia | `living-lenia.gif` | `living-lenia.mp4` | `scripts.art.lenia.generate` | morphogenetic |
| `ferrofluid` | Ferrofluid | `living-ferrofluid.gif` | `living-ferrofluid.mp4` | `scripts.art.ferrofluid.generate` | magnetic |

Slug ≠ module: `topo` not `topography`; `inkgarden` not `ink_garden`.

**GIF write:** `timelapse.py:532` `out_dir / f"living-{style}.gif"`.  
**MP4 write:** `timelapse.py:337` `gif_path.with_suffix(".mp4")` (best-effort ffmpeg).  
**Budgets** (`artifacts.py:30-37`) are keyed by GIF name, **alpha insertion**, not slug order:

`living-ferrofluid.gif` 3.8M · `living-genetic.gif` 2.4M · `living-inkgarden.gif` 7.2M · `living-lenia.gif` 1.2M · `living-physarum.gif` 2.4M · `living-topo.gif` 10M

Inventory (`_validate_canonical_inventory`) uses `_canonical_living_art_names()` = those six GIF names sorted, **not** `LIVING_ART_STYLE_KEYS` directly. Bijection is tested at `tests/test_living_art_media.py:1668-1670`.

Dark filename `living-{slug}-dark.gif` **matches** `_TIMELAPSE_RE` but is **rejected** as unexpected canonical inventory (`tests/test_living_art_media.py:435` uses `living-topo-dark.gif`).

---

## Equality assertions (path:line)

Today every published consumer assumes **list equality including order** between `ALL_STYLES` and `LIVING_ART_STYLE_KEYS`, and **set equality** with `STYLE_DIALECTS`.

### Hard equality: `ALL_STYLES` vs `LIVING_ART_STYLE_KEYS`

| Path:line | Assertion | Order? | R5 action |
|---|---|---|---|
| `tests/test_overhaul_fact_ids.py:185` | `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` | yes | Split: `ALL_STYLES == CANDIDATE`; `LIVING_ART_STYLE_KEYS == SHIPPED`; keep `==` **only while** shipped==candidates (until S1/S10) |
| `tests/test_overhaul_fact_ids.py:186-193` | `ALL_STYLES == [inkgarden, topo, genetic, physarum, lenia, ferrofluid]` | yes | Move literal onto `CANDIDATE_STYLE_KEYS` |
| `tests/test_living_art_media.py:1650` | `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` | yes | Same split; this test is named `test_fact_living_art_spine_shared_daily_end_of_day_frames` |
| `tests/test_living_art_media.py:1327-1334` | `ALL_STYLES == [same six]` | yes | Candidate registry, not shipped |
| `tests/test_living_art_contracts.py:231` | `set(ALL_STYLES) == set(LIVING_ART_STYLE_KEYS)` | **set** | After shrink this **must fail** unless rewritten to `SHIPPED ⊆ CANDIDATE`. R5 owns the relax; S6/S10 finish after K2 |

`ALL_STYLES` is a **list**; `LIVING_ART_STYLE_KEYS` is a **tuple**. Ordered checks always wrap `tuple(ALL_STYLES)`.

### Hard equality: `STYLE_DIALECTS` vs `ALL_STYLES`

| Path:line | Assertion | R5 / later |
|---|---|---|
| `tests/test_art_shared_package.py:90` | `set(STYLE_DIALECTS) == set(ALL_STYLES)` | Keep as **candidate** identity. Do **not** retarget at SHIPPED. |
| `tests/test_art_shared_package.py:91-98` | `ALL_STYLES == [same six]` | Candidate list |
| `tests/test_art_shared_package.py:101` | `len(families) == 6` | Six-lock on **candidate** dialect count |
| `tests/test_living_art_media.py:1423` | `families == STYLE_DIALECTS` | Candidate generators |
| `tests/test_living_art_media.py:1424` | `len(set(families.values())) == 6` | Distinct-family six-lock |

### Soft / derived (follow `LIVING_ART_STYLE_KEYS` if R2 points it at SHIPPED)

These are not `ALL_STYLES == KEYS` but they **are** the published roster. After R2 they auto-follow SHIPPED. After S1 they shrink. I05/I06/I07 own the test rewrites; listed here so R1 knows the blast radius.

| Path:line | What it binds |
|---|---|
| `tests/test_living_art_media.py:292` | manifest styles `== set(LIVING_ART_STYLE_KEYS)` |
| `tests/test_living_art_media.py:1668` | on-disk `living-*.gif` `== {living-{k}.gif for k in KEYS}` |
| `tests/test_living_art_artifact_handoff.py:258` | prepare `styles` JSON `== list(KEYS)` |
| `tests/test_living_art_e2e_rehearsal.py:192-193` | `total_assets` / `timelapse_gif` `== len(KEYS)` |
| `tests/test_cli.py` loops | full-fleet generate / refresh uses `KEYS` (see CLI) |
| `tests/test_readme_gfm_ux.py:200-204` | README has `living-{k}.gif` + `.mp4` for every KEY |
| `tests/test_profile_workflow.py:425` | workflow source contains `from scripts.art.artifacts import LIVING_ART_STYLE_KEYS` |
| `tests/test_living_art_contracts.py:240-241` | every KEY has an `animate` module attr via `module_map` |

### Production (not tests) that assume coincidence

| Path:line | Behavior |
|---|---|
| `timelapse.py:436` | `active_styles = styles or ALL_STYLES` |
| `_common.py:119` | default generate `= tuple(LIVING_ART_STYLE_KEYS)` |
| `_common.py:210` | index refresh iff `active_styles == tuple(LIVING_ART_STYLE_KEYS)` |
| `profile-updater.yml:1022-1025` | matrix `json.dumps(list(LIVING_ART_STYLE_KEYS))` |
| `accretion.py:211-212` | unknown slug → `KeyError` (dialects, not inventory) |
| `timelapse.py:483-485` | unknown slug in `styles=` → **warn + skip** (no exit) |

Until K2 these four lists are the same six slugs in the same order (except budgets, which are alpha-by-filename). R5 must not break that while shipped still equals candidates.

---

## CLI behavior

### Typer picker (the one R4 owns)

Entry: `uv run readme generate living-art|timelapse [--only SLUG]`.

Both commands call `_generate_living_art_timelapse` (`art.py:298-307` and `:402-411`).

`_selected_living_art_styles(only)` (`_common.py:109-119`):

1. `only` set and `only not in LIVING_ART_STYLE_KEYS` → print `Unknown style` + `Choose from: {_LIVING_ART_STYLE_HELP}` → `typer.Exit(1)`.
2. `only` set and member → `return (only,)`.
3. `only is None` → `return tuple(LIVING_ART_STYLE_KEYS)` (**default = published keys, not `ALL_STYLES`**).

Help string `_LIVING_ART_STYLE_HELP` (`_common.py:106`) is `_format_style_help(LIVING_ART_STYLE_KEYS)` → `"inkgarden, topo, genetic, physarum, lenia, or ferrofluid"`. Wired into `--only` help on **three** Typer commands: `animated`, `living-art`, `timelapse` (`art.py:160`, `:258`, `:378`).

After a successful render:

- Expected GIF names = `{living-{style}.gif for style in active_styles}` (`_common.py:161`). Incomplete / extra / duplicate / wrong-dir → exit 1 **before** index refresh.
- If `active_styles != tuple(LIVING_ART_STYLE_KEYS)` → **skip** `_refresh_living_art_artifacts` (`_common.py:210-214`). Partial `--only` never publishes a stale-peer manifest.
- If default (full KEYS) → refresh manifest + gallery (+ docs showcase when present).

Covered by:

- `tests/test_service_cli_coverage.py:421-424` — `None` truthy; `"inkgarden"` → `("inkgarden",)`; `"not-a-style"` → `typer.Exit`
- `tests/test_cli.py:129-144` — default generate missing last KEY → exit 1 (`KEYS[:-1]`)
- `tests/test_cli.py:178-214` — `--only topo` does not call refresh
- `tests/test_cli.py:217-247` — `only=None` full KEYS **does** refresh

**Plan match:** today `--only` and default generate both use `LIVING_ART_STYLE_KEYS`. After shrink that would **block** generating a retired candidate. R4 must split: `--only` ∈ `CANDIDATE_STYLE_KEYS`; default list = `SHIPPED_STYLE_KEYS`. Index-refresh equality must compare against **SHIPPED** (full shipped fleet), not candidates.

### `generate all`

`all_cmd.py:215-220` calls `g.living_art(...)` with no `only` → default list. After R4 that becomes SHIPPED only. There is **no** “render every candidate” flag.

### `generate animated --only`

Does **not** go through `_selected_living_art_styles`. Forwards `--only` into `animate.main` (`art.py:193-194`). `animate.py:369-375` validates against local `all_generators` (same six slugs). Unknown → `sys.exit(1)`. Help text still uses `_LIVING_ART_STYLE_HELP` from **KEYS**. After R4, animated help/validation should stay on **CANDIDATE** (legacy growth of a retired generator must remain selectable). Duplicate six-list is a drift risk R1 should absorb or R3 should document.

### Module CLI `python -m scripts.art.timelapse --only`

`timelapse.py:566-584`: `--only` is **not** membership-checked. `styles = [args.only] if args.only else None`. `None` → `render_timelapse` uses `ALL_STYLES`. Unknown slug → warn + skip (`:483-485`), command can exit 0 with **zero** GIFs. Help text at `:569-572` is a **hardcoded** six-name string, not `_format_style_help`. R3/R4 should either reuse candidate keys or keep this path documented as lax. I15 already notes: no Typer completeness check / no index skip on this entry.

### CI

`profile-updater.yml:1022-1025` exports `list(LIVING_ART_STYLE_KEYS)` as the matrix. Each child (`:1090-1098`) runs `generate living-art --only "$LIVING_ART_STYLE"`. After R2, shrinking SHIPPED shrinks CI **without** editing YAML style literals. `max-parallel: 6` (`:1046`) is a separate six-lock (I04/S3).

### Docs mismatch (I16 copy; I15 also recorded)

`living-art-modes.mdx:100` claims a `--only` run “reports the missing styles” if the other five GIFs are absent. **False.** Typer `--only` succeeds with one GIF and skips index refresh. Isolated CI dirs contain one GIF on purpose.

---

## What `roster.py` (R1) must become SSOT for

Plan (`plan.md` Wave ROS + task-graph R1): add `scripts/art/roster.py` with `CANDIDATE_STYLE_KEYS` vs `SHIPPED_STYLE_KEYS` plus legend copy. **Do not delete generators.** Candidates stay selectable.

Until K2 / S1: `SHIPPED_STYLE_KEYS is CANDIDATE_STYLE_KEYS` (same six, same order). Verify: “SHIPPED == current six; legends have title/metaphor/mapping”.

### `CANDIDATE_STYLE_KEYS` — generators / `--only`

Must remain the full inspect/bake-off set even after S1 retires some from daily CI.

| Consumer | Today | After R1–R4 |
|---|---|---|
| `_STYLE_REGISTRY` / `ALL_STYLES` | own copy | R3: keep as candidates; `ALL_STYLES` **is** candidates |
| `STYLE_DIALECTS`, `_DEFAULT_ORIGIN`, `_knobs_for` | own copy | Stay keyed by **candidates** (every generator still has a dialect) |
| Typer `--only` membership + help | `LIVING_ART_STYLE_KEYS` | R4: `CANDIDATE_STYLE_KEYS` |
| `animate.all_generators` | own copy | Import candidate keys (or keep dict but keys ⊆ candidates) |
| `python -m scripts.art.timelapse --only` | lax / hardcoded help | Prefer candidate membership |
| Generator modules | six files | **Never delete** (plan + facts) |
| Dialect / generator tests | hardcoded six | Iterate candidates |
| living-art-modes dialect table | hardcoded six | Candidate dialects (I16: split vs shipped output matrix) |

### `SHIPPED_STYLE_KEYS` — README, CI, budgets, docs

| Consumer | Today | After R1–R4 / S1 |
|---|---|---|
| `LIVING_ART_STYLE_KEYS` | own tuple from labels | R2: **alias of SHIPPED** (keep the name; workflow + tests import it) |
| `LIVING_ART_STYLE_LABELS` | six titles | Subset or filter to SHIPPED for inventory descriptors; full titles live on roster |
| `LIVING_ART_BYTE_BUDGETS` / `_canonical_living_art_names` / `_validate_canonical_inventory` | six GIF names | R2 now; S2 drops retired budgets |
| CI matrix | `list(KEYS)` | Follows SHIPPED automatically |
| Default generate + index-refresh gate | `tuple(KEYS)` | R4: default = SHIPPED; refresh iff active == SHIPPED |
| `generate all` | full `living_art()` | SHIPPED |
| README `_rewrite_living_art_section` items | hardcoded six | M2 should read roster legends; until then a **sixth** copy |
| living-art-modes output matrix | six files | S11 shipped files only |
| Exact-inventory / GFM / e2e counts | `len(KEYS)` or `== 6` | S4–S10 after K2 |

### Legend copy (R1 placeholders; M1 fills)

Need per-style: **title**, **1–2 sentence metaphor**, **repos/stars/commits/followers mapping**.

Today those are **not** on one object:

| Field | Current home |
|---|---|
| Title | `LIVING_ART_STYLE_LABELS` **and** `readme_sections.py` `items[0]` (duplicate) |
| Metaphor | README alt suffixes only (`readme_sections.py:1096-1127`). living-art-modes has prose, not README dropdowns. |
| Four-signal mapping | Machine: `STYLE_DIALECTS` + `_knobs_for` (`accretion.py:166-206`). Human: living-art-modes table `:70-77`. README has **no** dropdown yet (M2). |

R1 should introduce a roster record (title / metaphor / mapping strings) for **every candidate**. SHIPPED is an ordered subset of those keys. M1 (`L-ROS`) fills copy; README assembler consumes SHIPPED legends.

Suggested shape (for R1, not implemented here):

```python
CANDIDATE_STYLE_KEYS: tuple[str, ...] = (
    "inkgarden", "topo", "genetic", "physarum", "lenia", "ferrofluid",
)
SHIPPED_STYLE_KEYS: tuple[str, ...] = CANDIDATE_STYLE_KEYS  # until S1
# plus StyleLegend(title, metaphor, mapping) keyed by candidate slug
```

Invariant R5 should encode **now** (green while equal) and **after S1**:

`set(SHIPPED_STYLE_KEYS) <= set(CANDIDATE_STYLE_KEYS)`  
`ALL_STYLES == list(CANDIDATE_STYLE_KEYS)`  
`LIVING_ART_STYLE_KEYS == SHIPPED_STYLE_KEYS`  
`tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` **only when** shipped == candidates

---

## Implications for R1–R5

| Node | Lock | Do | Do not |
|---|---|---|---|
| **R1** | `L-ROS` | Create `roster.py`: candidate tuple, shipped tuple (= six), legend slots. | Do not shrink shipped. Do not delete generators. Do not edit `artifacts.py` / `timelapse.py` / CLI (those are R2–R4). |
| **R2** | `L-ARTF` | `LIVING_ART_STYLE_KEYS = roster.SHIPPED`; labels/budgets derived from shipped (or filtered). Verify `KEYS == roster.SHIPPED`. | Do not drop budget rows yet (S2). Keep `__all__` name `LIVING_ART_STYLE_KEYS` (workflow + I05 tests). |
| **R3** | `L-TL` | Document `_STYLE_REGISTRY` / `ALL_STYLES` as **candidates**. Optionally `ALL_STYLES = list(CANDIDATE_STYLE_KEYS)` if registry keys stay that tuple. `--only` on module CLI still works for every candidate. | Do not shrink the registry when S1 shrinks shipped. Unknown-style skip at `:483` stays a generator-loop concern. |
| **R4** | `L-CLI` | `--only` ∈ candidates (help lists candidates). Default generate = shipped. Index refresh iff active == shipped. Unknown still exit 1. | Do not validate `--only` against shipped only (that would strand retired bake-off styles). `animated` help should not silently stay on shipped if keys diverge. |
| **R5** | `L-T-MEDIA` | Rewrite the three modules in the task-graph: `test_living_art_media.py`, `test_art_shared_package.py`, `test_overhaul_fact_ids.py`. Replace `ALL_STYLES == KEYS` with the invariant above. Keep `STYLE_DIALECTS` tied to **candidates**. Pytest those modules still green on six. | Do not retarget dialect tests at SHIPPED. Do not rewrite `test_cli.py` here (S8). Do not rewrite contracts `test_all_style_registries_have_same_keys` beyond what R5’s three files cover — that test is S6/S10 (`test_living_art_contracts.py:224-231` **will go red** at S1 unless S6/S10 run). **Gap:** R5’s file list omits `test_living_art_contracts.py`. I99 should assign that assertion to R5 **or** S6/S10 explicitly. |

W1M: “Roster split accepted; shipped still six.” Exact-six tests stay green because SHIPPED is still the six.

S1 later: `SHIPPED_STYLE_KEYS = bakeoff accepted` (4–6). Then KEYS, CI, default generate, budgets (S2), tests (S4–S10), docs (S11–S12) shrink. Candidates and `--only` do not.

---

## Evidence ( reproductions, read-only )

- Registry: `timelapse.py:58-67`.
- Published keys: `artifacts.py:46-54`.
- Dialects: `accretion.py:26-33`; `build_style_dialect` KeyError `:211-212`.
- Typer picker: `_common.py:97-119`, `:210-216`; `art.py` three `--only` options.
- Ordered equality: `tests/test_overhaul_fact_ids.py:185`; `tests/test_living_art_media.py:1650`.
- Set equality: `tests/test_living_art_contracts.py:231`; `tests/test_art_shared_package.py:90`.
- Matrix: `profile-updater.yml:1022-1025`, `:1094`.
- No `roster.py` (`Glob **/roster.py` → 0).

---

## Risks

1. **Four (plus README) copies, no alias.** Adding a seventh candidate requires touching registry, labels, dialects, animate, README items, docs, and hardcoded test lists. R1 exists to stop that — but R2–R4 must actually import roster or the split is theater.
2. **Budgets are not derived from keys.** Inventory can accept a GIF name that is not a slug in `LIVING_ART_STYLE_KEYS` if someone edits only `LIVING_ART_BYTE_BUDGETS`. R2 should derive `living-{slug}.gif` from SHIPPED.
3. **R5 file list misses `test_living_art_contracts.py:224-231`.** `set(ALL_STYLES) == set(KEYS)` is the exact assertion R5 is named for, but the task-graph files are media / shared-package / fact-ids. After S1 that contracts test goes red unless S6/S10 own it.
4. **CLI `--only` == published keys today.** Without R4, S1 would make `generate living-art --only <retired>` exit 1. That violates “candidates stay selectable.”
5. **Index refresh gated on KEYS.** After R4, generating all **candidates** (if someone loops them into one call) must **not** refresh a shipped-only inventory. Default path is fine. No “all candidates” command exists.
6. **`timelapse.main` is lax; Typer is strict.** A retired-or-typo `--only` on the module CLI silently skips. Do not document them as equivalent pickers.
7. **`STYLE_DIALECTS` must not shrink with SHIPPED.** Dialects belong to generators. `len(families) == 6` is a candidate-count lock; after adding a seventh candidate it becomes 7, not `len(SHIPPED)`.
8. **README assembler ignores both registries.** M2 must switch to roster legends or Living Art will keep showing six (or the wrong six) after S1 even if KEYS shrinks.
9. **Docs `--only` “other five” sentence is already wrong** (I15/I16). R4 help text should list candidates; S11 must not describe `--only` as a full-fleet validator.
10. **Type split.** `ALL_STYLES: list` vs `KEYS: tuple`. Roster should pick **tuple** for both public constants so `==` does not depend on wrapping.

---

## Gap

None that block R1. Open assignment for I99: put `tests/test_living_art_contracts.py::test_all_style_registries_have_same_keys` on R5 or S6/S10 so S1 does not surprise that module.
