# I08 — Map accretion knobs + A1 unreadability leftovers

**Date:** 2026-08-18  
**Status:** complete  
**Locks later:** A1/A2 → `L-T-ACC` / `tests/test_art_shared_package.py`; A3 → `L-ACC` / `scripts/art/shared/accretion.py`  
**Deps:** A1 needs W1M + this node. D\*1 all need A3. D\*2 serialize on `L-T-ACC` after D\*1.

**Target (plan):** four-signal spine (repos / stars / commits / followers), per-style dialect knobs, what A1–A3 must add, what must **not** change unless all six need the same clock.

Cross-check (read, not rewritten): I09–I14 are present and agree the unreadability is **on-canvas**, not a missing overlay. Profile-overhaul A1 leftovers: t0 empty garden, physarum spore-only, ferrofluid clustered.

## Scope

Map `scripts/art/shared/accretion.py` (channels, dialects, knobs, ceilings, overlay) and the only shared-package test that touches it (`tests/test_art_shared_package.py:69-101`). Relate that clock to generator consumption, to `tests/test_living_art_media.py` overlay/isolation tests, and to the 2026-08-14 A1 stills.

This node does **not** redesign a generator. I09–I14 own Dig1…Dfe1. I08 owns the **shared contract** A1–A3 will write against.

---

## The two clocks (do not conflate)

| Clock | File | What it is | A3? |
|---|---|---|---|
| **Daily spine** | `scripts/art/daily_snapshots.py:1440` `validate_snapshot_monotonic_contract` | Frame t = cumulative end-of-day world. Keys include `stars`, `followers`, `total_commits`, `public_repos` (`:36-49`). Called from `timelapse.py:457`. | **No.** `L-TL` / snapshots. |
| **Accretion dialect** | `scripts/art/shared/accretion.py` | Same four totals → log 0–1 scales → per-style knobs + overlay ticks. | **Yes, only if all six need the same formula.** |
| **Maturity / atmosphere** | `scripts/art/shared/world_state.py:167-195` `compute_maturity` | Smoothstep mix of commits, stars, last-year contribs, followers, repo count, age, forks, network. A1 t0 `1/2/20/1` ≈ **0.012**. | **No.** Gates plants/veins/peaks. That is D\*1. |
| **Composite signals** | per generator (`_compute_ferrofluid_signals`, `_derive_landscape_dynamics`, …) | PRs, streaks, traffic, star velocity. Move the picture **without** the four knobs. | **No.** |

GIF worker (`scripts/art/timelapse.py:90-96`): `generate(metrics_dict, seed=…, maturity=snapshot.maturity, timeline=False)`. Isolation fixtures must use that path (`timeline=False`, no `evolution_state`).

`resolve_render_metrics` (`scripts/art/shared/metrics.py:182-202`) overlays `evolution_state` then `render_state` **before** most generators call `build_style_dialect`. A1 isolation payloads must not carry those envelopes unless the test is specifically about the envelope.

`is_monotonic_timelapse_metrics` (`metrics.py:205-215`) is true when `render_state` or `cumulative_state` is present. It is **not** an accretion test. Snapshot GIFs always hit it; A1 stills (`timeline=False`, no wrapper) often do not.

---

## Four-signal spine

```16:24:scripts/art/shared/accretion.py
ACCRETION_CHANNELS: tuple[str, ...] = ("repos", "stars", "commits", "followers")

# Log ceilings keep early growth visible instead of saturating at tiny totals.
_CHANNEL_CEILINGS: dict[str, float] = {
    "repos": 36.0,
    "stars": 500.0,
    "commits": 8000.0,
    "followers": 400.0,
}
```

Docs matrix: `docs/content/docs/scripts/living-art-modes.mdx:62-81`. Module docstring (`accretion.py:1-7`): all six consume one spine; dialects differ; the clock does not.

Facts (`facts.md`): each shipped piece has a real mapping for all four; not every signal must scream; overlay/README legend teaches; mood-only is not enough. The **intro sentence must not list the spine** (README Wave RM). Engineering SSOT **should** keep this table.

### `extract_accretion_channels` (`accretion.py:146-163`)

| Channel | Source | Notes |
|---|---|---|
| **repos** | `_repo_count` (`76-91`) | Prefer `repos` list with named dicts; else typed dicts; else `top_repos` the same way; else `public_repos`. Empty/`None` → 0. |
| **stars** | `metrics["stars"]` via `_as_nonneg_int` | **Must be a scalar.** A history-shaped **list** of star events raises `TypeError` inside `int()` and becomes **0**. Snapshots already flatten via `cumulative_state["stars"]` (`daily_snapshots.py:457`). |
| **commits** | `total_commits` | Not `contributions_last_year`, not monthly series. |
| **followers** | `followers` | Scalar. |

`_as_nonneg_int` (`69-73`): `TypeError`/`ValueError` → 0. No logging.

Snapshot `metrics_dict` already has int `stars` / `total_commits` / `followers` plus a `repos` list (`daily_snapshots.py:455-470`). Live GIF path is safe. Isolation tests that pass raw `fetch_history` payloads would silently zero stars.

Public re-export: `scripts/art/shared/__init__.py:22-31` (`ACCRETION_CHANNELS`, `STYLE_DIALECTS`, `AccretionChannels`, `StyleDialect`, `accretion_log_scale`, `build_style_dialect`, `channel_mark_count`, `dialect_group_markup`, `extract_accretion_channels`). **Not** exported: `_CHANNEL_CEILINGS`, `_knobs_for`, `_FAMILY_INK`, `_DEFAULT_ORIGIN`.

### Log scale + tick count

`accretion_log_scale` (`54-57`): `min(1, log1p(value) / log1p(ceiling))`, floor value at 0, ceiling at ≥1.

`channel_mark_count` (`61-66`): `0` if value ≤ 0; else `1 + round(scale * 7)` clamped to a 0–8 tick. **First unit always draws ≥1 overlay tick.** Overlay `_channel_glyph` then caps drawn ticks at **6** (`232`).

A1 inspection spine `1/2/20/1` → `2/24/400/18` → `4/120/2400/80` (same helper as `tests/test_living_art_media.py:1144-1210`):

| Frame | repos / stars / commits / followers | repo_scale | star_scale | commit_scale | follower_scale | overlay mark_counts (approx) |
|---|---|---|---|---|---|---|
| t0 | 1 / 2 / 20 / 1 | 0.192 | 0.177 | 0.339 | 0.116 | 2 / 2 / 3 / 2 |
| t1 | 2 / 24 / 400 / 18 | 0.304 | 0.518 | 0.667 | 0.491 | 3 / 5 / 6 / 4 |
| t2 | 4 / 120 / 2400 / 80 | 0.446 | 0.771 | 0.866 | 0.733 | 4 / 6 / 7 / 6 |

Root + overlay attrs on every A1 still already show these numbers (`data-accretion-*` from `StyleDialect.svg_attrs`, `accretion.py:129-143`). **Overlay-yes is not growth-readable.** That is the leftover.

`test_style_dialects_make_accretion_readable` (`tests/test_living_art_media.py:1341-1394`) already asserts overlay counts/scales/mark_counts rise t0→t2 for **all six**. A1 must not duplicate that as the new contract.

---

## Dialects (families, overlay, origins)

```26:51:scripts/art/shared/accretion.py
STYLE_DIALECTS: dict[str, str] = {
    "inkgarden": "botanical",
    "topo": "cartographic",
    "genetic": "fitness",
    "physarum": "mycelial",
    "lenia": "morphogenetic",
    "ferrofluid": "magnetic",
}
```

Keys **must** stay `== ALL_STYLES` (`timelapse.py:67`). The one shared-package test asserts that (`test_art_shared_package.py:90-101`) plus six distinct families.

`build_style_dialect` (`209-219`): unknown style → `KeyError`. `_knobs_for` default for a hypothetical extra key is `{}` (`206`) but `STYLE_DIALECTS` rejects first.

`dialect_group_markup` (`453-513`): always a 248×28 `#accretion-dialect` strip (`id` + `data-role="accretion-dialect"`). Family word + four `data-channel` groups (glyph + raw number). Generators append it last (I09–I14). **A2/D\*2 must strip or ignore this node.** Counting overlay ticks will pass today on the unreadability leftovers.

| Style | Family | Overlay origin (`_DEFAULT_ORIGIN`) | Family ink / paper |
|---|---|---|---|
| inkgarden | botanical | (40, 742) bottom-left | `#6a5a4a` / `#f5f0e6` |
| topo | cartographic | (58, 708) bottom margin | `#4a3a28` / `#f3ead8` |
| genetic | fitness | **(44, 44) on the map** | `#9ec9d8` / `#1a1a2e` |
| physarum | mycelial | (40, 742) | `#d4c07a` / `#14120c` |
| lenia | morphogenetic | (516, 36) top-right | `#7ee0c0` / `#0c1020` |
| ferrofluid | magnetic | (516, 742) below pool | `#8ec8ff` / `#0a0c14` |

Glyphs (`_channel_glyph`, `222-450`): per-family, per-channel. Magnetic family is the **fallback `else`** after morphogenetic (`421`), not a named `if family == "magnetic"`. Cosmetic only.

Moving overlay origins is an A3 change (shared markup). I11 notes genetic’s strip can cover a NW peak. That is **one style**, so Dge1 should dodge the peak rather than A3 moving every origin unless all six collide.

---

## Per-style knobs (`_knobs_for`, `accretion.py:166-206`)

Docs: `living-art-modes.mdx:70-81`. **Repo count has no named knob in any dialect.** Overlay still ticks it. Generators encode repos as plant / hill / peak / node / seed / dipole **count**.

| Style | stars | commits | followers | Floor behavior |
|---|---|---|---|---|
| **inkgarden** | `bloom_scale = 0.55 + 1.85 * star_scale` | `trunk_scale = 0.70 + 1.50 * commit_scale` | `glint_count = 0` if followers≤0 else `1 + 7 * follower_scale` | Only **followers** hard-zero until first unit |
| **topo** | `prominence_scale = 0.70 + 0.90 * star_scale` | `contour_gain = 0.35 + 0.65 * commit_scale` | `settlement_gain = follower_scale` | Followers 0–1, no floor |
| **genetic** | `peak_scale = 0.72 + 0.70 * star_scale` | `generation_gain = commit_scale` | `colony_gain = follower_scale` | |
| **physarum** | `nutrient_scale = 0.70 + 0.80 * star_scale` | `trail_scale = 0.72 + 0.55 * commit_scale` | `vein_gain = follower_scale` | |
| **lenia** | `halo_scale = 0.75 + 0.70 * star_scale` | `field_gain = commit_scale` | `extent_gain = follower_scale` | |
| **ferrofluid** | `spike_scale = 0.70 + 0.80 * star_scale` | `ripple_gain = commit_scale` | `field_gain = 0.82 + 0.30 * follower_scale` | Followers **compressed** (floor 0.82, gain 0.30) |

A1-spine knob values (from the formulas + scales above):

| Style | t0 knobs | t2 knobs |
|---|---|---|
| inkgarden | bloom 0.877, trunk 1.209, glint 1.81 | bloom 1.98, trunk 2.00, glint 6.13 |
| topo | prominence 0.859, contour 0.570, settlement 0.116 | 1.394 / 0.913 / 0.733 |
| genetic | peak 0.844, generation 0.339, colony 0.116 | 1.260 / 0.866 / 0.733 |
| physarum | nutrient 0.842, trail 0.906, vein 0.116 | 1.317 / 1.196 / 0.733 |
| lenia | halo 0.874, field 0.339, extent 0.116 | 1.290 / 0.866 / 0.733 |
| ferrofluid | spike 0.842, ripple 0.339, field **0.855** | 1.317 / 0.866 / **1.040** |

Ferrofluid followers only move the leased knob **22%** t0→t2. That formula is **style-local**. A3 must not “fix” it unless every follower lease is equally dead — I09–I14 say no.

### Name collisions (do not “unify” in A3)

| Name | Accretion knob | Other meaning |
|---|---|---|
| `field_gain` | Lenia **commits**; ferrofluid **followers** | `FerrofluidSignals.field_gain` composite (`ferrofluid.py:400-421`), product with dialect knob at `1015` |
| `peak_scale` | Genetic **stars** (`0.72+0.70*star_scale`) | `CFG.peak_scale = 8.0` height multiplier (`genetic_landscape.py`) |
| `spike_scale` | Ferrofluid **stars** | `CFG.spike_scale = 80` px (`ferrofluid.py:57`) |

A3 keep lease names as documented. Dfe1/Dge1 may rename **local** composites (I14 already asks to disambiguate ferrofluid’s two `field_gain`s).

---

## How generators consume knobs (I09–I14, not rewritten)

Every `generate()` calls `build_style_dialect("<key>", metrics)` then reads `dialect.knobs[...]`. Overlay always appended.

| Channel | Intended picture (plan / docs) | On-canvas today (inventory consensus) | Knob attrs on SVG root? |
|---|---|---|---|
| **repos** | Count of primary marks | Count exists (after I09 plant rescue / I12 node floor). Overlay ticks. Isolation of **count** missing from `test_leased_style_knobs_*` except ink/physarum early-spine. | No repo knob. `data-accretion-repos` only. |
| **stars** | Bloom / peaks / halo / nutrient / spike height | Wired into knobs; **picture weak** (topo `prominence_scale` never enters terrain height — I10; genetic no stars isolation — I11; ferro no stars branch — I14). | Lenia `data-halo-scale`. Others mostly not. |
| **commits** | Trunk / contours / generations / trails / field / ripples | Attrs often move; **picture is caption** (lenia mix cap 0.34 — I13; ferro ripples op ~0.09 — I14; topo contours already dominate — I10). | Topo no `data-contour-gain`. Genetic `data-generations`. Lenia `data-field-gain`. Ferro `data-ripple-gain`. |
| **followers** | Glints / settlements / colonies / veins / extent / field | Ink glints leak `star_velocity` (I09). Topo settlements ~1 px (I10). Genetic colonies op 0.04 (I11). Physarum veins gated (I12). Lenia satellites hug host (I13). Ferro floor 0.82 (I14). | Topo `data-settlement-*`. Genetic `data-colony-*`. Lenia `data-extent-gain`. Ink/physarum/ferro dialect follower knobs **not** serialized. |

Leaks A1 isolation **must pin**:

- Ink `glint_count` vs `star_velocity` fireflies (`I09`; `test_ink_and_physarum_knobs_track_stars_and_followers` does **not** pin velocity — `test_living_art_media.py:1616-1632`).
- Topo `n_levels += followers//10` (`I10`) — commits isolation must freeze followers.
- Genetic `pop_count *= (1 + … + 0.55 * follower_scale)` (`I11`) — followers leak into the swarm.
- Ferro `signals.field_gain` includes `0.14 * follower_scale` **and** `0.08 * star_scale` (`ferrofluid.py:418-419`) **and** dialect `field_gain`.

---

## A1 unreadability leftovers (2026-08-14 stills)

Sources: `goals/profile-readme-overhaul/inventory/A1.md`, `A1-frame-inspection.md`, `inventory/frames/<style>-t{0,1,2}.svg`. Spine `1/2/20/1` → `2/24/400/18` → `4/120/2400/80`. `timeline=False`. Published `living-*.gif` **not** regenerated (I15 / plan). Do not reuse those SVGs as bake-off evidence (D\*3).

| Style | A1 caption | Stale still vs live `generate()` (I09–I14) | A2 t0 primary mark | Isolation leftover for A1 red tests |
|---|---|---|---|---|
| **inkgarden** | t0 sparse / maturity gate; overlay yes | Stale t0: **0** `repo-tree` (CSS only). Live GIF path: ≥1 plant at `tree_t≥0.34` (`I09`; `test_early_spine…` already requires `ink_plants[0]≥1`). Bloom gate 0.48 still hides flowers. | Visible **plant** (stem+canopy), not overlay ticks | bloom / trunk / glints on canvas; pin `star_velocity=0` |
| **topo** | Overlay + peaks grow; **stars weaker than contours** | Not empty. Settlement attrs present. Stars never enter `peak_h`. | Visible **repo-peak**, not the anonymous central blob | stars → summit height even when per-repo star **ratio** is fixed |
| **genetic** | Overlay + peaks grow; colonies on t2 | Not empty. 1 peak + 37 dots. Colony op 0.04. | **Peak core** brighter than swarm | stars height; drawn colonies; gens without scribble; distinct peaks |
| **physarum** | Food after t0; **t0 spore-only** | Stale still: spore only. Live GIF: spore **+ node floor 0.64**, **0 veins** (`growth_mat≈0.01 < 0.05`). SMIL early frames still spore-only. | **Network** (node **and** veins), not spore | veins from first repo; followers must not drop vein mass |
| **lenia** | Overlay + repo seeds grow | Not empty. Halo op ~0.03; CA killed by mix≤0.34. | Visible **organism**, not overlay | field mass / extent spread, not only `data-field-gain` |
| **ferrofluid** | Overlay + ripple mass; **same-language dipoles clustered** | t0 **has** a tower (1 dipole, 17 spikes). t2 still: four Python `cx` 82.2 / 100.9 / 129.3 / 235.7, min gap **18.7** (pre-spread). Live `_spread_positions` min 44 px; media test only requires ≥36 (`:1609-1613`). | t0: one **tower**. Clustering is **not** an empty-t0 bug | N repos → N **columns** (same-language fixture); stars height; followers a visible field mark |

Plan research note (`plan.md:33`): “Those three are the first dialect leaves” = inkgarden empty, physarum spore, ferrofluid clustered. I10/I11/I13 add **caption-only** growth for the other three. A1 tests must cover **all six**, not only the named three.

---

## Existing tests (what A1 must not clone)

### `tests/test_art_shared_package.py` — A1/A2 **write here**

| Lines | What it asserts | Gap |
|---|---|---|
| `23-36` | `accretion` is an importable submodule | keep |
| `69-101` `test_accretion_channels_and_style_dialects_stay_distinct` | extract 2/24/400/18; `star_scale∈(0,1)`; `STYLE_DIALECTS` keys `== ALL_STYLES` exact six; six families | **No** log monotonicity, mark_count, knob isolation, unknown-style, t0 generate, repo-count fallbacks |

No `accretion_log_scale` / `channel_mark_count` / `_knobs_for` tests anywhere under `tests/` (repo grep).

S10 later rewrites `ALL_STYLES` assertions when shipped shrinks. Until K2, keep six.

### `tests/test_living_art_media.py` — already green, **insufficient**

| Test | Lines | Covers | Misses |
|---|---|---|---|
| `_accretion_metrics` | `1144-1210` | Shared A1 spine fixture; **sets `star_velocity` from stars** | Follower isolation leak for ink |
| `test_shared_daily_spine_from_account_creation_enforces_monotonic_contract` | `1263-1334` | Snapshot **calendar** monotonicity | Not dialect knobs |
| `test_style_dialects_make_accretion_readable` | `1341-1394` | Overlay t0→t2 all six | All channels move **together**; overlay only |
| `test_living_art_dialects_remain_visually_distinct` | `1397-1424` | Family strings | Not pictures |
| `test_leased_style_knobs_track_isolated_channels` | `1453-1537` | topo followers attrs; genetic colony/gens attrs; lenia halo/field/extent attrs; ferro **commits→ripples only** | **No inkgarden, no physarum, no repos-only, no topo stars/commits, no genetic stars, no ferro stars/followers** |
| `test_early_spine_dialects_keep_repo_accretion_readable` | `1577-1613` | ink plants + phys nodes monotonic; ferro t2 gap≥36 | genetic/topo/lenia omitted; ferro **no t0 dipole≥1**; gap≠towers |
| `test_ink_and_physarum_knobs_track_stars_and_followers` | `1616-1640` | ink fireflies vs followers; phys max node r vs stars | No `star_velocity=0`; no commits; no repos |

A1/A2 live in `test_art_shared_package.py` so D\*2 serialize on `L-T-ACC` as planned. **Do not move the contract into `test_living_art_media.py`.** Existing media tests stay as regression; they are not the red bar.

---

## What A1–A3 must add

Plan (`plan.md:91`, `214-216`; `task-graph.json` A1–A3):

| ID | Lock | File | Title | Verify |
|---|---|---|---|---|
| **A1** | `L-T-ACC` | `tests/test_art_shared_package.py` | Each dialect knob moves when each of 4 channels increases | “new tests fail or skip until dialects ship” |
| **A2** | `L-T-ACC` | same | t0 frame contains style primary mark (not empty field) | “t0 plant/contour/peak/vein/creature/spike asserted” |
| **A3** | `L-ACC` | `scripts/art/shared/accretion.py` | Adjust shared knobs/ceilings **only if all six need the same clock change** | “monotonic contract + extract_accretion_channels still hold” |

### A1 — red tests (channel isolation)

**Trap:** asserting only `build_style_dialect(…).knobs` is **green today** for stars/commits/followers. That would not drive Dig1…Dfe1 and would violate verify “fail or skip until dialects ship.”

Write **two layers** in the same file:

1. **Clock unit tests (green now, keep green through A3):**
   - `extract_accretion_channels` on the A1 spine and on fallbacks (`top_repos`, `public_repos`, unnamed dicts, list-valued `stars` → 0).
   - `accretion_log_scale`: 0→0, monotonic, ceiling→1, above ceiling clamps.
   - `channel_mark_count`: 0→0, first unit ≥1.
   - `build_style_dialect` unknown style → `KeyError`.
   - Per-style **knob dict** isolation: increase one of stars/commits/followers, assert the leased knob **strictly increases** and the other two leased knobs **do not**.
   - **Repos have no knob.** Assert `channels.repos` / overlay `mark_count("repos")` move, and that **no** stars/commits/followers knob moves when only the repo list grows (same stars/commits/followers scalars).
   - Ink: `glint_count == 0` at 0 followers.

2. **On-canvas isolation (red or `pytest.xfail` / skip until D\*1, then D\*2 greens):** parametrize all six styles. Fixture = `_accretion_metrics` with **`star_velocity` forced `{recent_rate:0, peak_rate:0}`**, `timeline=False`, no `evolution_state`. For each channel, generate low vs high **one channel only**, strip `#accretion-dialect`, assert the **picture** mark from I09–I14 (plants, `repo-peak` height, `genetic-peak-core` radius, tagged physarum veins, lenia field mass, ferro columns). Repos-only must move **count of primary marks**.

Do **not** require overlay `data-mark-count` as the pass condition for layer 2.

Suggested primary-mark selectors (A2 + isolation; overlay ignored):

| Style | t0 / repos mark | stars | commits | followers |
|---|---|---|---|---|
| inkgarden | `class="repo-tree"` with visible canopy | blooms | trunk stroke/length | fireflies with velocity pinned |
| topo | `data-role="repo-peak"` | summit / `prominence` geometry | contour identity **without** `followers//10` | settlement geometry, not 1 px |
| genetic | `genetic-peak-core` **brighter than** organism dots | core/glow radius | generation mark ≠ scribble | `gl-micro-colony` ink, 0 at 0 followers |
| physarum | node-core **and** tagged veins | max core r | vein width/mass | vein spread **non-decreasing** |
| lenia | `lenia-seed-halo` / organism op above GIF floor | halo r **and** ink | field occupancy | satellite **spread** |
| ferrofluid | `ferro-dipole` / canonical tower | max spike height | visible ripples (or replacement) | leased field mark, not composite soup |

Same-language ferro fixture (all Python) for repos isolation — I14: alternating Py/Go still shares x-fraction 0.25.

### A2 — red tests (t0 primary mark)

Same spine t0: `_accretion_metrics(repos=1, stars=2, commits=20, followers=1)`, `timeline=False`.

Assert **not empty field** with overlay stripped:

| Style | Pass today if you only count | Fail if you require the plan leftover |
|---|---|---|
| inkgarden | live `repo-tree≥1` (stale A1 SVG would fail) | readable plant at 400×400 (bloom/canopy) |
| topo | `repo-peak≥1` | peak ink > contour / no taller anonymous center |
| genetic | `genetic-peak-core≥1` | peak > swarm |
| physarum | `physarum-node-core≥1` (live); spore-only still fails | veins present — **this is the leftover** |
| lenia | seed/organism count≥1 | opacity/size at GIF scale |
| ferrofluid | `ferro-dipole≥1` + spikes | t0 is already a tower; do **not** encode clustering here |

A2 verify wording (“plant/contour/peak/vein/creature/spike”) maps: plant=ink, contour/peak=topo+genetic, vein=physarum **network**, creature=lenia, spike=ferro. Counting overlay glyphs must not satisfy this.

Empty / 0-repo payloads may stay blank (account-created-before-first-repo). A2 uses **1 repo**. Ferro empty-metrics fake ripples (`I14`) must not count.

### A3 — shared clock only if all six need it

I09–I14 **unanimous**: do the redesign in the generator; keep lease names; do not retune `_CHANNEL_CEILINGS` in D\*1.

**Default A3: documented no-op** (file untouched except maybe a comment / export of ceilings if tests need them). That still satisfies “only if all six need the same clock change.”

**Allowed A3 changes** (all six, same formula):

- Raise/lower `_CHANNEL_CEILINGS` if the **real account saturates** the log band for every style (mock `wyatt` profile is stars 42 / commits 4800 / followers 85 / repos 35 — commits already 0.95 of 8000; stars 0.29 of 500). Only then.
- Change `accretion_log_scale` / `channel_mark_count` if every overlay **and** every knob is judged too compressed at first-unit growth.
- Add a **fourth named repo knob to every dialect** if A1 cannot express repos-only without it. Prefer **not to**: count of primary marks is the repo encoding (`living-art-modes.mdx:79-81`).
- Shared overlay geometry if **all six** registers hide the picture (only genetic origin is on-map today → Dge1).

**A3 verify:** re-run `test_accretion_channels_and_style_dialects_stay_distinct`, new A1 clock unit tests, and `validate_snapshot_monotonic_contract` (snapshots, not accretion.py). Extract key names (`stars` / `total_commits` / `followers` / repo list) must still match `daily_snapshots` cumulative fields.

---

## What must NOT change unless all six need the same clock

Do **not** touch in A3 / `L-ACC` (leave to D\*1 or other locks):

| Surface | Why |
|---|---|
| `ACCRETION_CHANNELS` order/names | README legends + overlay + facts spine |
| `STYLE_DIALECTS` keys | Roster / `ALL_STYLES` (ROS / S1 later). Families stay 1:1 with styles. |
| Extract key mapping (`stars`, `total_commits`, `followers`, repo list) | Must match snapshot `metrics_dict` |
| Per-style **knob names** (`bloom_scale`, …) | Docs + generator reads. One-style retune = D\*1. |
| Per-style **floors** (ink `glint_count` zero; ferro `field_gain` 0.82) | Not a shared clock. Ferro compression is Dfe1. |
| `compute_maturity` / fade gates | Maturity is why t0 looks empty; D\*1 ungates **marks**, not the calendar. |
| `validate_snapshot_monotonic_contract` / `sample_frames` | Daily spine (`L-TL`) |
| `resolve_render_metrics` / `evolution_state` | Envelope (`metrics.py`) |
| `dialect_group_markup` as the **picture** | Do not “fix” unreadability by enlarging the caption. Overlay may stay. |
| `language_cluster` in `visual.py` | Shared helper; I14: Dfe1 **stop calling it**, do not rewrite the helper (genetic/physarum/lenia still use it). |
| `test_living_art_media.py` overlay tests | Stay green; not the A1 file. |
| OpenSpec `prevent-living-art-repo-growth` | Out of scope (I19 / S15). |

D\*1 after A3: if A3 is a no-op, generators still consume today’s knobs. That is intended.

D\*2 after D\*1: green the A1/A2 tests in `test_art_shared_package.py` only. Serialize on `L-T-ACC`. Prefer one greening agent after all D\*1 (`plan.md:51,164`).

---

## Implications for later nodes

| Node | Use this inventory |
|---|---|
| **A1** | Two-layer tests; reuse `_accretion_metrics` spine; pin velocity; xfail on-canvas until D\*1. |
| **A2** | t0 1-repo GIF kwargs; ignore overlay; physarum = veins, not spore; ferro t0 = tower exists. |
| **A3** | Default no-op. Consensus of I09–I14. |
| **Dig1…Dfe1** | Consume knobs; make the **picture** match the table; do not edit `accretion.py`. |
| **D\*2** | Green A1/A2. `pytest -k <style>` also hits generator modules outside `L-T-ACC` — coordinate goldens (I09 ink fixtures). |
| **D\*3** | New stills under `goals/living-art-overhaul/bakeoff/`. Do not reuse 2026-08-14 frames. |
| **M1** | Legend copy should match this knob table (repos=count, then three leases). |
| **S10** | Exact-six `ALL_STYLES` assert in `test_art_shared_package.py:91-98` becomes exact-shipped. |

---

## Finding

The shared accretion clock already extracts repos / stars / commits / followers, log-normalizes them to 36 / 500 / 8000 / 400, and maps stars/commits/followers onto six distinct knob triples. Overlay ticks and `data-accretion-*` already grow t0→t2 on every style (`test_style_dialects_make_accretion_readable`). **Repos have no named knob.** Unreadability leftovers are **generator gates and layouts**, not a missing extract: Ink Garden’s signed t0 still is empty (live code draws a bloom-gated stub); Physarum t0 is a network-less spore (live GIF adds a node, still no veins); Ferrofluid t0 is a real tower but t2 same-language dipoles remain a hedge. Topo/genetic/lenia already have t0 marks that fail as **learnable channels** once the overlay is ignored. `test_art_shared_package.py` does not yet isolate knobs or assert t0 primary marks.

## Gap

A1 must add clock unit tests **and** red/xfail on-canvas isolation (knob-only tests would pass now and stall the dialect wave). A2 must assert t0 primary marks with `#accretion-dialect` ignored; physarum’s mark is veins. A3 should not retune ceilings or lease names unless all six inventories show the same clock failure — they do not. I09–I14 already assign the picture work to D\*1.

Verify later: `uv run python -m pytest -q tests/test_art_shared_package.py` after A1–A2; A3 only if that bar proves the **shared** formulas are the bottleneck.
