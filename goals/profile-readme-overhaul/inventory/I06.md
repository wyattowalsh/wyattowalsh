# I06 — Map tech-stack summary regex + test fixtures for 200+

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/readme_sections.py`:

- `_TECH_STACK_BARE_SUMMARY = "<summary><strong>Tech Stack</strong></summary>"`
- `_TECH_STACK_SUMMARY_RE` rewrites any `<summary><strong>…</strong></summary>` after `<details>` to that bare label.
- `_TECH_STACK_TEASER_RE` strips H2-to-details teaser copy down to `## Tech Stack\n\n`.

Live `README.md`:

```html
<summary><strong>Tech Stack</strong></summary>
```

`rg "200\\+" README.md` is empty. Tests keep the old “View full stack (200+ technologies)” string **only as rewrite input** (`tests/test_readme_sections.py`, `tests/test_readme_gfm_ux.py`) and assert the output has no `200+` and no “View full stack”.

## Finding

Summary is a bare label. 200+ copy is stripped by rewrite regexes; current README is clean.

## Gap

None.
