# I10 — Map blog RSS → SVG card fields

**Date:** 2026-08-14  
**Status:** complete

## Evidence

- `config.yaml`: `blog_feed_url: https://w4w.dev/feed.xml`, `blog_post_limit: 5`.
- Worktree `_render_blog_posts` builds one `SvgCard` per post (title, `_one_line_hook`, `_normalize_blog_date`) and writes **per-post** SVGs via `_make_blog_asset_name` + `_render_card_svg_asset`. Each card is its own `<a href=post><img></a>`; caption is `title · date · hook`. Empty-feed fallback still uses combined `blog-posts.svg`.
- `_rewrite_blog_disclosure` unwraps any `<details><summary>Latest Blog Posts</summary>…` so the strip stays in the open flow.

Live committed `README.md` (re-checked 2026-08-14, HEAD `f8c3a3de7c9c295f1c1a088d8b85883701cfe715`): `## Latest Blog Posts` then **one** `<a>` wrapping `blog-posts.svg` (href = first post) plus five `www.w4w.dev/blog/posts/…` text links `title — YYYY-MM-DD` with **no** hooks. No blog `<details>`. Subline: “Auto-updated from RSS feed” → `https://w4w.dev/feed.xml`. Leftover unused `readme/blog-*.svg` files sit beside `blog-posts.svg`. Designed board item rows (`blog-posts.svg` L38–49) are title+date only; hook is featured-only + combined alt.

## Finding

Worktree generator is 4–5 independently linked cards. origin/`dev` README preview is still one composite board + one wrap href. Not collapsed.

## Gap

fact-blog on the shipped preview: not 4–5 independently linked cards each carrying title+date+hook+link. Finalize has not republished the per-card strip.
