---
title: GitHub Actions Workflows
description: The Unified Profile Updater workflow runs every generator job on a daily schedule.
---

## Workflow file

`.github/workflows/profile-updater.yml`

**Triggers:** `push` to `main`/`master`/`dev`, daily at 01:00 UTC, `workflow_dispatch`.

## Job graph

```text
update-starred-lists
        ▼
generate-assets
        ▼
generate-profile-metrics
        ▼
generate-event-art
        ▼
update-readme-wakatime
        ▼
update-skills
```

## Job details

### `update-starred-lists`

Uses the `starred` CLI to rebuild `.github/assets/topics.md` and `.github/assets/languages.md` from your GitHub stars.

```bash
uv run starred --username wyattowalsh --sort \
  --filename .github/assets/languages.md
uv run starred --username wyattowalsh --sort --topic --topic_limit 500 \
  --filename .github/assets/topics.md
```

Commits both files with `chore(data): update awesome-stars lists`.

### `generate-assets`

Depends on `update-starred-lists`. Generates:
- QR code — `generate qr`
- Word cloud by topic — `generate word-cloud --from-topics-md` → `wordcloud_metaheuristic-anim_by_topics.svg`
- Word cloud by language — `generate word-cloud --from-languages-md` → `wordcloud_metaheuristic-anim_by_languages.svg`

Commits to `.github/assets/img/`.

### `update-readme-wakatime`

Uses [`anmol098/waka-readme-stats`](https://github.com/anmol098/waka-readme-stats) to inject WakaTime coding stats. Requires `WAKATIME_API_KEY` secret.

### `generate-profile-metrics`

Uses [`felipecrs/metrics`](https://github.com/felipecrs/metrics) — a fork of [`lowlighter/metrics`](https://github.com/lowlighter/metrics) pinned at `b300bc3a0fcf0fa3cbb101064948c0f2126c3f25` — to generate:
- `metrics.svg` — personal metrics (lines, isocalendar, languages, habits, notable, topics)
- `metrics.additional.svg` — music (Spotify), activity, stargazers, featured repos

`METRICS_TOKEN` is the GitHub PAT for repository-aware sections such as traffic, notable repositories, stars, and stargazers. If it is missing, the workflow falls back to `GITHUB_TOKEN` and disables the repo-sensitive plugins that need broader access.

Each SVG is generated progressively: the workflow tries the richer plugin set first, then retries with a stable subset if validation fails (`plugin_habits` off for personal metrics; `plugin_activity` and `plugin_music` off for additional metrics). Placeholder SVGs are only written if every attempt still fails validation.

### `generate-event-art`

Fetches live metrics + history via `scripts/fetch_metrics.py` and `scripts/fetch_history.py`, then runs `uv run python -m scripts.cli generate living-art` to refresh the canonical living-art timelapse surface.

The job stages and commits:

- `.github/assets/img/living-*.gif`
- `.github/assets/img/living-art-manifest.json`
- `.github/assets/img/living-art-preview.html`
- `docs/public/showcase/living-*.gif`
- `docs/public/showcase/living-art-manifest.json`
- `docs/public/showcase/living-art-preview.html`

The uploaded staging bundle also includes the fetched `metrics.json` and `history.json` inputs plus the mirrored docs showcase surface for review.

### `update-skills`

Depends on `update-readme-wakatime`. Runs:
- `generate readme-sections` — assembles all dynamic README sections
- `generate skills` — re-renders skills badges

Commits `README.md` + `.github/assets/img/readme/*.svg`.

## Secrets used by the workflow

| Secret | Used by | Notes |
|---|---|---|
| `GITHUB_TOKEN` | All jobs | Auto-provided by Actions |
| `METRICS_TOKEN` | `generate-profile-metrics` | Optional PAT for repo-aware metrics (`read:user`, `public_repo`); workflow falls back to `GITHUB_TOKEN` when absent |
| `WAKATIME_API_KEY` | `update-readme-wakatime` | Optional WakaTime API key; README WakaTime update is skipped when absent |
| `SPOTIFY_CLIENT_ID` | `generate-profile-metrics` | Optional Spotify OAuth secret for the music plugin |
| `SPOTIFY_CLIENT_SECRET` | `generate-profile-metrics` | Optional Spotify OAuth secret for the music plugin |
| `SPOTIFY_REFRESH_TOKEN` | `generate-profile-metrics` | Optional Spotify OAuth secret for the music plugin |

## Related

<Cards>
  <Card href="/docs/data/fetch-metrics" title="Fetch Metrics">
    How the metrics collector works
  </Card>
  <Card href="/docs/data/fetch-history" title="Fetch History">
    How the historical GitHub signal collector works
  </Card>
  <Card href="/docs/cli/generate" title="CLI generate">
    All generate subcommands used by the workflow
  </Card>
</Cards>
