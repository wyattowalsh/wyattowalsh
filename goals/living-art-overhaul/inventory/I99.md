# I99 — Inventory merge / lock assignment

**Date:** 2026-08-18  
**Status:** complete  
**Lane:** CAP (Wave I merge). **No production code edits. No R1/ROS start. No commit / push / branch switch.**  
**Inputs:** I01–I19 (all present), `goal.md`, `facts.md`, `grill-notes.md`, `plan.md`, `task-graph.json` v3, `AGENTS.md`, `scripts/art/AGENTS.md`.

Until K2, **shipped == candidates == six**. Exact-six tests stay green. After `bakeoff.json` `accepted`, live surfaces shrink to 4–6; `openspec/changes/prevent-living-art-repo-growth` does not.

**Ship surface:** `origin/dev` only. **`main` is untouched by this goal.** OpenSpec growth change is untouched (I19 / S15).

---

## Inventory complete

All **I01–I19** exist under `goals/living-art-overhaul/inventory/` and mark **status: complete**. I99 is this merge. No leaf is missing.

| ID | Title | Status |
|---|---|---|
| I01 | README `_rewrite_living_art_section` + GFM wrap tests | complete |
| I02 | `ALL_STYLES` / `LIVING_ART_STYLE_KEYS` / `STYLE_DIALECTS` / CLI picker | complete |
| I03 | artifacts inventory / stage / publish / byte budgets | complete |
| I04 | workflow prepare / generate / assemble / finalize | complete |
| I05 | `test_profile_workflow` + artifact_handoff exact-six | complete |
| I06 | `test_cli` living-art matrix | complete |
| I07 | media / contracts / e2e rehearsal | complete |
| I08 | accretion knobs + A1 unreadability leftovers | complete |
| I09 | `ink_garden` generate + t0 maturity gate | complete |
| I10 | `topography` generate + settlement / stars | complete |
| I11 | `genetic_landscape` generate + colonies | complete |
| I12 | `physarum` generate + t0 spore path | complete |
| I13 | `lenia` generate + field / extent | complete |
| I14 | `ferrofluid` generate + dipole clustering | complete |
| I15 | timelapse render + `_export_mp4` + published frame contract | complete |
| I16 | living-art-modes + AGENTS + workflows docs exact-six copy | complete |
| I17 | GitHub README `<video>` constraint (try then GIF fallback) | complete |
| I18 | finalize GIF pathspec vs MP4 commit block | complete |
| I19 | OpenSpec `prevent-living-art-repo-growth` stays untouched | complete |
| **I99** | **this merge / lock assignment** | **complete** |

**Not missing files — leftover assignment gaps (handled below, not re-inventory):**

- `tests/test_readme_sections.py` wrap/360/no-details is **unlocked** in `task-graph.json` (I01).
- `scripts/art/AGENTS.md` “exact-six” + `ink-garden.mdx` / `topography.mdx` / `world-state.mdx` “six-style fleet” sit **outside** S12 `files[]` (I16).
- `scripts/art/animate.py` is a **third** hardcoded six (I02, I06).
- `tests/test_service_cli_coverage.py` owns unknown-`--only` and is **unlocked** (I06).
- G2 pytest does not validate committed GIF bytes or MP4 siblings (I07).
- `scripts/art/roster.py` and `goals/living-art-overhaul/video-check.md` do not exist yet (R1 / V1).
- `scripts/AGENTS.md` has **zero** exact-six copy (I16); incomplete `art/` module map is hygiene, not a quota hit.

---

## Current contract snapshot

Today’s live contract is **exact six**, **GIF-only inventory**, **360 wrap**, **no `<details>`**. Inverse of facts (`facts.md:3-11`).

### Six keys (order)

`inkgarden`, `topo`, `genetic`, `physarum`, `lenia`, `ferrofluid`

| Key | Label | GIF | MP4 (unmanaged) | Generator |
|---|---|---|---|---|
| `inkgarden` | Ink Garden | `living-inkgarden.gif` | `living-inkgarden.mp4` | `scripts.art.ink_garden.generate` |
| `topo` | Topography | `living-topo.gif` | `living-topo.mp4` | `scripts.art.topography.generate` |
| `genetic` | Genetic Landscape | `living-genetic.gif` | `living-genetic.mp4` | `scripts.art.genetic_landscape.generate` |
| `physarum` | Physarum | `living-physarum.gif` | `living-physarum.mp4` | `scripts.art.physarum.generate` |
| `lenia` | Lenia | `living-lenia.gif` | `living-lenia.mp4` | `scripts.art.lenia.generate` |
| `ferrofluid` | Ferrofluid | `living-ferrofluid.gif` | `living-ferrofluid.mp4` | `scripts.art.ferrofluid.generate` |

Evidence: `scripts/art/artifacts.py:46-54` (`LIVING_ART_STYLE_KEYS`); `scripts/art/timelapse.py:58-67` (`_STYLE_REGISTRY` / `ALL_STYLES`); `scripts/art/shared/accretion.py:26-33` (`STYLE_DIALECTS`).

There is **no** `scripts/art/roster.py`. Four (plus README + `animate.py`) independent six-lists coincide by copy, not alias (I02).

### GIF-only inventory

- Canonical names = `sorted(LIVING_ART_BYTE_BUDGETS)` (`artifacts.py:291-293`), not slug order.
- `_validate_canonical_inventory` docstring: “exact six canonical … GIFs” (`artifacts.py:308-314`). Logic is **set-equality against budget keys**, not `== 6`.
- `stage_living_art_fleet` / `publish_living_art_fleet` docstrings: “exact-six” (`artifacts.py:931-935`, `:993-999`).
- Stage `exact_directory=True`: **only** those GIF names. No MP4, no manifest, no gallery (`artifacts.py:931-990`; `scripts/art/AGENTS.md:72-73`).
- Primary img `exact_directory=False`: only `living-*.gif` compared; MP4s, growth leftovers, metrics SVGs allowed (`artifacts.py:308-343`).
- Managed-surface names **exclude MP4** (`artifacts.py:418-422`). Publish unlinks extra **GIFs** only (`:1060-1066`).
- Budgets (GIF bytes): ferrofluid 3.8M, genetic 2.4M, inkgarden 7.2M, lenia 1.2M, physarum 2.4M, topo 10M; total **27_000_000** (`artifacts.py:28-38`). Dimensions **400×400**, loop 0, frames 2..120, runtime ≥ 24_000 ms (`:39-43`).
- `_TIMELAPSE_RE` allows optional `-dark`; dark GIFs are **unbudgeted** and rejected (`artifacts.py:64-65`; `tests/test_living_art_media.py:435-450`). Matches fact-one-look.

### 360 wrap, no details

Assembler (`scripts/readme_sections.py:1095-1148`): hardcoded six-tuple → one `<p align="center">` of `width="360"` GIF posters, each `<a href="…mp4">`. **No intro, no `<details>`, no `<video>`, no import of `LIVING_ART_STYLE_KEYS`.** Shipped `README.md:68-79` matches.

GFM freeze (`tests/test_readme_gfm_ux.py:107-219`): `_LIVING_WRAP_RE` `{6}` pairs; `width="360" == 6`; `width="100%" == 0`; `"<details" not in living`.

### Exact-six tests (non-exhaustive; see I05/I07)

| Pin | Path:line |
|---|---|
| `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` | `tests/test_living_art_media.py:1650`; `tests/test_overhaul_fact_ids.py:185` |
| `set(ALL_STYLES) == set(LIVING_ART_STYLE_KEYS)` | `tests/test_living_art_contracts.py:231` |
| `total_assets == 6` (literal) | `tests/test_living_art_media.py:286,342,631,880-881` |
| stdout `passed: 6 assets` | `tests/test_living_art_artifact_handoff.py:331` |
| `STAGE_STEP = "Stage exact-six living-art artifact"` | `tests/test_living_art_artifact_handoff.py:31` |
| `max-parallel == 6` | `tests/test_profile_workflow.py:434` |
| wrap `width="360" == 6` / no details | `tests/test_readme_gfm_ux.py:196,214-215`; `tests/test_profile_workflow.py:1140-1143`; `tests/test_readme_sections.py:1010-1013` |
| Matrix **keys** dynamic | `profile-updater.yml:1022-1048`; `tests/test_profile_workflow.py:435-437` |

Workflow: `on.push.branches: [dev]` only (`profile-updater.yml:13-15`). Finalize push refuses `TARGET_BRANCH != "dev"` (`:1625-1627`).

---

## Lock table

Graph SSOT: `goals/living-art-overhaul/task-graph.json` `locks` + `nodes`. **Bold** = I99 extra files / owners. Same-file writers stay sequential.

| Lock | Files (graph + I99) | Later nodes | Notes |
|---|---|---|---|
| `L-ROS` | `scripts/art/roster.py` (new) | **R1**, **M1**, **S1** | R1 creates; M1 fills legends; S1 sets `SHIPPED = accepted`. Sequential on one file. |
| `L-ARTF` | `scripts/art/artifacts.py` | **R2**, **S2** | R2 points KEYS/labels/budget **membership** at SHIPPED (keep six rows). S2 drops retired budgets + “exact six” docstrings. |
| `L-TL` | `scripts/art/timelapse.py`; **`scripts/art/animate.py` (I99)** | **R3** | Registry / `ALL_STYLES` = **candidates**. Animate’s `all_generators` is the third six-list (I02, I06). |
| `L-CLI` | `scripts/cli/generate/_common.py`, `scripts/cli/generate/art.py` | **R4** | `--only` ∈ CANDIDATE; default generate = SHIPPED; refresh iff active == SHIPPED. Route `animated --only` help/allowlist onto candidates. |
| `L-ACC` | `scripts/art/shared/accretion.py` | **A3** | **Default no-op.** Touch only if all six need the same clock (I08 + I09–I14). |
| `L-IG` | `scripts/art/ink_garden.py` | Dig1, Dig4 | After A3. Dig2 is `L-T-ACC`. |
| `L-TO` | `scripts/art/topography.py` | Dto1, Dto4 | |
| `L-GE` | `scripts/art/genetic_landscape.py` | Dge1, Dge4 | |
| `L-PH` | `scripts/art/physarum.py` | Dph1, Dph4 | |
| `L-LE` | `scripts/art/lenia.py` | Dle1, Dle4 | |
| `L-FE` | `scripts/art/ferrofluid.py` | Dfe1, Dfe4 | Do **not** rewrite `visual.py` `language_cluster`; Dfe1 stops calling it (I14). |
| `L-RS` | `scripts/readme_sections.py` | **M2**, **V4** | V4 = assembler only. **Drop `README.md` from V4 `files[]`** (I01). Regen is M3 / S14 (`L-README`). |
| `L-README` | `README.md` | **M3**, **S14** | M3 six-piece stack for P0/V2. S14 shipped roster so K3 greens. |
| `L-WF` | `.github/workflows/profile-updater.yml` | **S3** | Rename exact-six **step names / comments**; `max-parallel` may equal `len(SHIPPED)`. Do **not** unify GIF+MP4 globs (I18). |
| `L-ASSETS` | `.github/assets/img/living-*.gif`, `living-*.mp4` | **S13** | Delete **GIF and MP4** for retired stems. Finalize will **re-commit** leftover MP4s (I03, I18). |
| `L-ASSET-IG` … `L-ASSET-FE` | per-style `living-{style}.gif` + `.mp4` | G1ig…G1fe | Parallel GEN. `--only` one style. No index refresh. |
| `L-T-GFM` | `tests/test_readme_gfm_ux.py`; **`tests/test_readme_sections.py` (I99)**; **wrap piggyback in `tests/test_profile_workflow.py` ~1128-1143 during M4 (I99)** | M4 → M5 → M6, then **K3** | Sequential. M4 inverts wrap/360/no-details **before** SHR. S9 later owns the rest of `test_profile_workflow.py`. |
| `L-T-MEDIA` | `tests/test_living_art_media.py` | **R5**, **G2**, **S4** | Never parallel. R5 relaxes `ALL_STYLES == KEYS`. G2 must add committed-fleet + MP4 presence (I07 hole). S4 exact-shipped. **Do not shrink dialect parametrize to shipped.** |
| `L-T-HAND` | `tests/test_living_art_artifact_handoff.py` | **S5** | **I99: S5 deps += S3** (`STAGE_STEP` is the YAML `name:`). |
| `L-T-CON` | `tests/test_living_art_contracts.py` | **S6** | Owns `set(ALL_STYLES) == set(KEYS)` (`:224-231`). **Not R5.** Split: equality → shipped ⊆ candidates; README/docs loops → SHIPPED; generator parametrize → CANDIDATE. |
| `L-T-E2E` | `tests/test_living_art_e2e_rehearsal.py` | **S7** | Counts already `len(KEYS)`. `--only` stays candidate (`topo` / `inkgarden`). |
| `L-T-CLI` | `tests/test_cli.py`; **unknown-`--only` case (add here or pin unlocked coverage — I99)** | **S8** | **I99: S8 deps += R4.** Keep `only="topo"` as **candidate** shard. Fleet loops stay on `LIVING_ART_STYLE_KEYS` (SHIPPED after R2). |
| `L-T-WF` | `tests/test_profile_workflow.py` | **S9** | After S3. `max-parallel`, job/step wording, GIF/MP4 pathspec split. **Not** wrap/360 (M4 already inverted). Expand verify beyond `-k living_art`. |
| `L-T-ACC` | `tests/test_art_shared_package.py` | **A1**, **A2**, then **D\*2** (serialize) | On-canvas + overlay ignored (I08). Prefer one greening agent after all D\*1 (`plan.md:51,164`). |
| `L-T-IDS` | `tests/test_overhaul_fact_ids.py` (+ S10 also lists `test_art_shared_package.py`) | **R5** (fact-ids equality), **S10** | S10: candidates may exceed shipped. Serialize vs R5 on fact-ids; vs D\*2 on shared-package. |
| `L-DOCS-MODES` | `docs/content/docs/scripts/living-art-modes.mdx`; **`ink-garden.mdx`, `topography.mdx`, `world-state.mdx` (I99)** | **S11** | After S1 + M1. Quota → shipped; legends from M1. Sibling “six-style fleet” otherwise fails S12 `rg`. |
| `L-DOCS-WF` | `AGENTS.md`, `docs/content/docs/workflows/index.mdx`; **`scripts/art/AGENTS.md` (I99)** | **S12** | After S3. Expand W4M `rg` to include `workflows/index.mdx`. |
| `L-BAKE` | `goals/living-art-overhaul/bakeoff.json` | K1, **K2** (human), | K2 writes `accepted[]`. CI must not shrink before K2. |
| `L-VID` | `goals/living-art-overhaul/video-check.md` (new) | V1, V3 | Template then result. V2 is github.com (no lock). |
| `L-GIT` | `.git` | **P0**, **C2** | `origin/dev` only. Never `main`. Never commit message `art: regenerate living-art assets` (loop guard). |
| `L-T-HAND` / `L-T-WF` pathspecs | (tests above) | S5 / S9 | Keep GIF `:(glob)` vs MP4 `mp4s=(...)`. Forbid `:(glob)…living-*.mp4` (I18). |

**Stills (`D\*3`)** and **G3** contact sheet: no shared lock; fan out. **Bake-off scores (`K1*`)** write independent JSON.

**Do not lock / do not edit:** `openspec/changes/prevent-living-art-repo-growth/**` (I19). `docs/public/showcase` layout / `living-art-preview.html` gallery redesign (facts). `scripts/art/shared/visual.py` `language_cluster` helper (I14). `main`.

---

## Lock corrections / extra files I99 must assign

Parent-flagged items. Each is **confirmed** with evidence. Graph today is wrong or incomplete; I99 assigns the fix. **Do not apply these as production edits in this node** — record them for ROS/SHR writers and a later `task-graph.json` patch.

### 1. S5 does not depend on S3; wrap `width="360" == 6` / no-`<details>` also live in `test_profile_workflow.py` outside S9 `-k living_art` (I05)

**Confirm.**

**S5 ↛ S3:** `task-graph.json` S5 `deps: [S2]` only; S3 `deps: [S2]`; parallel `SHR-T` vs `SHR`. `STAGE_STEP = "Stage exact-six living-art artifact"` (`tests/test_living_art_artifact_handoff.py:31`) **is the YAML step `name:`** (`profile-updater.yml:1180`), not a comment. If S3 renames the step, S5 lookups break (`I05.md` sequencing risk; `I04.md` §K).

**Assign:** **S5 deps += S3**. S3 may rename `Stage exact-six…` / `Summarize exact-six…`. S5 updates `STAGE_STEP` in the same wave. Do not keep the lying YAML name (plan: no compatibility shims).

**Wrap outside `-k living_art`:** `test_finalize_applies_waka_before_readme_sections` (`tests/test_profile_workflow.py:1120`) then:

- `:1137` `living_art_wrap(readme)`
- `:1140` `width="360" == 6`
- `:1143` `"<details" not in wrap.lower()`

S9 verify is `pytest … -k living_art` → tests at `:411`, `:460`, `:849` **only** (I05 `:266`). I01 `:152-157`: GFM `-k living_art` also **misses** `test_waka_and_blog_are_visible_not_details` (`test_readme_gfm_ux.py:325`).

**Assign wrap invert to M4 (Wave RM), not S9 (Wave SHR):**

- M4 files += wrap piggyback in `tests/test_profile_workflow.py` (~1128-1143) **and** `tests/test_readme_sections.py:959-1021` (unlocked today; I01 `:176-190`).
- Expand `L-T-GFM` to include `tests/test_readme_sections.py`.
- M4 also flips `test_waka_and_blog_are_visible_not_details` `:325` even though `-k living_art` misses it; expand M4 verify to that test **or** rename it so the slice hits.
- S9 must **not** restore wrap/360/no-details. S9 verify expands to include the finalize test (waka-order + new stack asserts if any remain).

**Why M4 not S9:** M2 goes red in three files immediately. Waiting for post-K2 S9 would leave RM red. `L-T-WF` has **no writer before S9**, so M4 may touch the piggyback during RM; S9 later owns the rest of the file.

I01 vs I05: both flag the gap; I01 prefers M4, I05 says M4 **or** S9. **Pick M4** — wrap is a GFM/README contract that must invert with the assembler, not with CI shrink.

### 2. `scripts/art/AGENTS.md` “exact-six handoff” + sibling docs will fail S12’s repo-wide `rg` unless assigned (I16)

**Confirm.**

| Path:line | Quota sentence | Graph owner today |
|---|---|---|
| `scripts/art/AGENTS.md:72-73` | `stage_living_art_fleet()` “exact-six, media-only workflow handoff” | **none** (S12 `files[]` = root `AGENTS.md` + `workflows/index.mdx` only) |
| `docs/content/docs/scripts/ink-garden.mdx:61` | “complete **six-style** canonical fleet” | none |
| `docs/content/docs/scripts/topography.mdx:70` | “complete **six-style** canonical GIF fleet” | none |
| `docs/content/docs/scripts/world-state.mdx:3,12` | “six living-art generators” / “six generators” | none |

S12 verify: `rg exact-six only in historical openspec` (`task-graph.json` S12). Plan W4M `rg` covers `scripts/` (hits `art/AGENTS.md`) but **omits** `docs/content/docs/workflows/index.mdx` (I16 `:17-18`, `:148-153`).

**Assign:**

- Add `scripts/art/AGENTS.md` to **`L-DOCS-WF` / S12** (live agent SSOT for stage/publish; I03 also pointed here).
- Add `ink-garden.mdx`, `topography.mdx`, `world-state.mdx` to **`L-DOCS-MODES` / S11** (same `docs/content/docs/scripts/` tree; quota not candidate-CLI).
- Extend W4M `rg` to `docs/content/docs/workflows/index.mdx`.
- `docs/content/docs/cli/generate.mdx:107,132` six-key `--only` list is **candidate CLI** — leave as candidates after R4; not a hyphenated `exact-six` hit.

I16 vs I03/I19 on `art/AGENTS.md`: I03 said S2 or S12; I19 said S2/S12/I99. **Pick S12** so one docs pass owns agent copy; S2 still rewrites **Python** docstrings in `artifacts.py` (`:314`, `:935`, `:999`). Do **not** “fix” OpenSpec to silence `rg`.

### 3. S15: empty unstaged diff is not enough (I19)

**Confirm.** Task-graph S15 verify is `git diff -- openspec/changes/prevent-living-art-repo-growth empty`. `git diff` without `HEAD` is **unstaged vs index**. Staged edits or a **commit during the goal** still look empty.

**Assign S15 required checks** (all empty / print nothing), from I19:

```bash
git diff -- openspec/changes/prevent-living-art-repo-growth
git diff --cached -- openspec/changes/prevent-living-art-repo-growth
git diff HEAD -- openspec/changes/prevent-living-art-repo-growth
git status --porcelain=v1 -uall -- openspec/changes/prevent-living-art-repo-growth
git log --oneline 2c21731be4716df7d376714db6ea6d7395a55881..HEAD \
  -- openspec/changes/prevent-living-art-repo-growth
```

Leftover `rg exact-six` **inside that folder is intended**. Do not archive or rewrite the change. I19 `HEAD` baseline: `2c21731be`; last touch `ceb6861d8` (2026-08-13). Blob fingerprints in `I19.md`.

### 4. `--only` and default generate both use `LIVING_ART_STYLE_KEYS` today; R4 must split (I02)

**Confirm.** `_selected_living_art_styles` (`scripts/cli/generate/_common.py:109-119`): membership **and** default list are `LIVING_ART_STYLE_KEYS`. Index refresh iff `active_styles == tuple(LIVING_ART_STYLE_KEYS)` (`:210-216`).

After S1 without R4, `generate living-art --only <retired candidate>` exits 1 — violates “candidates stay selectable.”

**Assign R4:** `--only` ∈ `CANDIDATE_STYLE_KEYS` (help lists candidates); default = `SHIPPED_STYLE_KEYS`; refresh iff active == **SHIPPED**. Unknown still exit 1. `generate all` stays default (SHIPPED). Module `python -m scripts.art.timelapse --only` is lax (`timelapse.py:483-485`, `:566-584`) — R3 documents; do not treat as Typer-equivalent.

### 5. Matrix already follows keys; `max-parallel: 6` + assembler step names stay hardcoded until S3 (I04)

**Confirm.** Prepare exports `json.dumps(list(LIVING_ART_STYLE_KEYS))` (`profile-updater.yml:1022-1028`). Matrix `fromJSON(…outputs.styles)` (`:1048`). **No YAML style list.** After R2+S1, shrinking SHIPPED shrinks children **without** editing style literals.

Hardcoded until S3:

- `max-parallel: 6` (`:1046`; test `:434`)
- step names `Stage exact-six living-art artifact` (`:1180`) / `Summarize exact-six…` (`:1205`)
- comment “Keep exact-six stage GIF-only” (`:1194`)

**Assign S3:** rename those strings; optionally set `max-parallel` to `len(SHIPPED)` (4–6, ≤6). Leave matrix dynamic. Do **not** hardcode a 4- or 5-key YAML list. Coordinate `STAGE_STEP` with S5 (correction 1).

### 6. MP4s unmanaged; S13 must delete leftover `living-*.mp4` or finalize re-commits them (I03)

**Confirm.** `_is_managed_surface_name` is GIFs + manifest + gallery (`artifacts.py:418-422`). Publish unlinks extra GIFs (`:1060-1066`), **never MP4s**. Finalize `mp4s=(.github/assets/img/living-*.mp4)` then append if `[ -e "${mp4s[0]}" ]` (`profile-updater.yml:1580-1585`). Leftover retired MP4s still on disk are **re-added**. Bash glob does **not** stage MP4 **deletions** (deleted files are absent from the glob) (I18).

**Assign S13:** `git rm` (or delete + ensure not re-added) **both** `living-{retired}.gif` and `.mp4` on `.github/assets/img`. Verify stems == SHIPPED for gif **and** mp4. Showcase: GIF stems only (no MP4s today). Do not wait for finalize.

### 7. Media tests are GIF-only; G2 pytest will not prove MP4 pairs; `ALL_STYLES == LIVING_ART_STYLE_KEYS` is the shrink bomb (I07)

**Confirm.** Zero `mp4` matches in `test_living_art_media.py` / contracts / e2e. `_export_mp4` is best-effort (`timelapse.py:331-381`); CLI completeness is GIF-only (`_common.py:161`). On-disk facts check `living-*.gif` names + `n_frames > 1` (`test_living_art_media.py:1656-1673`), **not** `validate_living_art_byte_budgets` on committed files.

`tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` at `test_living_art_media.py:1650` (and fact-ids `:185`, contracts `:231`) **fails the moment S1 shrinks SHIPPED** unless rewritten.

**Assign:**

- **R5** rewrites equality in **its three files** to the gated invariant: `ALL_STYLES == CANDIDATE`; `LIVING_ART_STYLE_KEYS == SHIPPED`; `tuple(ALL_STYLES) == KEYS` **only while** shipped==candidates. Keep `STYLE_DIALECTS` on **candidates**. Until K2 this stays green.
- **S6** owns `test_living_art_contracts.py:224-231` (I02 gap vs I07: both agree it’s a bomb; **pick S6** because R5 lock is `L-T-MEDIA` and does not include contracts. After S1, contracts go red until S6 — accept that sequencing; do not expand R5 into `L-T-CON`).
- **G2** (same `L-T-MEDIA`, after G1): add committed-fleet `validate_living_art_byte_budgets(build_living_art_manifest(img_dir))` **and** assert sibling `living-*.mp4` exist for each shipped GIF. Today’s `pytest -q tests/test_living_art_media.py` does **not** prove G1 pairs. Do not raise budgets (repo-growth out of scope).

### 8. `generate animated` / `animate.py` is a third hardcoded six; keep `only="topo"` as candidate shard; S8 should depend on R4 if topo can be retired (I06)

**Confirm.** `animate.py:361-377` local `all_generators` dict (same six slugs). `generate animated --only` does **not** call `_selected_living_art_styles` (`art.py:193-194`). `tests/test_cli.py:187-213` is the only `--only` test: `only="topo"`. S8 deps = S2 only. If S1 retires `topo` before R4 splits the picker, that test reds.

**Assign:**

- Add `scripts/art/animate.py` to **`L-TL` / R3`**: keys ⊆ `CANDIDATE_STYLE_KEYS` (or import candidate tuple). Growth outputs stay `{style}-growth.gif` — **not** published `living-*` (I15).
- R4 `art.py`: `animated` help/validation on **CANDIDATE**, not SHIPPED.
- **S8 deps += R4.** Keep `only="topo"` as candidate-shard coverage (CI / G1 pattern). Do **not** rewrite it to “first shipped key.” Fleet loops stay on `LIVING_ART_STYLE_KEYS`.
- Add unknown-`--only` exit 1 under **S8** (or R4 verify must run `tests/test_service_cli_coverage.py:423-424`). **Pick: S8 adds an explicit assert** so `L-T-CLI` owns it; unlocked coverage file is insufficient.

### 9. A3 should be a no-op unless all six need the same ceiling; A1/A2 must be on-canvas with overlay ignored, not knob-only (I08)

**Confirm.** I09–I14 **unanimous**: redesign in the generator; do not retune `_CHANNEL_CEILINGS` (`accretion.py:16-24`) or lease names in D\*1. Overlay ticks already grow (`test_style_dialects_make_accretion_readable`, `test_living_art_media.py:1341-1394`). Knob-only A1 tests would be **green today** and stall DIAL.

**Assign:**

- **A3 default: documented no-op** (`L-ACC` untouched except maybe exporting ceilings if tests need them). Allowed only if all six share a clock failure (real-account saturation, or a fourth named repo knob for every dialect — prefer not).
- **A1 two layers** in `tests/test_art_shared_package.py`: (1) clock unit tests, green now; (2) on-canvas isolation, red/xfail until D\*1, overlay `#accretion-dialect` **stripped**. Pin `star_velocity=0`. `timeline=False`. Repos-only moves **count of primary marks** (no repo knob).
- **A2** t0 primary marks with overlay ignored: plant / peak / network-with-veins / organism / tower — not overlay glyphs. Physarum mark is **veins**, not spore (I12). Ferro t0 **already has a tower**; clustering is Dfe1, not empty-t0 (I14).

---

## Disagreements (inventories vs graph)

| Topic | Who | Pick | Reason |
|---|---|---|---|
| Wrap/360/no-details in `test_profile_workflow.py` + `test_readme_sections.py` | I01: M4; I05: M4 or S9 | **M4** (+ expand `L-T-GFM` with `test_readme_sections.py`) | Invert with assembler in Wave RM. S9 is post-K2 CI wording. |
| `test_all_style_registries_have_same_keys` | I02: R5 or S6/S10; I07: S6 | **S6** | R5 files/lock omit contracts. Document S1→S6 red window. |
| `scripts/art/AGENTS.md` exact-six | I16: S12/R3/S12b; I03/I19: S2 or S12 | **S12** (`L-DOCS-WF`) | Agent copy; S2 still owns `artifacts.py` docstrings. |
| Sibling script docs six-quota | I16 leakage | **S11** | Same docs tree as living-art-modes. |
| S8 vs R4 | I06 | **S8 deps += R4**; keep `topo` | Candidate shard is the R4 proof after shrink. |
| V4 `files[]` includes `README.md` under `L-RS` | I01 | **Assembler only**; S14 regen | Avoid `L-README` leak vs M3/S14. |
| A3 retune | I08 vs none | **No-op** | I09–I14 agree unreadability is on-canvas. |

No inventory claimed I01–I19 files were missing.

---

## Per-style D\*1 briefs

Shared: consume today’s knobs; leave `accretion.py` to A3; overlay may stay but **must not be the picture**; GIF path is `generate(..., timeline=False, maturity=snapshot.maturity)` (`timelapse.py:90-96`); 800 SVG → 400×400 GIF; one look (no `-dark` pair); published GIFs were **not** regenerated after 2026-08-14 A1 stills — do not reuse those SVGs as bake-off evidence (D\*3). Stay inside per-style byte budgets (I03).

### Dig1 — Ink Garden (`L-IG`, I09)

Plants already = repos; `show_existing_world` / `is_oldest` rescue draws ≥1 `repo-tree` at `tree_t ≥ 0.34` on the daily GIF path (`ink_garden.py:1605-1612`). The signed A1 t0 still (0 trees) is **stale**. Live unreadability: rescued `tree_t=0.34` **fails bloom gate 0.48**, so first-repo is stem/soil; bloom/trunk motion is tiny; `glint_count` leaks `star_velocity` (`:4301-4305`). Dig1: first repo = visible plant (stem **and** canopy); isolate bloom / trunk / glints on canvas with velocity pinned; 0-repo days stay empty. Dig4: collapse night-cosmic vs parchment into **one** look (`ink_garden.py:12` vs `palette.py` night). Goldens under `tests/fixtures/ink_garden/` churn on Dig1, not Dig2’s lock.

### Dto1 — Topography (`L-TO`, I10)

Not empty: t0 has a `repo-peak` plus a sub-pixel outpost. Stars leased as `prominence_scale` **never enter** `peak_h` (in-frame min–max `star_frac` only, `topography.py:1194-1200`); contours + a repo-count **central blob** (`:1432-1455`) own the map; `followers//10` leaks into `n_levels` (`:2198-2206`); settlements inherit the starriest (often newest) peak’s `static_signal` (~0.10). Dto1: multiply terrain by `prominence_scale`; stop the central blob beating the first hill; give contours a commit identity **without** follower leak; make settlements geometry-visible at 1 follower (not `r=0.8`). Tightest GIF budget after redesign risk: **10 MB** (`living-topo.gif`). Stale `topography.mdx` still teaches followers → contour density.

### Dge1 — Genetic Landscape (`L-GE`, I11)

t0 is **not** empty: one `genetic-peak-core` at op ~0.20 under a **pop_base=30 swarm** (37 dots at op ~0.29). Colonies exist as attrs (`data-colony-count`) but drawn `gl-micro-colony` op ~0.04 **and** stamp elevation that **welds** peaks. GIF contract **skips** `optimize_placement` (`genetic_landscape.py:805-815`); language-cluster packing + ridges collapse same-language hills. Name collision: `CFG.peak_scale=8.0` vs dialect `peak_scale`. Dge1: distinct stable peaks without per-frame PSO; height = visible stars; generations ≠ louder scribble; colonies = followers (0 at 0 followers, **no** saddle-fill terrain); t0 primary mark = peak, not swarm. Overlay origin **(44, 44)** sits on the map — dodge the only hill rather than A3-moving every origin.

### Dph1 — Physarum (`L-PH`, I12)

A1 still is extra-stale (spore-only, pre node-floor). Live GIF t0 (`timeline=False`, mat ≈ 0.01): **spore + one node**, **0 veins** (`vein_fade` gated at `growth_mat ≤ 0.05`). Node floor `max(..., 0.64)` (`physarum.py:1494-1497`) is **not** the leftover — the leftover is the **missing network**. SMIL `maturity=0.08` can draw veins **without** nodes (`test_physarum.py:334-357`). Followers (`+36 * vein_gain` agents) were **non-monotonic** on path count in isolation. Dph1: veins from first repo even when `growth_mat < 0.05`; tag veins (`data-role="physarum-vein"`) so overlay commit `<path>` is not counted; commits/followers move tagged vein mass **up**. Do not treat spore as the A2 mark.

### Dle1 — Lenia (`L-LE`, I13)

t0 has a seed, but halo op ~0.03 and CA is killed by `simulation_mix ≤ 0.34` then `max(seed_residue, field * mix)` (`lenia.py:1937-1953`). Field/extent isolation tests assert **`data-*` only**. `_BIO_RAMP` / designed dark ground are **unused**; live look is muddy gray + amber blobs. Tightest budget: **1.2 MB**. Dle1: stop residue from hiding the CA; field occupancy/luminance tracks `field_gain`; extent **spreads** (not host-hugging satellites at op 0.007); t0 organism survives 400×400. Dle4: wire the intended dark morphogenetic ground — one look, not a pair.

### Dfe1 — Ferrofluid (`L-FE`, I14)

t0 **already has a tower** (1 dipole, 17 spikes). Clustering is the leftover: `language_cluster` packs Python/Go at **x-fraction 0.25**; `_spread_positions` min gap 44 px (22 px at GIF) is a **hedge**, not columns. Media test `min(gaps) >= 36` (`test_living_art_media.py:1609-1613`) greens the leftover. Two `field_gain`s multiplied (`ferrofluid.py:1015`); dialect followers floor **0.82** (22% t0→t2). Ripples op ~0.09. Dfe1: stop calling `language_cluster` for x (tint may stay); N repos ⇒ N readable columns at 400×400, **same-language fixture**; keep dipole x stable (`test_ferrofluid.py:935-950`); stars height obvious; commits a visible pool mark; followers a leased field mark not composite soup. Empty-metrics fake ripples must not count as t0.

---

## GEN recipe (I15)

Published media is **only** `render_timelapse()`. Do **not** use `generate animated` / `animate.py` for `living-*` (those write `{style}-growth.gif` from interpolated maturity on **one** blob — not frame *t*).

Frame *t* = cumulative end-of-day world (`build_daily_snapshots` + `sample_frames` ≤ 120 + `validate_snapshot_monotonic_contract`; `timelapse.py:389-542`). Seed = **final** sampled day’s `metrics_dict`. `timeline=False`.

### One style (G1ig…G1fe)

```bash
uv run python -m scripts.cli generate living-art \
  --profile wyattowalsh \
  --metrics-path /path/to/metrics.json \
  --history-path /path/to/history.json \
  --only inkgarden \
  --max-frames 120 \
  --size 400 \
  --workers 4 \
  --output-dir .github/assets/img
```

Same metrics/history for all six. `--only` writes `living-{style}.gif` then `_export_mp4` sibling (`timelapse.py:331-337`, `:531-535`). Partial CLI **skips** index refresh (`_common.py:210-216`). Parallel locks `L-ASSET-*`.

| Must | Must not |
|---|---|
| ffmpeg on PATH (CLI will **not** fail if MP4 skipped) | Omit `--only` (regenerates all six) |
| Stay inside `LIVING_ART_BYTE_BUDGETS` (do not open repo-growth) | Trigger `_assemble_gif` 12 MB **halve** (breaks 400×400, `timelapse.py:295-324`) |
| GIF 400×400, 2–120 frames, ≥24 s, loop 0 | Pass rehearsal `--frames 6` / `--size 96` |
| Sibling MP4 size > 0 | Refresh manifest on a shard |
| Treat dropped frames as failed G1 | Raise caps to pass G2 |

G1 merge: six gif+mp4 pairs. **G2** must validate **committed** budgets + MP4 presence (correction 7). **G3** contact sheet from first/mid/last GIF frames; do not re-render.

---

## Video path (I17) + GIF/MP4 finalize (I18)

### Try then fallback (I17)

2026 sources still say profile/repo README **strips `<video>`** (GitHub sanitizer allowlist: `img`/`a`/`details`, **not** `video`; community #173635, RepoClip 2026). Drag-drop CDN players are **external hosts** — forbidden.

Today: GIF poster + `href` in-repo MP4 at wrap 360 (`README.md:68-79`; `readme_sections.py:1132-1140`). Grill: that is **not** the designed default.

```text
M2  emit full-width stack + try <video src=".github/assets/img/living-{style}.mp4">
    + per-piece <details> + intro; prefer inner GIF so a strip does not blank art
M3  regenerate README
M4–M6  invert wrap tests; both <video> and GIF+href legal; youtube/cloudinary fail
P0  push origin/dev (README group; do not use art: regenerate living-art assets)
V1  write video-check.md template (play / strip / no-autoplay)
V2  check https://github.com/wyattowalsh/wyattowalsh/blob/dev/README.md
V3  decision = native | gif-fallback
V4  if strip or no autoplay → assembler = visible GIF + obvious MP4 click
    on the NEW stack (not a return to 360 wrap). L-RS only; S14 regen README.
```

Stripped `<video>` with no inner GIF **violates fact-visible-art**. Fallback is an **allowed done state**, not a failed goal. No YouTube / Cloudinary / `user-images.githubusercontent.com` / `github.com/*/assets/`.

### Finalize split (I18)

One art commit message, **two add mechanisms** (`profile-updater.yml:1572-1586`):

1. **GIFs (required):** `:(glob).github/assets/img/living-*.gif` + showcase GIF glob + manifests/galleries. `git add -A` **does** stage GIF deletions.
2. **MP4s (optional):** `mp4s=(.github/assets/img/living-*.mp4)` appended only if `[ -e "${mp4s[0]}" ]`. Tests **forbid** `:(glob)…living-*.mp4` (`tests/test_profile_workflow.py:478-479`). Bash glob does **not** stage MP4 deletions; leftovers on disk are **re-added**.

Video-primary **must keep the `mp4s` block**. Unifying globs fails S9 and drops films when GIFs are byte-identical. Showcase has **zero** MP4s; V4 `href` stays `.github/assets/img/living-*.mp4`.

P0: README-only is enough (tracked MP4s already exist). C2 after shrink: human `git rm` retired **gif and mp4** (S13); do not rely on finalize.

---

## Wave ROS (R1–R5) ready checklist

**Until K2: `SHIPPED_STYLE_KEYS is CANDIDATE_STYLE_KEYS` (same six, same order).** Exact-six tests stay green. Do not shrink. Do not delete generators. Do not start DIAL/RM production in this node.

| Node | Lock | Files | Do | Verify |
|---|---|---|---|---|
| **R1** | `L-ROS` | `scripts/art/roster.py` **new** | `CANDIDATE_STYLE_KEYS` + `SHIPPED_STYLE_KEYS` (= six) + legend slots (title / metaphor / four-signal mapping). Prefer **tuple** constants. | SHIPPED == current six; each key has legend fields (placeholders OK; M1 fills copy) |
| **R2** | `L-ARTF` | `scripts/art/artifacts.py` | `LIVING_ART_STYLE_KEYS = roster.SHIPPED`; labels/regex follow SHIPPED; **keep all six budget rows**. Do not import CANDIDATE into artifacts. | `LIVING_ART_STYLE_KEYS == roster.SHIPPED` |
| **R3** | `L-TL` | `timelapse.py`; **`animate.py` (I99)** | `_STYLE_REGISTRY` / `ALL_STYLES` = candidates; document vs SHIPPED; point `all_generators` at candidate keys. Module `--only` still works for every candidate. | `ALL_STYLES` is candidates; `render --only` still works |
| **R4** | `L-CLI` | `_common.py`, `art.py` | `--only` ∈ CANDIDATE; default generate = SHIPPED; refresh iff active == SHIPPED; `animated` help on CANDIDATE. Unknown → exit 1. | unknown style exits 1; default list == SHIPPED |
| **R5** | `L-T-MEDIA` | `test_living_art_media.py`, `test_art_shared_package.py`, `test_overhaul_fact_ids.py` | Gated equality (correction 7). Dialects stay on candidates. **Not** `test_cli.py`. **Not** contracts (`:231` → S6). | `uv run python -m pytest -q tests/test_living_art_media.py tests/test_art_shared_package.py tests/test_overhaul_fact_ids.py` still green on six |

W1M: “Roster split accepted; shipped still six.”

**R4 extra verify (I99):** `uv run python -m pytest -q tests/test_cli.py -k living` plus the unknown-style case once S8/R4 owns it.

**Do not in ROS:** rewrite wrap GFM tests (M4); drop budgets (S2); rename workflow steps (S3); shrink README (M2/S14); edit OpenSpec; push `main`.

---

## Risks that block or sequence later waves

1. **Human gate K2.** CI must not shrink until `bakeoff.json` `accepted` exists (`plan.md:19`). Early P0 six-piece stack on `dev` is allowed.
2. **`<video>` will probably be stripped.** Planned fallback (I17). Inner GIF required so V2 does not blank the gallery.
3. **D\*2 lock pile-up** on `test_art_shared_package.py` (`L-T-ACC`). Prefer one greening agent after all D\*1 (`plan.md:164`). `pytest -k <style>` also hits generator modules / goldens **outside** `L-T-ACC` (I09 ink fixtures; I12 SMIL staging test).
4. **GEN budget tightness.** Topo / ferrofluid / inkgarden sit >91% of cap (I03). Raising caps is repo-growth — **out of scope**. G2 hole means a fat GIF can look green until committed-fleet validation lands.
5. **S2 without S13** leftover `living-*.gif` fail inventory. **S13 GIF-only** leftover MP4s re-commit (I03, I18).
6. **S5 ∥ S3** `STAGE_STEP` coupling — **I99 adds S5 deps += S3**.
7. **S1 red window** on `test_living_art_contracts.py:231` until S6; on `--only topo` until R4 if `topo` retired — **I99 adds S8 deps += R4**.
8. **Three wrap freezes, one GFM lock** — **I99 assigns extra files to M4**. Incomplete `-k living_art` verify (I01, I05).
9. **Docs `rg` orphans** — **I99 assigns `scripts/art/AGENTS.md` + sibling mdx**. W4M `rg` misses workflows index.
10. **Assembler private six-tuple** (`readme_sections.py:1096-1127`). S1 shrinking SHIPPED does nothing to README until **M2 reads roster**.
11. **V4 `L-RS` vs `L-README`** — I99: assembler-only V4.
12. **Stale OpenSpec** will still say exact-six. Live code must not wait on it. S15 must not “clean” it.
13. **`main` vs docs.** Live YAML is `dev` only; `AGENTS.md:88` still says push to `main`/`master`/`dev` (I04). S12 fixes copy; **do not restore `main` triggers**.
14. **MP4 optionality vs video-primary.** Encode skip / `if-no-files-found: ignore` / finalize `continue-on-error` can leave **stale film**. C3 must click each piece. Fail-closing MP4 is a VID/S3 choice that must **not** introduce `:(glob)…mp4` without rewriting S9 `:478`.

---

## Explicit: `main` untouched; OpenSpec growth change untouched

- **`main`:** not a workflow trigger (`profile-updater.yml:13-15`); prepare/finalize `if:` require `refs/heads/dev`; push script errors unless `TARGET_BRANCH == "dev"` (`:1625-1627`); tests `test_fact_ship_dev_main_is_not_a_publication_target` (`tests/test_profile_workflow.py:1179-1213`). Goal success is origin/`dev` README. **P0 and C2 push `dev` only.** Banner jobs may **read** `origin/main` pinned banners — read-only, unrelated living-art writes (I04).
- **`openspec/changes/prevent-living-art-repo-growth`:** out of scope (`facts.md` last bullet; grill “Not this goal”; design.md Non-Goal `fewer styles`). I19: clean tree at `HEAD` `2c21731be`; last edit `ceb6861d8`. S15 proves no unstaged, no staged, no `HEAD` diff, empty porcelain, **no new commits on that path**. Leftover `exact-six` **in that folder is the intended historical hit.** Do not archive. Do not retune budgets here.

---

## Finding

Wave I is merged. Live contract is six keys, GIF-only exact-directory inventory, 360 wrap, no Living Art `<details>`, and exact-six tests. ROS may start with **shipped == candidates == six**. I99 assigns the parent-flagged lock holes: M4 wrap files, S5 deps S3, S8 deps R4, S12/`art/AGENTS.md`, S11 sibling docs, S13 MP4 deletes, G2 committed+MP4 check, R3 `animate.py`, A3 no-op, R4 picker split. **`main` and the OpenSpec growth change stay untouched.**
