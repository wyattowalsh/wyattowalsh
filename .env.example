# GitHub token for local development only; store real values in .env and rotate immediately if exposed
GITHUB_TOKEN=replace-with-github-token

# Optional: WakaTime API key for coding stats
# WAKATIME_API_KEY=

# Canonical profile workflow expects this PAT for rich repository-aware metrics cards.
# Recommended scopes: read:user, public_repo (add read:org for org/collab visibility).
# METRICS_TOKEN=

# Docs admin auth and first-party telemetry
# Leave DOCS_TELEMETRY_BACKEND unset for the safe runtime defaults:
# filesystem in development, disabled when NODE_ENV=production.
# DOCS_TELEMETRY_BACKEND=filesystem

# DOCS_ADMIN_PASSWORD must be a scrypt verifier (not plaintext):
#   scrypt$<N>$<r>$<p>$<salt_b64url>$<hash_b64url>
# Mint locally with hashAdminPassword() from docs/lib/server/admin-password.ts;
# never put the plaintext password in source control or shell history.
# DOCS_ADMIN_PASSWORD=scrypt$N$r$p$salt_b64url$hash_b64url
# DOCS_ADMIN_SESSION_SECRET=

# Filesystem mode settings
# DOCS_TELEMETRY_STORE_PATH=.telemetry/store.json
# DOCS_TELEMETRY_RETENTION_DAYS=30
# DOCS_TELEMETRY_MAX_EVENTS=2500

# Redis is a reserved configuration mode. The adapter and the production admin
# rate limiter are not implemented, so setting these values does not enable either.
# DOCS_TELEMETRY_REDIS_REST_URL=
# DOCS_TELEMETRY_REDIS_REST_TOKEN=
# DOCS_TELEMETRY_REDIS_KEY=docs:telemetry:events

# Canonical profile workflow expects the full Spotify OAuth trio for the music panel on
# metrics.additional.svg. Missing values disable that panel and the workflow will keep
# the last valid asset instead of committing placeholder output.
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# SPOTIFY_REFRESH_TOKEN=
