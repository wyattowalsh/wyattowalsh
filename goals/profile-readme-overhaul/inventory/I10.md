# I10 — Map blog RSS → SVG card fields

**Date:** 2026-08-14  
**Status:** complete

## Evidence

- `config.yaml`: `blog_feed_url: https://w4w.dev/feed.xml`, `blog_post_limit: 5`.
- `_render_blog_posts` fetches that limit; each card alt is `title · date · hook` (`_normalize_blog_date` + `_one_line_hook`).
- `_rewrite_blog_disclosure` unwraps any `<details><summary>Latest Blog Posts</summary>…` so the strip stays in the open flow.

Live `README.md` (2026-08-14): `## Latest Blog Posts` then five `<a>` cards from `www.w4w.dev/blog/posts/…`, each alt with date + hook. No blog `<details>`. Subline: “Auto-updated from RSS feed” → `https://w4w.dev/feed.xml`.

## Finding

Visible 5-card w4w.dev RSS strip with title, date, and hook. Not collapsed.

## Gap

None.
