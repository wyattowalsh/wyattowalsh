# K1.b5 — Audit skills.yaml batch 5/6 (url+icon)

**Date:** 2026-08-14  
**Status:** complete (catalog audit; shipped surface residual)  
**Batch:** Workflow Tools (19) + Optimization Tools (6) — **25** skills

## Evidence

**Workflow:** Git, Linux, Shell Script, Bash, Zsh, Jupyter, Docker, Kubernetes, Pytest, Python Hypothesis, Visual Studio Code, Sublime Text, GNU Make, uv, pnpm, CMake, Pre-commit, Ruff, Prettier — all https. Shell Script → `pubs.opengroup.org` (POSIX); Bash → `gnu.org/software/bash` (urls are **distinct**; earlier shared-bash residual is closed in YAML).

**Optimization:** AMPL, IBM CPLEX, Gurobi, PuLP, Metaheuristics (`mealpy.readthedocs.io`), Reinforcement Learning (`spinningup.openai.com`) — all https, not Wikipedia.

Shipped `tech-workflow-tools.svg` and `tech-optimization-tools.svg` are unlinked chip boards.

## Finding

Catalog urls are homepage-like. Shell Script ≠ Bash. Concept rows (Metaheuristics, Reinforcement Learning) use docs hosts + `custom-generic` logos.

## Gap

fact-badges-stack / qa: boards still have no per-skill hrefs or badge-size icons; no Camo QA.
