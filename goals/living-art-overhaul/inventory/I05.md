# I05 — Map test_profile_workflow + artifact_handoff exact-six assertions

**Date:** 2026-08-18  
**Status:** complete  
**Locks:** `L-T-HAND` → **S5** (`tests/test_living_art_artifact_handoff.py`); `L-T-WF` → **S9** (`tests/test_profile_workflow.py`)  
**Depends (later):** S2 (`L-ARTF` inventory/budgets), S3 (`L-WF` workflow comments + `max-parallel`)  
**YAML SSOT:** I04 maps the workflow; this leaf maps the tests that freeze it.

Until K2, shipped == candidates == six. These tests stay green. After `bakeoff.json` `accepted`, S5/S9 must become exact-**shipped** (4–6), not exact-six.

## Current roster the tests treat as canonical

From `scripts/art/artifacts.py` (imported, not duplicated as a six-tuple in either test file):

| Surface | Values |
|---|---|
| `LIVING_ART_STYLE_KEYS` | `inkgarden`, `topo`, `genetic`, `physarum`, `lenia`, `ferrofluid` |
| `LIVING_ART_BYTE_BUDGETS` keys (= `CANONICAL_NAMES` sorted) | `living-ferrofluid.gif`, `living-genetic.gif`, `living-inkgarden.gif`, `living-lenia.gif`, `living-physarum.gif`, `living-topo.gif` |

Handoff tests bind inventory to `set(CANONICAL_NAMES)` / `list(LIVING_ART_STYLE_KEYS)` except where a literal `6` or a named style (`topo` / `lenia`) is hardcoded.

---

## A. `tests/test_living_art_artifact_handoff.py` — S5 / `L-T-HAND`

Executable rehearsal: extract inline Python from `.github/workflows/profile-updater.yml` and run it against temp repos. **Job ids and step names are lookup keys.** If S3 renames a YAML `name:`, this file breaks even when S9 is not yet run.

### Constants that encode six / exact inventory / job-step names

| Line | Symbol | Encodes | S5 action |
|---|---|---|---|
| 30 | `CANONICAL_NAMES = tuple(sorted(LIVING_ART_BYTE_BUDGETS))` | Exact GIF inventory (follows S2 if budgets shrink) | Keep; do not hardcode six names |
| 31 | `STAGE_STEP = "Stage exact-six living-art artifact"` | **Exact YAML step name** (workflow `assemble-event-art` ~1180) | Rename in lockstep with S3; “exact-six” → “exact shipped” / drop count |
| 32 | `PUBLISH_STEP = "Publish and validate living-art artifact"` | Finalize step name (no “six”) | Keep unless S3 renames |
| 33 | `CONTRACT_STEP = "Resolve living-art runtime contract constants"` | Prepare step name | Keep |
| 34 | `SHARD_VALIDATE_STEP = "Validate isolated living-art GIF"` | Matrix producer step name | Keep |
| 158 | `_fan_in_matrix_style_artifacts` docstring | **“Approximate six immutable matrix uploads”** | Wording only |

### Helpers (asserts executed by later tests)

| Path:line | Assertion | Kind |
|---|---|---|
| `tests/test_living_art_artifact_handoff.py:72` | heredoc is `uv run python - <<'PY'` | step script shape |
| `tests/test_living_art_artifact_handoff.py:137–138` | `_generate_fleet` writes **every** `CANONICAL_NAMES` GIF then `sync_living_art_artifacts` | exact inventory seed |
| `tests/test_living_art_artifact_handoff.py:162–165` | `source.glob("living-*.gif")` → shard `living-art-style-test-run-{style}` | pathspec / artifact naming |
| `tests/test_living_art_artifact_handoff.py:189–191` | managed roots `.github/assets/img` + `docs/public/showcase` | pathspec surfaces |
| `tests/test_living_art_artifact_handoff.py:213–218` | seed stale `CANONICAL_NAMES` **plus** `living-unknown.gif` | unexpected-name probe |
| `tests/test_living_art_artifact_handoff.py:232` | merged fan-in names `== set(CANONICAL_NAMES)` | **exact inventory** |
| `tests/test_living_art_artifact_handoff.py:234–235` | runs job **`assemble-event-art`** / `STAGE_STEP` | **job + step name** |
| `tests/test_living_art_artifact_handoff.py:239` | assembler returncode 0 | fail-closed |
| `tests/test_living_art_artifact_handoff.py:240` | stage path `RUNNER_TEMP/living-art-stage` | artifact path |

### Per-test assertion map

#### `test_prepare_job_exports_the_canonical_style_matrix` (243)

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_living_art_artifact_handoff.py:246–247` | job **`prepare-event-art-inputs`** / `CONTRACT_STEP` | job + step | keep names |
| `tests/test_living_art_artifact_handoff.py:253` | returncode 0 | | |
| `tests/test_living_art_artifact_handoff.py:258` | `json.loads(outputs["styles"]) == list(LIVING_ART_STYLE_KEYS)` | **matrix export = shipped keys** | auto-follows S1/S2 if `LIVING_ART_STYLE_KEYS` → `SHIPPED` |
| `tests/test_living_art_artifact_handoff.py:259` | `max_frames == DEFAULT_PUBLISHED_MAX_FRAMES == 120` | frame cap, not six | out of S5 count rewrite |

#### `test_matrix_shard_validation_isolated_and_fail_closed` (266)

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_living_art_artifact_handoff.py:271` | expected file **`living-topo.gif`** | **named style** | rewrite if `topo` retired |
| `tests/test_living_art_artifact_handoff.py:274` | wrong-name decoy **`living-lenia.gif`** | **named style** | rewrite if `lenia` retired |
| `tests/test_living_art_artifact_handoff.py:287–293` | job **`generate-event-art`** / `SHARD_VALIDATE_STEP`; env `LIVING_ART_STYLE=topo` | job + step + style | same |
| `tests/test_living_art_artifact_handoff.py:298–299` | success stdout **`Validated living-topo.gif`** | named GIF | same |
| `tests/test_living_art_artifact_handoff.py:301` | any failure → nonzero | fail-closed | keep |

#### `test_exact_six_gif_artifact_round_trip_regenerates_all_derived_surfaces` (304)

Function **name** is a six-lock (`exact_six`). Rename in S5.

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_living_art_artifact_handoff.py:310` | hashes keyed by `CANONICAL_NAMES` | exact inventory | follows S2 |
| `tests/test_living_art_artifact_handoff.py:315` | stage dir names `== set(CANONICAL_NAMES)` | **exact-six directory** | follows S2 |
| `tests/test_living_art_artifact_handoff.py:316` | every stage entry regular file, not symlink | inventory quality | keep |
| `tests/test_living_art_artifact_handoff.py:317–318` | no `living-art-manifest.json` / `living-art-preview.html` on stage | media-only stage | keep |
| `tests/test_living_art_artifact_handoff.py:325–326` | job **`finalize`** / `PUBLISH_STEP` | job + step | keep |
| `tests/test_living_art_artifact_handoff.py:331` | stdout contains **`Living-art publication validation passed: 6 assets`** | **hardcoded six** | **must** become `len(CANONICAL_NAMES)` / `len(SHIPPED)` — will go red the moment S2 drops a budget |
| `tests/test_living_art_artifact_handoff.py:336–337` | both surfaces `glob("living-*.gif") == set(CANONICAL_NAMES)` | **exact inventory + pathspec** | follows S2; extra `living-*.gif` (retired leftovers) fail — pairs with S13 |
| `tests/test_living_art_artifact_handoff.py:339` | unmanaged `preserve.txt` survives | mutation safety | keep |
| `tests/test_living_art_artifact_handoff.py:340–341` | each canonical GIF hash matches producer | | |
| `tests/test_living_art_artifact_handoff.py:349` | primary/public stable manifests equal | | |
| `tests/test_living_art_artifact_handoff.py:350` | `primary_manifest["total_assets"] == len(CANONICAL_NAMES)` | count via inventory | follows S2 (not a literal 6) |
| `tests/test_living_art_artifact_handoff.py:351–353` | gallery bytes equal across surfaces | | |

#### `test_assembler_rejects_incomplete_fleet_without_materializing_artifact` (356)

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_living_art_artifact_handoff.py:361` | unlinks **`living-topo.gif`** then fan-in | **named style missing** | pick any remaining shipped key if `topo` retired |
| `tests/test_living_art_artifact_handoff.py:366–367` | job **`assemble-event-art`** / `STAGE_STEP` | job + step | S3 name coupling |
| `tests/test_living_art_artifact_handoff.py:372` | nonzero | fail-closed | |
| `tests/test_living_art_artifact_handoff.py:373` | `"living-art producer source inventory mismatch"` | exact-inventory error | keep (label from `artifacts._validate_canonical_inventory`) |
| `tests/test_living_art_artifact_handoff.py:374` | `"living-topo.gif"` in stderr | named missing file | same as 361 |
| `tests/test_living_art_artifact_handoff.py:375` | `living-art-stage` **not** created | no partial artifact | keep |

#### `test_finalizer_rejects_invalid_stage_before_destination_mutation` (382)

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_living_art_artifact_handoff.py:391` | mutate **`living-lenia.gif`** (or add `living-unknown.gif`) | **named style** | rewrite if `lenia` retired |
| `tests/test_living_art_artifact_handoff.py:396` | unexpected `living-unknown.gif` | exact-directory reject | keep |
| `tests/test_living_art_artifact_handoff.py:400` | oversized vs `LIVING_ART_BYTE_BUDGETS[target.name]` | budget (S2) | keep |
| `tests/test_living_art_artifact_handoff.py:411–415` | job **`finalize`** / `PUBLISH_STEP` | job + step | |
| `tests/test_living_art_artifact_handoff.py:418–419` | nonzero + destinations unchanged | fail-closed | keep |

#### `test_handoff_steps_precede_upload_and_the_sole_git_writer` (422)

**Job names + step names** (order contracts). S3 step-name edits must match.

| Path:line | Assertion | Jobs / steps |
|---|---|---|
| `tests/test_living_art_artifact_handoff.py:424–427` | generate before upload | job **`generate-event-art`**: `Generate one canonical living-art GIF` < `Upload isolated living-art GIF` |
| `tests/test_living_art_artifact_handoff.py:430–437` | download → **exact-six stage** → upload | job **`assemble-event-art`**: `Download isolated living-art GIFs` < `STAGE_STEP` < `Upload living-art staging bundle` |
| `tests/test_living_art_artifact_handoff.py:439–446` | publish before git | job **`finalize`**: `Install dependencies` < `PUBLISH_STEP` < `Commit and push owned profile artifacts` |

Does **not** assert `Summarize exact-six living-art artifact` or `Upload living-art MP4 siblings`.

#### `test_finalize_stages_tracked_unexpected_living_gif_deletions` (449)

**Pathspecs.** Overlaps I18 (GIF glob vs MP4 block) — I05 owns the **test** assertions.

| Path:line | Assertion | Kind |
|---|---|---|
| `tests/test_living_art_artifact_handoff.py:453–456` | extract `art_files=(...)` from finalize **`Commit and push owned profile artifacts`** | **job + step + pathspec array** |
| `tests/test_living_art_artifact_handoff.py:462` | `git add -A -- "${owned_files[@]}"` | staging contract |
| `tests/test_living_art_artifact_handoff.py:465–466` | fixture paths `.github/assets/img/living-unknown.gif` and `docs/public/showcase/living-unknown.gif` | both GIF globs |
| `tests/test_living_art_artifact_handoff.py:508–511` | staged `D\t` those two paths | glob **does** pick up unexpected `living-*.gif` deletions |

Does **not** assert `:(glob).github/assets/img/living-*.mp4` (workflow forbids that glob; MP4s use `mp4s=(.github/assets/img/living-*.mp4)`). GIF-only deletion rehearsal.

#### `test_finalize_remains_the_only_first_party_git_writer` (514)

| Path:line | Assertion | Kind |
|---|---|---|
| `tests/test_living_art_artifact_handoff.py:532` | `writer_jobs == {"finalize"}` | **job name** (any `git add/commit/push` or `contents: write`) |

---

## B. `tests/test_profile_workflow.py` — S9 / `L-T-WF`

Static YAML string/structure contracts. Does **not** execute assembler Python. Plan S9 title is “exact-six **handoff wording**”; the file also freezes `max-parallel: 6`, job graphs, artifact names, GIF/MP4 pathspecs, **and** a README wrap `== 6` (see gap).

### Job-name tuples (module scope)

| Path:line | Symbol | Living-art job ids |
|---|---|---|
| `tests/test_profile_workflow.py:63–70` | `GENERATOR_JOBS` | `prepare-event-art-inputs`, `generate-event-art`, **`assemble-event-art`** |
| `tests/test_profile_workflow.py:72–78` | `FINALIZE_NEEDS` | **`assemble-event-art`** (not the matrix children) |

Used by `test_finalize_is_sole_first_party_git_commit_and_push` (546) and `test_finalize_needs_generators_and_waka_fail_closed` (533).

### Dedicated living-art tests (S9 core)

#### `test_living_art_uses_dynamic_six_way_matrix_and_fresh_outputs` (411)

Function **name** is a six-lock (`six_way_matrix`).

| Path:line | Assertion | Kind | Shrink note |
|---|---|---|---|
| `tests/test_profile_workflow.py:413–415` | jobs **`prepare-event-art-inputs`**, **`generate-event-art`**, **`assemble-event-art`** exist | **job names** | keep |
| `tests/test_profile_workflow.py:417` | prepare has no `needs` | graph | keep |
| `tests/test_profile_workflow.py:418–423` | prepare timeout 15, `contents: read`, outputs `styles` + `max_frames` from `steps.contract` | | keep |
| `tests/test_profile_workflow.py:425` | `from scripts.art.artifacts import LIVING_ART_STYLE_KEYS` | matrix SSOT import | keep (R2/S1 still export this name) |
| `tests/test_profile_workflow.py:426` | artifact `living-art-inputs-${{ github.run_id }}` | artifact name | keep |
| `tests/test_profile_workflow.py:427` | `overwrite: true` | | |
| `tests/test_profile_workflow.py:429–431` | producer `needs == prepare-event-art-inputs`, timeout 45, `contents: read` | **job graph** | keep |
| `tests/test_profile_workflow.py:433` | `fail-fast` is False | | keep |
| `tests/test_profile_workflow.py:434` | **`strategy["max-parallel"] == 6`** | **hardcoded six** | **S3** sets YAML to `len(shipped)`; **S9** must match. Plan: “max-parallel may equal len(shipped)” |
| `tests/test_profile_workflow.py:435–437` | matrix `style: ${{ fromJSON(needs.prepare-event-art-inputs.outputs.styles) }}` | **dynamic roster** | keep — shrinking keys shrinks CI without this assert changing |
| `tests/test_profile_workflow.py:439–443` | `--only "$LIVING_ART_STYLE"`, `--max-frames`, `--size 400`, `--workers 4`, `--output-dir` | CLI | `--only` stays candidate-capable (R4); default matrix is shipped |
| `tests/test_profile_workflow.py:444` | `living-art-output-${{ matrix.style }}` | path | keep |
| `tests/test_profile_workflow.py:445` | artifact `living-art-style-${{ github.run_id }}-${{ matrix.style }}` | **per-style artifact** | keep |
| `tests/test_profile_workflow.py:446–448` | `compression-level: 0`, `overwrite: true`, no `continue-on-error` | fail-closed upload | keep |
| `tests/test_profile_workflow.py:450–452` | assembler `needs == generate-event-art`, timeout 15, `contents: read` | **job graph** | keep |
| `tests/test_profile_workflow.py:454` | `pattern: living-art-style-${{ github.run_id }}-*` | **fan-in pathspec** | keep |
| `tests/test_profile_workflow.py:455` | `merge-multiple: true` | | keep |
| `tests/test_profile_workflow.py:456` | `stage_living_art_fleet(` in assembler | stage call site | keep |
| `tests/test_profile_workflow.py:457` | artifact `living-art-stage-${{ github.run_id }}` | **exact-fleet artifact** | keep |

Does **not** assert `max-parallel` equals `len(LIVING_ART_STYLE_KEYS)` — it is a literal `6`. After S1 ships 4 or 5, YAML+S9 must move together or the test is red.

Does **not** assert step title `Stage exact-six living-art artifact` (that string lives only in the handoff file).

#### `test_living_art_workflow_uses_exact_six_primary_only_handoff` (460)

Function **name** is a six-lock (`exact_six_primary_only_handoff`). This is the S9 wording target.

| Path:line | Assertion | Kind | Shrink / I18 note |
|---|---|---|---|
| `tests/test_profile_workflow.py:462–464` | blocks **`generate-event-art`**, **`assemble-event-art`**, **`finalize`** | **job names** | keep |
| `tests/test_profile_workflow.py:466` | `stage_living_art_fleet(` **not** in producer | stage only at assemble | keep |
| `tests/test_profile_workflow.py:467` | `stage_living_art_fleet(` **in** assembler | | keep |
| `tests/test_profile_workflow.py:468` | `publish_living_art_fleet(` in finalize | | keep |
| `tests/test_profile_workflow.py:469–471` | forbid `living-art-stage/outputs`, `outputs/docs-showcase`, `outputs/index` | no nested derived stage | keep |
| `tests/test_profile_workflow.py:472–473` | `if-no-files-found: error` in producer **and** assembler | exact upload | keep |
| `tests/test_profile_workflow.py:474` | `validate_living_art_byte_budgets(` in producer | shard budget | keep |
| `tests/test_profile_workflow.py:475` | **forbid** `for file in .github/assets/img/living-*.gif` | no workspace glob publish | keep |
| `tests/test_profile_workflow.py:476` | **forbid** `for file in "$stage"/outputs/timelapse/living-*.gif` | no nested timelapse glob | keep |
| `tests/test_profile_workflow.py:477` | **`':(glob).github/assets/img/living-*.gif'`** in finalize | **GIF pathspec** | I18; keep glob, roster length is implicit |
| `tests/test_profile_workflow.py:478` | **`':(glob).github/assets/img/living-*.mp4'` not** in finalize | **MP4 must not use git glob** | I18; keep |
| `tests/test_profile_workflow.py:479` | **`mp4s=(.github/assets/img/living-*.mp4)`** | **MP4 bash array** | I18; keep |
| `tests/test_profile_workflow.py:480` | **`':(glob)docs/public/showcase/living-*.gif'`** | **public GIF pathspec** | keep |
| `tests/test_profile_workflow.py:481` | `git add -A -- "${owned_files[@]}"` | owned add | keep |

### Adjacent workflow tests that encode living-art jobs / artifacts (S9 should not drop)

| Test | Path:line | Assertion | Kind |
|---|---|---|---|
| `test_generate_event_art_installs_lossless_gif_optimizer` | `tests/test_profile_workflow.py:406–408` | job **`generate-event-art`**; apt `librsvg2-bin gifsicle` | job name (not six) |
| `test_finalize_needs_generators_and_waka_fail_closed` | `tests/test_profile_workflow.py:537–539` | each `FINALIZE_NEEDS` including **`assemble-event-art`** as `- {dep}` and `needs.{dep}.result == 'success'` | **job graph** |
| `test_finalize_is_sole_first_party_git_commit_and_push` | `tests/test_profile_workflow.py:549–564` | `GENERATOR_JOBS` (includes prepare/generate/assemble) must not `git commit` / `git push` / `contents: write` | **job names** |
| `test_generator_jobs_upload_artifacts_without_git_writers` | `tests/test_profile_workflow.py:735–736` | **`generate-event-art`**: `living-art-style-${{ github.run_id }}-${{ matrix.style }}` | artifact |
| same | `tests/test_profile_workflow.py:740–741` | **`prepare-event-art-inputs`**: `living-art-inputs-${{ github.run_id }}` | artifact |
| same | `tests/test_profile_workflow.py:745–749` | **`assemble-event-art`**: `living-art-stage-${{ github.run_id }}`, download+upload, `contents: read` | **job + artifact** |
| same | `tests/test_profile_workflow.py:762–769` | finalize downloads include **`living-art-stage-${{ github.run_id }}`** | artifact |
| `test_finalize_publishes_and_validates_living_art_before_git_staging` | `tests/test_profile_workflow.py:852–859` | `publish_living_art_fleet(`; both `validate_living_art_byte_budgets(...)`; `stable_payload`; `Persisted living-art GIF hashes differ`; publish **before** `git add -A --` | publish order (not a count) |
| `test_fact_ship_dev_main_is_not_a_publication_target` | `tests/test_profile_workflow.py:1192–1200` | `github.ref == 'refs/heads/dev'` on **`prepare-event-art-inputs`** (not generate/assemble jobs) | job name subset |

`test_finalize_uses_explicit_generated_asset_pathspecs` (`tests/test_profile_workflow.py:862–875`) asserts **qr/wordcloud/banner** arrays only — **not** `art_files`. Living-art pathspecs are solely in `test_living_art_workflow_uses_exact_six_primary_only_handoff` (S9) and the handoff deletion test (S5).

### README wrap `== 6` inside this file (not handoff; lock-gap)

`test_finalize_applies_waka_before_readme_sections` (`tests/test_profile_workflow.py:1120`) also freezes GFM Living Art wrap:

| Path:line | Assertion | Kind | Owner |
|---|---|---|---|
| `tests/test_profile_workflow.py:1136` | Living Art section present | | |
| `tests/test_profile_workflow.py:1137` | `living_art_wrap(readme)` (helper from `test_readme_gfm_ux.py:152`) | wrap-flow paragraph | |
| `tests/test_profile_workflow.py:1140` | **`wrap.count('width="360"') == 6`** | **hardcoded six** | Plan **M4 / `L-T-GFM`** (`test_readme_gfm_ux.py`); **also here under `L-T-WF`** |
| `tests/test_profile_workflow.py:1141` | **`wrap.count('loading="lazy"') == 6`** | **hardcoded six** | same |
| `tests/test_profile_workflow.py:1143` | `"<details" not in wrap.lower()` | **forbids** per-piece details | **M2/M4 will go red**; S9 verify `-k living_art` **does not run this test** |

### False-positive “six” (do not treat as fleet size)

| Path:line | Assertion | Why not exact-six |
|---|---|---|
| `tests/test_profile_workflow.py:308` | `workflow.count("github.event.head_commit.message !=") >= 6 * 6` | skip-message multiplicity (includes `art: regenerate living-art assets`), not GIF count |
| `tests/test_profile_workflow.py:223`, `:263`, `:289` | checkout/download/upload step counts `== 9` | job/step cardinality, not roster |

### Out of scope: other “exact inventory”

`test_profile_asset_artifact_is_structurally_validated_and_exact` (`tests/test_profile_workflow.py:773`) is the **five-file** banner/QR/wordcloud allowlist, not living-art.

---

## Lock mapping (S5 / S9)

| Later node | Lock | File | What to rewrite |
|---|---|---|---|
| **S5** | `L-T-HAND` | `tests/test_living_art_artifact_handoff.py` | Rename `exact_six` test/STAGE_STEP; replace literal **`6 assets`**; keep `CANONICAL_NAMES` / `LIVING_ART_STYLE_KEYS`; retarget `topo`/`lenia` fixtures if those keys leave `SHIPPED`; keep job ids unless S3 renames steps |
| **S9** | `L-T-WF` | `tests/test_profile_workflow.py` | Rename `six_way` / `exact_six` tests; set **`max-parallel` == len(shipped)** to match S3; keep dynamic matrix JSON; keep GIF/MP4 pathspec split; **also** the wrap `== 6` / no-`<details>` block unless M4 already moved it |
| **S3** (prerequisite) | `L-WF` | `.github/workflows/profile-updater.yml` | `max-parallel`; comments; likely step names `Stage exact-six…` / `Summarize exact-six…` |
| **S2** (prerequisite) | `L-ARTF` | `scripts/art/artifacts.py` | budgets + inventory; `CANONICAL_NAMES` and `list(LIVING_ART_STYLE_KEYS)` then shrink automatically |

Plan verify:

- S5: `pytest -q tests/test_living_art_artifact_handoff.py` — covers all eight tests.
- S9: `pytest -q tests/test_profile_workflow.py -k living_art` — covers 411, 460, 849 **only**. Misses wrap `== 6` (1120), `GENERATOR_JOBS` / `FINALIZE_NEEDS`, and `test_generator_jobs_upload_artifacts_without_git_writers`.

### Sequencing risk (flag for I99)

S5 **deps = S2 only**. S3 **deps = S2**. They are parallel (`SHR-T` vs `SHR`).

`STAGE_STEP` (`tests/test_living_art_artifact_handoff.py:31`) **is the YAML step `name:`**, not a comment. If S3 renames `Stage exact-six living-art artifact`, S5 reds until STAGE_STEP matches.

**Fix for I99:** either (a) S5 deps += S3, or (b) S3 must not rename step `name:` (comments + `max-parallel` only; S5/S9 own wording in tests, and a later docs/workflow pass owns the YAML name), or (c) S3 keeps the step name until S5 lands.

S9 already deps S3 — correct for `max-parallel == 6`.

---

## Finding

Exact-six is a **test contract**, not a comment:

- **Literal 6:** handoff stdout `"…passed: 6 assets"` (`:331`); workflow `max-parallel == 6` (`test_profile_workflow.py:434`); README wrap `width="360"` / `loading="lazy"` `== 6` (`:1140–1141`).
- **Exact inventory via `CANONICAL_NAMES` / `LIVING_ART_STYLE_KEYS`:** prepare `styles` JSON; stage dir; both-surface `living-*.gif` globs; `total_assets == len(CANONICAL_NAMES)`.
- **Named styles hardcoded:** `topo` / `living-topo.gif`, `lenia` / `living-lenia.gif`.
- **Job names:** `prepare-event-art-inputs`, `generate-event-art`, `assemble-event-art`, `finalize` (plus `GENERATOR_JOBS` / `FINALIZE_NEEDS`).
- **Step names:** `Stage exact-six living-art artifact` is the dangerous one; also generate/upload/download/publish/commit titles in the handoff order test.
- **Pathspecs:** finalize `:(glob)…/living-*.gif` (img + showcase); **forbid** `:(glob)…/living-*.mp4`; require `mp4s=(.github/assets/img/living-*.mp4)`; forbid workspace `living-*.gif` for-loops; assemble download `pattern: living-art-style-${{ github.run_id }}-*`.

Matrix **length** is dynamic (`fromJSON` styles). **Parallelism** is not.

## Gap

1. **S5 ↛ S3:** `STAGE_STEP` couples handoff tests to a workflow step name S3 may rename.
2. **S9 verify `-k living_art` misses wrap `== 6` / no-details** in `test_finalize_applies_waka_before_readme_sections` — M2 will fail that test; M4’s lock is `test_readme_gfm_ux.py` only. I99 should assign those three asserts to **M4** (expand `L-T-GFM`) or **S9** (expand S9 verify).
3. **Neither file asserts** `Summarize exact-six living-art artifact`, `living-art-mp4-${{ github.run_id }}`, or MP4 `if-no-files-found: ignore`. MP4 sibling upload is uncovered (I18/S3).
4. **`living-unknown.gif` deletion test** proves the GIF glob stages extras; it does not prove retired canonical GIFs are removed — that is S13 + exact-directory inventory after S2.
5. Do not edit `openspec/changes/prevent-living-art-repo-growth` (I19/S15). Live tests above are the shrink SSOT.
