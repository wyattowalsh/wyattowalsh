# C1 — Star-count fidelity of each renderer

**Date:** 2026-08-14  
**Status:** complete (score notes; no per-cloud contact prints)

## Evidence

`tests/test_word_clouds.py` `test_svg_engine_bakeoff_typographic_best_encodes_volume`:

- Synthetic frequencies: Python 100 … Tcl 1.
- Spearman rank of count vs `font_size` after `place_words`.
- Engines in the bake-off: **wordle**, **clustered**, **typographic**, **shaped** (all `require` full placement).
- Assert: `scores["typographic"] == max(scores)` and `>= 0.99`; typographic rotations are all 0.

`metaheuristic-anim` is **not** in the volume bake-off. It is registered (`RENDERERS`) but is a multi-solver animation path — not volume-safe for the two shipped SVGs.

CI / default ship only typographic topics + languages (`DEFAULT_RENDERER`, generate-assets filenames).

## Score notes

| Renderer | Volume bake-off | Ship? |
|---|---|---|
| typographic | wins (ρ ≥ 0.99, monotone size/weight/opacity on fixture) | **yes** — only volume-safe ship renderer |
| wordle | included; loses to typographic | no — not volume-safe |
| clustered | included; loses to typographic | no — not volume-safe |
| shaped | included; loses to typographic | no — not volume-safe |
| metaheuristic-anim | excluded from bake-off | no — not volume-safe |

C1.wordle/clustered/shaped/metaheuristic-anim × topics/langs: do not ship. C1.typographic.topics + C1.typographic.langs: ship.

## Gap

No separate checked-in numeric table per cloud beyond the pytest assertion. Sufficient to pick the winner.
