# I03 — Map production lowlighter plugin matrix vs probe

**Date:** 2026-08-14  
**Status:** complete

## Evidence

Production `generate-profile-metrics` (`profile-updater.yml` Personal / additional / extra):

| Knob | Primary | Additional | Extra |
|---|---|---|---|
| `plugin_habits` | **yes** (facts/charts/langs) | no | no |
| `plugin_calendar_limit` | **0** (all years) | — | — |
| `plugin_languages_threshold` | **0%** | — | — |
| `plugin_topics_limit` | **20** | — | — |
| `plugin_stars_limit` | — | **16** (token) / 0 | — |
| `plugin_people_limit` | — | **36** | — |
| `plugin_music` / `plugin_tweets` / `plugin_activity` | no | no | no |
| `plugin_lines` / `plugin_gists` / `plugin_achievements` | no | gists no | gists no; lines/achievements omitted |
| extra panels | — | stargazers / repos / people | reactions + followup |

Probe `probe-full-metrics` (dispatch-only): languages threshold **2%**, calendar **1**, topics **15**, stars **3**, people **12**, **activity yes** (comment: probe-only diagnostics; production stays off), music no.

`tests/test_profile_workflow.py` `test_metrics_production_plugins_match_relevance_matrix` locks the production matrix including `plugin_topics_limit: 20`.

## Finding

Production is the maximal GitHub-native cluster; music/tweets/activity stay off. Probe is intentionally smaller except activity-on.

## Note (topics overshoot)

Inventory brief recorded `plugin_topics_limit: 30` as an overshoot vs lowlighter max **20**. Current tree is already capped at 20 (bug being / already fixed). Do not raise above 20.

## Gap

None for the matrix map. Isolate retries of lines/achievements/gists remain off (see I04).
