# A1 — Export sequential frame contact sheets per style

**Date:** 2026-08-14  
**Status:** still-frame inspection signed in `A1-frame-inspection.md`. Published `living-*.gif` not regenerated. No composite strip.

## Evidence

Sibling dump now at `inventory/frames/`: three sequential stills per registry style (`t0`/`t1`/`t2`).

| Style | Files |
|---|---|
| inkgarden | `inkgarden-t0.svg` … `t2.svg` |
| topo | `topo-t0.svg` … `t2.svg` |
| genetic | `genetic-t0.svg` … `t2.svg` |
| physarum | `physarum-t0.svg` … `t2.svg` |
| lenia | `lenia-t0.svg` … `t2.svg` |
| ferrofluid | `ferrofluid-t0.svg` … `t2.svg` |

SVGs carry `data-style`, `data-accretion-*` attrs, and an “A1 Frame Inspection” caption. Accretion numbers increase t0 → t2 (e.g. genetic repos 1→2→4, stars 2→24→120).

`A1-frame-inspection.md` records the post-wiring inspection. `living-art-preview.html` remains the finished-GIF gallery, not this still-frame set.

## Finding

Per-style stills now carry current knob attrs (`data-settlement-gain`, `data-colony-gain`, `data-field-gain`, `data-ripple-gain`). Overlay accretion grows t0 → t2. Early-spine inkgarden/physarum scenes remain maturity-gated.

## Gap

Do not treat these SVGs as shipped GIF replacements. Regenerating the exact-six `living-*.gif` fleet is a separate CI/cost step.
