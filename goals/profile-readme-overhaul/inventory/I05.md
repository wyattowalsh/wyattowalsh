# I05 — Map _SUPPLEMENTAL_METRICS_ASSETS + workflow artifact lists

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/readme_sections.py` `_SUPPLEMENTAL_METRICS_ASSETS` is:

1. `metrics-languages.svg`
2. `metrics-habits.svg`
3. `metrics-music.svg`
4. `metrics-posts.svg`

No `metrics-activity.svg` embed helper.

`scripts/supplemental_metrics.py` generate path writes languages + habits (and optional music/posts), then **`_remove_asset_if_present(..., ASSET_SPECS["activity"].asset_name)`**. Manifest status for activity is `enabled=False`, reason `removed-duplicate-github-feed`.

Workflow (`profile-updater.yml`):

- Validate/upload/finalize first-party metrics lists: languages, habits, music, posts (plus the three lowlighter `metrics*.svg` files and `wakatime.svg` on finalize).
- `rg metrics-activity .github/workflows/profile-updater.yml` is empty.
- `plugin_activity: no` on production additional/extra (and primary).

Worktree has no `.github/assets/img/metrics-activity.svg`. `metrics-languages.svg` is present (7956 bytes).

## Finding

Activity is deleted at generate, not emitted in README, and not in finalize. Remaining first-party cards: languages / habits / music / posts.

## Gap

Asset table has no live `metrics-activity.svg` embed. Residuals (out of generate lease): `_fetch_recent_activity` / `_render_activity_card` remain in `scripts/supplemental_metrics.py`; `metrics-posts.svg` is listed for emit / finalize but absent from `.github/assets/img`.
