# I11 — Map daily_snapshots monotonic contract + style registry

**Date:** 2026-08-14  
**Status:** complete

## Evidence

Shared spine: `scripts/art/daily_snapshots.py` `build_daily_snapshots` reconstructs one calendar day sequence from account creation → now (`include_today=True` in `timelapse.py`). Frame t is the cumulative end-of-day world through that day.

Monotonic contract: `validate_snapshot_monotonic_contract` forbids regressions on `MONOTONIC_CUMULATIVE_KEYS` (stars, forks, followers, public_repos, total_commits, …) and on `maturity`. Called after snapshot build and again after `sample_frames`.

Style registry (`scripts/art/timelapse.py` `_STYLE_REGISTRY` / `ALL_STYLES`):

| Key | Module |
|---|---|
| inkgarden | `ink_garden.generate` |
| topo | `topography.generate` |
| genetic | `genetic_landscape.generate` |
| physarum | `physarum.generate` |
| lenia | `lenia.generate` |
| ferrofluid | `ferrofluid.generate` |

Exact-six artifact contract: `scripts/art/artifacts.py` `_validate_canonical_inventory` requires `living-{style}.gif` for those six names only. Worktree has exactly those six GIFs under `.github/assets/img/`.

## Finding

One daily spine + monotonic contract + exact-six GIFs. Human contact-sheet sign-off is still open (see A1).

## Gap

A1 now has unsigned `inventory/frames/` stills (`t0`–`t2` × six styles). No signed sheet yet.
