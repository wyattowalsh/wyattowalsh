# Goal — gh-profile-readme-e2e

Maximally enhance, refine, finalize, and assure the live GitHub profile README end-to-end (generators, CI/finalize, assets, composition, verification): ship the unpushed fleet after Wave 0 PAT attestation, finish residual structure (dark banner fix, shared SVGO, F2 CLI domain packages), measure 6–8 section-order candidates under a fixed Banner→Connect header, land the winner with durable order config and neighbor-aware rewrite anchors, and leave local suite + remote CI green under atomic conventional commits with ask-before-push.

## Shared understanding

See [`facts.md`](./facts.md) (and [`facts.meta.json`](./facts.meta.json)) for the accepted, testable fact sheet.

## Execution plan

See [`plan.md`](./plan.md) (v4, Plannotator-approved) and the machine-readable DAG [`task-graph.json`](./task-graph.json).

Supporting provenance: [`grill-notes.md`](./grill-notes.md), interview/facts JSON under this directory.

## Done when

- Every accepted fact in `facts.md` is verified.
- Critical path in `task-graph.json` is complete (`G0`…`F5`).
- Local full test suite is green after residual structure + composition land.
- Remote CI (`ci.yml` + relevant `profile-updater` jobs) is green after the final push.
- Winning section order is live in `README.md`, regenerate-stable, and preserves Banner→Connect, fleet locks (10 featured, 6 living-art wrap-flow, no tech teaser, card `@media` dark), and GFM safety.
- Git discipline held: atomic conventional commits, ask before every push, no force-push.
