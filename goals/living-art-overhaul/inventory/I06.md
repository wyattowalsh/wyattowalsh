# I06 — Map test_cli living-art matrix

**Date:** 2026-08-18  
**Status:** complete  
**Locks:** `L-T-CLI` → **S8** (`tests/test_cli.py`); `L-CLI` → **R4** (`scripts/cli/generate/_common.py`, `scripts/cli/generate/art.py`)  
**Adjacent (not this file):** R5 (`L-T-MEDIA` / `L-T-IDS` — `ALL_STYLES == LIVING_ART_STYLE_KEYS`); S7 (`L-T-E2E` — Typer `--only` forwarding)

Until K2, shipped == candidates == six. These tests stay green. After `bakeoff.json` `accepted`, default-generate loops must follow **SHIPPED**; `--only` must keep accepting **CANDIDATE**.

There is **no** `generate art` command. Living-art CLI lives in `scripts/cli/generate/art.py`: `generative` (unrelated Clifford/Phyllotaxis), `animated` (legacy growth), `living-art`, `timelapse`.

---

## Current roster the tests treat as canonical

`tests/test_cli.py` imports **only** `LIVING_ART_STYLE_KEYS`. It never imports `ALL_STYLES`, never hardcodes the six-tuple, never asserts `len(...) == 6`.

| Surface | File | Values (today) |
|---|---|---|
| `LIVING_ART_STYLE_KEYS` | `scripts/art/artifacts.py:54` | `("inkgarden", "topo", "genetic", "physarum", "lenia", "ferrofluid")` from `LIVING_ART_STYLE_LABELS` keys (`:46–53`) |
| `ALL_STYLES` | `scripts/art/timelapse.py:67` | `list(_STYLE_REGISTRY.keys())` — **same six, same order** (`:58–65`) |
| CLI default + `--only` allowlist | `scripts/cli/generate/_common.py:109–119` | **both** `LIVING_ART_STYLE_KEYS` |
| Renderer default if `styles is None` | `scripts/art/timelapse.py:436` | `ALL_STYLES` |
| Legacy `generate animated` | `scripts/art/animate.py:361–377` | **third copy**: hardcoded `all_generators` dict, same six keys |

Today `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS`. **test_cli.py does not assert that.** R5 owns that equality in other modules.

Named fixture used as a one-style shard: **`topo`** (`tests/test_cli.py:187, 204, 213`).

Order-dependent slice: `LIVING_ART_STYLE_KEYS[:-1]` (`:136`) drops the **last** key (`ferrofluid` today). After shrink, last-of-SHIPPED is whoever remains last — not a ferrofluid lock.

---

## Three style lists (what test_cli actually loops)

| Loop source | In `test_cli.py`? | Count / shape | After roster split |
|---|---|---|---|
| `ALL_STYLES` (`timelapse.py`) | **Never imported** | n/a | R3 keeps this as **candidates**. Do not start looping it for default generate. |
| `LIVING_ART_STYLE_KEYS` (`artifacts.py`) | **Every fleet loop** (`:69, :87, :136, :164, :185, :224`) | 6 today via the tuple | R2 points this at **SHIPPED**. Loops auto-follow unless S8 switches them. |
| Hardcoded six-tuple / `== 6` | **None** | — | Nothing to rename for count. Contrast I05/S5 stdout `"6 assets"`. |
| Hardcoded one key | `only="topo"` / `living-topo.gif` (`:187–213`) | 1 | Keep as **CANDIDATE** `--only` (R4). Only retarget if `topo` leaves `_STYLE_REGISTRY`. |

Plan S8 title (“rewrite loops to SHIPPED”) is already the current import. The real S8 work is **not** swapping `ALL_STYLES` → shipped; it is keeping default-fleet loops on `LIVING_ART_STYLE_KEYS` and **not** collapsing `--only` onto shipped-only.

---

## A. Production picker (read-only context for R4 / `L-CLI`)

### `_common.py` — one function, two jobs

```109:119:scripts/cli/generate/_common.py
def _selected_living_art_styles(only: str | None) -> tuple[str, ...]:
    """Return the active living-art styles for this command invocation."""
    if only:
        if only not in LIVING_ART_STYLE_KEYS:
            ...
            raise typer.Exit(code=1)
        return (only,)
    return tuple(LIVING_ART_STYLE_KEYS)
```

| Path:line | Behavior | R4 change |
|---|---|---|
| `scripts/cli/generate/_common.py:16` | imports `LIVING_ART_STYLE_KEYS` | also import `CANDIDATE_STYLE_KEYS` / `SHIPPED_STYLE_KEYS` (or keep shipped alias) |
| `scripts/cli/generate/_common.py:97–103` | `_format_style_help` | keep |
| `scripts/cli/generate/_common.py:106` | `_LIVING_ART_STYLE_HELP` baked at **import time** from `LIVING_ART_STYLE_KEYS` | rebuild from **CANDIDATE** (help is `--only` allowlist) |
| `scripts/cli/generate/_common.py:112` | `--only` membership | `only not in CANDIDATE_STYLE_KEYS` |
| `scripts/cli/generate/_common.py:118–119` | default generate | `return tuple(SHIPPED_STYLE_KEYS)` (`LIVING_ART_STYLE_KEYS` after R2) |
| `scripts/cli/generate/_common.py:146` | `active_styles = _selected_living_art_styles(only)` | unchanged call |
| `scripts/cli/generate/_common.py:153` | `render_timelapse(..., styles=list(active_styles))` | default list == SHIPPED; `--only` one candidate |
| `scripts/cli/generate/_common.py:161` | expected GIF names `{f"living-{style}.gif"}` | GIF-only; no MP4 (I15). S8 does not need MP4 asserts |
| `scripts/cli/generate/_common.py:210–216` | refresh iff `active_styles == tuple(LIVING_ART_STYLE_KEYS)` | compare to **SHIPPED**. Partial `--only` skips index. Full candidate set ≠ shipped → also skip (no CLI flag generates “all candidates” today) |

Unknown `--only` exits 1 (`:112–117`). **Not tested in `test_cli.py`.** Covered weakly in `tests/test_service_cli_coverage.py:423–424` (`"not-a-style"`). That file is **not** on `L-T-CLI` / `L-CLI`.

### `art.py` — three `--only` flags, two implementations

| Command | Path:line | `--only` wiring | Default generate |
|---|---|---|---|
| `generate living-art` | `scripts/cli/generate/art.py:254–260`, `:298–307` | help = `_LIVING_ART_STYLE_HELP`; passes `only=` into `_generate_living_art_timelapse` | `_common` picker |
| `generate timelapse` | `scripts/cli/generate/art.py:374–380`, `:402–411` | **same helper** (alias of living-art) | same |
| `generate animated` | `scripts/cli/generate/art.py:156–163`, `:193–194` | help string from `_LIVING_ART_STYLE_HELP`, but **does not call** `_selected_living_art_styles`. Forwards `--only` on `sys.argv` to `animate.main()` | `animate.py:361–377` hardcoded six |

R4 files include `art.py` because help text is imported there. If `_LIVING_ART_STYLE_HELP` is rebuilt in `_common.py`, all three help strings update **without** an `art.py` edit. R4 still needs an `art.py` decision: leave `animated --only` as a third picker, or route it through the candidate allowlist.

`generate all` (`scripts/cli/generate/all_cmd.py:215–220`) calls `g.living_art(...)` with **no** `only=` → Typer default `None` → full default list. After R4 that is SHIPPED. It also calls `animated` without `--only` (`all_cmd.py` earlier block) → still all six growth files until `animate.py` is split. `test_cli.py` stubs both and does not assert style lists.

---

## B. `tests/test_cli.py` — S8 / `L-T-CLI`

File: 1494 lines. Living-art block is **`:1–284`** plus **`generate all` / `animated` at `:1269–1493`**. No other living-art hits (`rg` for `ALL_STYLES` / `exact-six` / `ferrofluid` / `inkgarden` as literals: empty except `topo`).

Import: `tests/test_cli.py:18` — `from scripts.art.artifacts import LIVING_ART_STYLE_KEYS`.

### Helpers

| Path:line | Role | `only` | Loop |
|---|---|---|---|
| `tests/test_cli.py:93–98` | `_living_art_input_paths` writes empty JSON | n/a | none |
| `tests/test_cli.py:101–126` | `_run_living_art_generation` stubs `render_timelapse` + refresh; calls `_generate_living_art_timelapse` | **always `only=None`** (`:121`) | none (caller supplies output list) |

Full-fleet helper cannot express `--only`. The one `--only` test calls `_generate_living_art_timelapse` directly (`:200–209`).

### Per-test assertion map

#### `test_refresh_living_art_artifacts_mirrors_docs_showcase` (60)

Calls `generate_cmd._refresh_living_art_artifacts` (re-export `scripts/cli/generate/__init__.py:18,53` → `_common.py:74–94` → `sync_living_art_artifacts`). **Does not** go through `_selected_living_art_styles`.

| Path:line | Assertion | Kind | S8 / shrink |
|---|---|---|---|
| `tests/test_cli.py:69–81` | write `living-{style}.gif` for **every** `LIVING_ART_STYLE_KEYS` | **fleet loop = shipped after R2** | auto-follows S1/S2; do not hardcode six names |
| `tests/test_cli.py:85–86` | primary `living-art-manifest.json` + `living-art-preview.html` | index | keep |
| `tests/test_cli.py:87–88` | showcase has each `living-{style}.gif` | **fleet loop** | same |
| `tests/test_cli.py:89–90` | showcase manifest + gallery | docs surface; grill: do not redesign as second gallery | keep as mirror contract |

If S13 leaves a retired `living-*.gif` on disk, this test **does not** glob `living-*.gif`; it only asserts shipped names exist. Exact-directory inventory is I03/S2, not this test.

#### `test_living_art_generation_fails_when_a_requested_style_is_skipped` (129)

`only=None` via helper → expected names = full `LIVING_ART_STYLE_KEYS`. Renderer returns **all but last**.

| Path:line | Assertion | Kind | S8 / shrink |
|---|---|---|---|
| `tests/test_cli.py:136–139` | seed `LIVING_ART_STYLE_KEYS[:-1]` as “fresh” returns | **order-dependent n−1** | still valid for any `len(SHIPPED) ≥ 2`; last key changes with S1 order |
| `tests/test_cli.py:141–144` | `typer.Exit` code 1 | fail-closed incomplete fleet | keep |

This is the default-generate completeness gate (`_common.py:161–204`). After R4, “requested” = SHIPPED, not ALL_STYLES. Returning five candidate GIFs while shipped is four would still fail if the missing name is in SHIPPED.

#### `test_living_art_generation_fails_when_no_frames_are_rendered` (147)

Empty `outputs=[]`. Exit 1. Independent of roster length. Keep.

#### `test_preexisting_stale_living_art_cannot_mask_a_skipped_style` (157)

| Path:line | Assertion | Kind | S8 / shrink |
|---|---|---|---|
| `tests/test_cli.py:164–167` | stale files for **every** `LIVING_ART_STYLE_KEYS` | fleet loop | SHIPPED |
| `tests/test_cli.py:169–170` | renderer returns `stale_outputs[:-1]` (one skipped); mutates `[0]` to `b"fresh"` | n−1 + stale peers | keep; last-of-SHIPPED is the skipped name |
| `tests/test_cli.py:172–175` | exit 1 | fail-closed | keep |

Proves CLI does not treat leftover files as generated. After shrink, leftover **retired** GIFs on disk are a different bug (S13 + exact inventory). This test never globs extras.

#### `test_partial_living_art_generation_never_refreshes_stale_fleet_index` (178)

**The only `--only` test in this file.** Matches I15: one-style shard must not `sync_living_art_artifacts`.

| Path:line | Assertion | Kind | S8 / R4 |
|---|---|---|---|
| `tests/test_cli.py:185–186` | seed stale GIFs for **every** `LIVING_ART_STYLE_KEYS` | fleet loop (peers) | SHIPPED peers are enough; optional extra candidate GIF would prove skip still holds |
| `tests/test_cli.py:187–188` | `living-topo.gif` written fresh | **hardcoded `topo`** | `--only` **CANDIDATE**. Keep `topo` unless it leaves the registry |
| `tests/test_cli.py:200–209` | `_generate_living_art_timelapse(..., only="topo")` | picker | R4: `topo` must remain a candidate; need not remain shipped |
| `tests/test_cli.py:211` | `outputs == [selected_output]` | one GIF | keep |
| `tests/test_cli.py:212` | `captured["output_dir"]` | | keep |
| `tests/test_cli.py:213` | `captured["styles"] == ["topo"]` | renderer style list | candidate key |
| `tests/test_cli.py:214` | `refresh.assert_not_called()` | `_common.py:210` partial skip | keep; predicate must stay `!= SHIPPED`, not `!= ALL_STYLES` |

Do **not** “rewrite this loop to SHIPPED” by changing `only="topo"` to the first shipped key if the point is candidate shards (CI matrix / G1ig…G1fe). After K2, `topo` may be unshipped and this test is then the R4 proof.

#### `test_full_living_art_generation_refreshes_requested_output_directory` (217)

| Path:line | Assertion | Kind | S8 |
|---|---|---|---|
| `tests/test_cli.py:224–227` | renderer returns one GIF per `LIVING_ART_STYLE_KEYS` | **full default fleet** | SHIPPED |
| `tests/test_cli.py:236–245` | `only=None` | default generate | R4 default = SHIPPED |
| `tests/test_cli.py:248` | `refresh.assert_called_once_with(output_dir)` | index refresh on full shipped set | if someone default-generated ALL_STYLES after split, this must **not** refresh |

Paired with the topo test: full shipped → refresh; one style → no refresh.

#### `test_living_art_commands_forward_custom_output_directory` (250)

| Path:line | Assertion | Kind | S8 |
|---|---|---|---|
| `tests/test_cli.py:250` | `@pytest.mark.parametrize("command", ["living-art", "timelapse"])` | both Typer aliases | keep; both share `_generate_living_art_timelapse` |
| `tests/test_cli.py:268–279` | `generate {command} --output-dir …` **without** `--only` | default generate | SHIPPED after R4; test does not inspect `only`/`styles` |
| `tests/test_cli.py:282–283` | exit 0; `captured["output_dir"]` | | keep |

Does **not** prove `--only` forwarding. That lives in S7 (`test_living_art_e2e_rehearsal.py:201–273` `living-art --only topo`; `:276–336` `timelapse --only inkgarden`). `--max-frames 6` there is a **frame cap**, not fleet size (I19 S7 note).

#### `test_generate_all_passes_metrics_and_history_to_living_art` (1269)

Stubs `living_art`; asserts metrics/history/profile forwarded. **No style list.** After R4, `generate all` still omits `--only` → SHIPPED. No S8 rewrite required unless someone adds `only=` to `all_cmd.py`.

#### `test_generate_all_passes_profile_to_living_art` (1373)

`--profile octocat` → stub. No roster. Keep.

#### `test_generate_all_skips_living_art_when_required_inputs_missing` (1469)

No metrics/history → `living_art` not called; stdout `"Skipping living art"`. Matches `all_cmd.py:206–212`. Keep.

#### Adjacent (same file, not fleet loops)

| Test | Path:line | Living-art relevance | S8? |
|---|---|---|---|
| `test_generate_all_passes_metrics_and_history_to_animated_art` | `:1321–1370` | `generate all` still invokes **legacy** `animated` | No. Third picker (`animate.py`). I15: not the published `living-*` writer |
| `test_generate_animated_invokes_compat_module` | `:1416–1466` | argv is `animate --profile … --frames 7 --size 400 --svg` — **no `--only`** | No. After shrink this still generates **all six** growth files if `generate animated` is run without `--only` |

---

## C. Coverage that looks like test_cli but is **not** `L-T-CLI`

Do not duplicate these into S8; I99 should not expand `L-T-CLI` to these files without a new lock.

| File | Path:line | What it covers | Owner |
|---|---|---|---|
| `tests/test_service_cli_coverage.py` | `:420–424` | `_format_style_help`; `_selected_living_art_styles(None)` **truthy only** (does **not** `== LIVING_ART_STYLE_KEYS`); `"inkgarden"` ok; `"not-a-style"` exits | **unlocked**. R4 can change default/allowlist without this file going red |
| `tests/test_living_art_e2e_rehearsal.py` | `:108–197` | Typer `living-art` + `timelapse` full fleet; loops `LIVING_ART_STYLE_KEYS`; `total_assets == len(...)` | **S7 / `L-T-E2E`** |
| same | `:201–273` | `generate living-art --only topo` | S7 |
| same | `:276–336` | `generate timelapse --only inkgarden` | S7 |
| `tests/test_living_art_media.py` | `:1650`, `:1327–1334` | `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS`; hardcoded six-list | **R5 / S4 / `L-T-MEDIA`** |
| `tests/test_overhaul_fact_ids.py` | `:185–193` | same equality + hardcoded six | **R5 / S10 / `L-T-IDS`** |
| `tests/test_art_shared_package.py` | `:90–101` | `ALL_STYLES ==` six-tuple; `len(families) == 6` | **R5 / S10 / `L-T-ACC`** (S10 files[] also names this) |
| `tests/test_living_art_contracts.py` | `:224–231` | `set(ALL_STYLES) == set(LIVING_ART_STYLE_KEYS)` | **S6 / `L-T-CON`** |

**R5 does not list `test_cli.py`.** Correct: there is nothing to relax here.

---

## Implications for R4 / R5 / S8

### R4 (`L-CLI`) — picker split

Today `--only` and default generate share one tuple (`_common.py:112` and `:119`). Plan: `--only` = CANDIDATE; default = SHIPPED.

| Verify (plan) | Current test | Gap |
|---|---|---|
| unknown style exits 1 | **not in test_cli**; `test_service_cli_coverage.py:423–424` | add under S8 or pin the unlocked file in R4 verify |
| default list == SHIPPED | `_run_living_art_generation` / full refresh test use `only=None` + `LIVING_ART_STYLE_KEYS` | green while shipped==candidates; after S1 the loops already follow `LIVING_ART_STYLE_KEYS` **if** R2 aliased it to SHIPPED |
| `--only` still works for a non-shipped candidate | only `only="topo"` | cannot go red until a key leaves SHIPPED; keep topo as the candidate fixture |

`art.py` `living-art` / `timelapse` need no logic change beyond help source. `animated` is a **separate** allowlist (`animate.py:361–377`). R4 title does not mention it; I99 should either extend R4 or leave growth CLI on candidates forever (I15: not daily publish).

Import-time help (`_common.py:106`) will stay stale if tests monkeypatch `LIVING_ART_STYLE_KEYS` after import. Production CI does not.

### R5 (`L-T-MEDIA`) — `ALL_STYLES == LIVING_ART_STYLE_KEYS`

**No edit to `test_cli.py`.** After R3/R2 those tuples may still be equal (W1M: shipped still six). After S1 they diverge. test_cli never compared them, so it stays green across R5.

Do not add `assert tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` in S8 — that would fight R5.

### S8 (`L-T-CLI`, deps S2 only)

Plan verify: `pytest -q tests/test_cli.py -k living`.

That `-k living` matches: refresh, skipped-style, no-frames, stale-mask, partial `--only`, full refresh, command forwarding, **and** the three `generate all` living_art tests. It also matches `test_generate_all_passes_metrics_and_history_to_animated_art` (`living` in the name). It does **not** run `test_generate_animated_invokes_compat_module`.

| What S8 should do | What S8 should not do |
|---|---|
| Keep fleet loops on `LIVING_ART_STYLE_KEYS` (SHIPPED after R2/S2) | Import `ALL_STYLES` for default-generate expected names |
| Keep `only="topo"` as candidate-shard coverage | Rewrite `--only` to “first shipped key” just to match the S8 title |
| Optionally assert `_selected_living_art_styles(None) == tuple(LIVING_ART_STYLE_KEYS)` (stronger than the unlocked truthy check) | Hardcode a six-tuple (that is R5/S10’s anti-pattern) |
| After shrink, confirm `[:-1]` still means “one shipped missing” | Treat `LIVING_ART_STYLE_KEYS[:-1]` as “drop ferrofluid” |

Sequencing: S8 deps **S2**, not R4. Safe because until S1, candidate==shipped. After S1, `--only topo` still works only if R4 already pointed `--only` at candidates **or** `topo` remains shipped. If R4 is late and S1 drops `topo` from `LIVING_ART_STYLE_KEYS` while `_selected_living_art_styles` still allowlists that tuple, **this file goes red** on `:204`.

**Fix for I99:** S8 deps += R4, **or** S8 retargets `only=` to a key that is guaranteed remaining in SHIPPED **and** add a separate candidate `--only` test after R4.

### `generate all` after shrink

`all_cmd.py:215–220` will generate SHIPPED only (R4 default). Tests stub the command; no loop to rewrite. Legacy `animated` inside `generate all` still fans out six growth files — out of S8; flag for R3/animate if “retired styles leave the daily matrix” is read as `generate all`.

---

## Finding

`test_cli.py` living-art matrix is **`LIVING_ART_STYLE_KEYS` loops + one hardcoded `topo` shard**. Not `ALL_STYLES`. Not a literal six.

- Default generate (`only=None`) is tested as “all keys in `LIVING_ART_STYLE_KEYS` must appear as `living-{style}.gif` or exit 1”; full set refreshes the index; incomplete set does not.
- `--only` is tested once, as `topo`, and must **not** refresh.
- Typer `living-art` / `timelapse` are aliases; test_cli only checks `--output-dir`. `--only` forwarding is S7.
- Unknown style, help text, and `ALL_STYLES == LIVING_ART_STYLE_KEYS` are **other files**.

## Gap

1. **S8 title vs `--only`:** blindly rewriting every loop to SHIPPED would either no-op (already `LIVING_ART_STYLE_KEYS`) or destroy the candidate-shard test (`topo`). I99: S8 = default loops stay shipped; `--only` stays candidate.
2. **S8 ↛ R4:** if S1 retires `topo` before R4 splits the picker, `test_partial_living_art_generation_never_refreshes_stale_fleet_index` reds.
3. **Unknown `--only` not in `L-T-CLI`.** Only `tests/test_service_cli_coverage.py:423–424`, unlocked. R4 verify should run it or S8 should add it.
4. **`_selected_living_art_styles(None)` is not pinned to the shipped tuple** in test_cli. The unlocked test only asserts truthiness — still passes for a 4-tuple or a 6-tuple.
5. **Third picker:** `generate animated` / `animate.py:361–377` duplicates the six keys. test_cli never passes `--only` there. R4 as specified does not update it.
6. **No `generate art` command.** Inventory of “generate art” = `art.py` module (`living-art` + `timelapse` + `animated`).
7. CLI expected outputs are **GIF-only** (`_common.py:161`). Video-primary / MP4 is I15/VID, not S8.
8. Do not edit `openspec/changes/prevent-living-art-repo-growth` (I19/S15).

## Sources

- `tests/test_cli.py` (living-art `:60–284`, generate-all `:1269–1493`)
- `scripts/cli/generate/_common.py` (`_selected_living_art_styles`, `_generate_living_art_timelapse`, `_refresh_living_art_artifacts`)
- `scripts/cli/generate/art.py` (`animated` / `living_art` / `timelapse`)
- `scripts/cli/generate/all_cmd.py:199–223`, `__init__.py` re-exports
- `scripts/art/artifacts.py:46–54` (`LIVING_ART_STYLE_KEYS`)
- `scripts/art/timelapse.py:58–67, 436` (`ALL_STYLES`, renderer default)
- `scripts/art/animate.py:361–377` (legacy `--only`)
- Adjacent tests: `test_service_cli_coverage.py:405–424`, `test_living_art_e2e_rehearsal.py:108–336`
- Plan nodes R4, R5, S8; I19 live-SSOT row for S8
