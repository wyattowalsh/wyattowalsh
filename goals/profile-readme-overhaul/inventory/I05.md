# I05 — Map _SUPPLEMENTAL_METRICS_ASSETS + workflow artifact lists

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/readme_sections.py` `_SUPPLEMENTAL_METRICS_ASSETS` is only:

1. `metrics-habits.svg`
2. `metrics-music.svg`
3. `metrics-posts.svg`

No `metrics-activity.svg` embed helper.

`scripts/supplemental_metrics.py` generate path writes habits (and optional music/posts), then **`_remove_asset_if_present(..., "metrics-activity")`**. Manifest status for activity is `enabled=False`, reason `removed-duplicate-github-feed`.

Workflow:

- Generate/validate/upload/finalize metrics lists: habits, music, posts only.
- `rg metrics-activity .github/workflows/profile-updater.yml` is empty.
- `plugin_activity: no` on production additional/extra (and primary).

Worktree has no `.github/assets/img/metrics-activity.svg`.

## Finding

Activity is deleted at generate, not emitted in README, and not in finalize. Remaining first-party cards: habits / music / posts.

## Gap

None for the asset table. Renderer leftovers (`_render_activity_card`) are unused; out of this inventory lease.
