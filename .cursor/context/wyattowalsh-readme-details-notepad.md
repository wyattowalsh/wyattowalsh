# Wyatt O. Walsh - GitHub Profile Project (`wyattowalsh`) - LLM Context: DETAILS

**Version:** 1.2
**Last Updated:** 2024-07-27
**Purpose:** Detailed technical breakdown of the `wyattowalsh` GitHub profile project for LLM context. For a high-level overview, see `@wyattowalsh-readme-summary-notepad.md`.

## 1. Core Scripts (`[@scripts/](mdc:scripts)`) Detailed Breakdown

### 1.1. `[@scripts/banner.py](mdc:scripts/banner.py)`

- **Purpose:** Generates a highly customizable, visually rich SVG banner.
- **Key Libraries:** `svgwrite`, `numpy`, `pydantic`, `loguru`, `noise`.
- **Configuration (`pydantic` Models):**
  - `BannerConfig`: Main config (dimensions, title, subtitle, colors, typography, effects, layout, generative patterns, output settings).
  - `ColorPalette`: Multiple color schemes, dark mode support.
  - `Typography`: Fonts, sizes, weights, spacing, text effects (shadow, gradient, glow), responsive adjustments.
  - `VisualEffects`: Blur, glassmorphism, Perlin noise, glow, shadows, chromatic aberration, grain.
- **Inputs:** `BannerConfig` model instance. Reads `[@.github/assets/img/octocat.svg](mdc:.github/assets/img/octocat.svg)`.
- **Outputs:** SVG file (e.g., `[@.github/assets/img/banner.svg](mdc:.github/assets/img/banner.svg)`).
- **Key Functionality:** SVG drawing, filter/gradient definitions, background rendering, complex visual pattern generation (chaotic attractors, flow fields, neural nets), Octocat embedding, text rendering with dynamic contrast adjustment, optional `svgo` optimization.
- **Invocation:** `make generate-banner`, `python scripts/cli.py banner`.

### 1.2. `[@scripts/qr.py](mdc:scripts/qr.py)`

- **Purpose:** Generates a vCard QR code PNG with an artistic SVG background.
- **Key Libraries:** `segno`.
- **Configuration:** Hardcoded vCard data, background path `[@.github/assets/img/icon.svg](mdc:.github/assets/img/icon.svg)`, default output `[@.github/assets/img/qr.png](mdc:.github/assets/img/qr.png)`, default scale.
- **Inputs:** Hardcoded vCard data, static `[@.github/assets/img/icon.svg](mdc:.github/assets/img/icon.svg)`.
- **Outputs:** PNG file (e.g., `[@.github/assets/img/qr.png](mdc:.github/assets/img/qr.png)`).
- **Key Functionality:** Structures vCard, generates QR object (error correction 'H'), renders artistic PNG with SVG background.
- **Invocation:** `make generate-qr`, `python scripts/cli.py qr`, `[@.github/workflows/qr.yml](mdc:.github/workflows/qr.yml)` (broken).

### 1.3. `[@scripts/word_clouds.py](mdc:scripts/word_clouds.py)`

- **Purpose:** Generates word cloud PNGs from topics/languages in Markdown files.
- **Key Libraries:** `wordcloud`, `markdown`, `beautifulsoup4`, `Pillow`, `numpy`, `matplotlib.colors`, `pydantic`.
- **Configuration:** Default input paths (`[@.github/assets/topics.md](mdc:.github/assets/topics.md)`, `[@.github/assets/languages.md](mdc:.github/assets/languages.md)`), default output paths (`[@.github/assets/img/wordcloud_by_topic.png](mdc:.github/assets/img/wordcloud_by_topic.png)`, etc.), hardcoded `WordCloud` parameters.
- **Inputs:** Markdown files from `[@.github/assets/](mdc:.github/assets)` (dynamically generated by `[@.github/workflows/awesome-stars.yml](mdc:.github/workflows/awesome-stars.yml)`).
- **Outputs:** `wordcloud_by_topic.png`, `wordcloud_by_language.png`.
- **Key Functionality:** Reads MD, converts to HTML, parses HTML for terms & frequencies (from `<a>` tags and `<li>` counts in `<ul>`), generates cloud from frequencies, saves to PNG.
- **Invocation:** `make generate-word-clouds`, `python scripts/cli.py generate word_cloud --profile-asset topic|language`, `[@.github/workflows/wordclouds.yml](mdc:.github/workflows/wordclouds.yml)` (broken).

### 1.4. `[@scripts/cli.py](mdc:scripts/cli.py)`

- **Purpose:** `typer`-based CLI for script execution with parameters.
- **Key Libraries:** `typer`.
- **Main Commands:**
  - `config`: Manage `config.yaml` (view, save, generate-default).
  - `generate <entity_type>`: (`banner`, `qr_code`, `word_cloud`).
    - `banner`: Wraps `[@scripts/banner.py](mdc:scripts/banner.py)`. Options for dimensions, output, optimization, title, subtitle.
    - `qr_code`: Wraps `[@scripts/qr.py](mdc:scripts/qr.py)`. Options for error correction, scale, background, output.
    - `word_cloud`: Wraps `[@scripts/word_clouds.py](mdc:scripts/word_clouds.py)`. `--profile-asset topic|language` for profile clouds; `--techs-path` for generic tech word clouds from e.g. `[@techs.md](mdc:techs.md)`.
- **Note:** `[@Makefile](mdc:Makefile)` primarily uses this CLI for generation tasks.

### 1.5. `[@scripts/techs.py](mdc:scripts/techs.py)`

- **Purpose:** Intended for generating a Markdown section of technologies. Currently parses tech data from Markdown using Pydantic models and can display it.
- **Status:** Not a stub. Contains Pydantic models (`Technology`), `load_technologies` (MD parser), `display_technologies` (Rich output). Currently, its data can be used by `[@scripts/cli.py](mdc:scripts/cli.py)` for generic tech word clouds. A dedicated function for generating a structured Markdown output (e.g., a 'Technologies I Use' section for `[@README.md](mdc:README.md)`) and a direct CLI command for this specific output are key future enhancements.
- **Inputs (inferred):** Markdown file (e.g., `[@techs.md](mdc:techs.md)`, `[@.github/assets/topics.md](mdc:.github/assets/topics.md)`).
- **Outputs (intended):** Markdown file or console output.
- **Action:** Define and implement `generate_techs_markdown_section(input_path: Path, output_path: Path) -> str` and integrate with `[@scripts/cli.py](mdc:scripts/cli.py)` as a new subcommand (e.g., `python scripts/cli.py generate techs_section`). Consider if this script should also produce other outputs like categorized lists or data for other visualizations.

## 2. Dependencies & Tooling (Key List)

- **Package Manager:** `uv`.
- **Core Runtime:** `loguru`, `numpy`, `pydantic`, `rich`.
- **Asset Generation:** `svgwrite`, `segno`, `qrcode-artistic`, `wordcloud`, `markdown`, `beautifulsoup4`, `fonttools`, `noise`.
- **Dev & QA:** `autoflake`, `autopep8`, `black`, `isort`, `mypy`, `pylint`, `ruff`, `pytest` (with many plugins like `pytest-cov`, `pytest-mock`, etc.).
- **CLI Dev:** `typer`.
- **External CLIs:** `svgo` (SVG optimization), `starred` (GitHub starred repos).
- **Docs Server:** `docsify-cli`.
  (Full list in `[@pyproject.toml](mdc:pyproject.toml)`)

## 3. Local Development Workflow (`[@Makefile](mdc:Makefile)`)

- `all`: `lint`, `test`, `generate`.
- `install`: `uv sync`.
- `format`: `black`, `isort`, `autoflake`.
- `lint`: `ruff`, `pylint`, `mypy`.
- `test`: `pytest`.
- `generate`: Runs sub-targets via `[@scripts/cli.py](mdc:scripts/cli.py)`.
  - `generate-banner`
  - `generate-qr`
  - `generate-word-clouds`
- `clean`: Removes caches, logs, generated assets, `.venv`.
- `venv`: `uv venv` & `uv sync`.
- `update-deps`: `uv pip compile --upgrade`.
- `docs`: `docsify serve ./docs`.

## 4. GitHub Actions (`[@.github/workflows/](mdc:.github/workflows)`) Overview

### 4.1. `[@.github/workflows/main.yml](mdc:.github/workflows/main.yml)` (Profile Metrics)

- **Action:** `lowlighter/metrics@latest`.
- **Output:** `[@.github/assets/img/metrics.svg](mdc:.github/assets/img/metrics.svg)`, `[@.github/assets/img/metrics.additional.svg](mdc:.github/assets/img/metrics.additional.svg)`.
- **Status:** Assumed functional.

### 4.2. `[@.github/workflows/qr.yml](mdc:.github/workflows/qr.yml)` (QR Code Gen)

- **Action:** Runs `[@scripts/qr.py](mdc:scripts/qr.py)`.
- **Status: BROKEN.** Needs Python 3.13, `uv install segno qrcode-artistic` (or `uv sync --with qr`).

### 4.3. `[@.github/workflows/wordclouds.yml](mdc:.github/workflows/wordclouds.yml)` (Word Cloud Gen)

- **Action:** Runs `[@scripts/word_clouds.py](mdc:scripts/word_clouds.py)`.
- **Status: BROKEN.** Needs Python 3.13, `uv install wordcloud markdown beautifulsoup4 fonttools` (or `uv sync --with word-clouds`).

### 4.4. `[@.github/workflows/waka.yml](mdc:.github/workflows/waka.yml)` (WakaTime Stats)

- **Action:** `anmol098/waka-readme-stats@master`.
- **Output:** Modifies `[@README.md](mdc:README.md)`.
- **Status:** Assumed functional.

### 4.5. `[@.github/workflows/awesome-stars.yml](mdc:.github/workflows/awesome-stars.yml)` (Starred Repos to MD)

- **Action:** Uses `pip install starred` then `starred` CLI.
- **Output:** `[@.github/assets/languages.md](mdc:.github/assets/languages.md)`, `[@.github/assets/topics.md](mdc:.github/assets/topics.md)`.
- **Status:** Assumed functional. Uses Python 3.10. **Action:** Update to 3.13, consider `uv` for `starred` install.

### 4.6. `[@.github/workflows/blog-post-workflow.yml](mdc:.github/workflows/blog-post-workflow.yml)` (Medium Blog Posts)

- **Action:** `gautamkrishnar/blog-post-workflow@master`.
- **Output:** Modifies `[@README.md](mdc:README.md)`.
- **Status:** Assumed functional.

## 5. Detailed Known Issues & Actions (from Summary)

- **Broken Workflows:**
  - `[@.github/workflows/qr.yml](mdc:.github/workflows/qr.yml)`: Update to Python 3.13, use `uv install segno qrcode-artistic` or `uv sync --with qr`. Fix commit message.
  - `[@.github/workflows/wordclouds.yml](mdc:.github/workflows/wordclouds.yml)`: Update to Python 3.13, use `uv install wordcloud markdown beautifulsoup4 fonttools` or `uv sync --with word-clouds`.
- **`[@scripts/techs.py](mdc:scripts/techs.py)` Enhancement:** If distinct Markdown output needed (e.g., for `[@README.md](mdc:README.md)`), implement `generate_techs_markdown_section(input_path: Path, output_path: Path) -> None` (or returns string for CLI to handle) and add a dedicated CLI subcommand like `generate techs_section --input <path> --output <path>`. Evaluate if `techs.py` should also be capable of outputting structured data (e.g., JSON) for other potential tools or visualizations.
- **Python Version Inconsistencies:**
  - MyPy: Clarify/set `python_version = "3.13"` (e.g., in `[@pyproject.toml](mdc:pyproject.toml)` `[tool.mypy]`).
  - GitHub Actions: Change `[@.github/workflows/awesome-stars.yml](mdc:.github/workflows/awesome-stars.yml)` (and already noted qr/wordclouds) to Python `3.13`.
- **Workflow Dependency Management:**
  - `[@.github/workflows/awesome-stars.yml](mdc:.github/workflows/awesome-stars.yml)`: Consider adding `github-starred` to `[@pyproject.toml](mdc:pyproject.toml)` and using `uv` for install.
- **Font Availability (`[@scripts/banner.py](mdc:scripts/banner.py)`):** Needs CI consideration if automated.
- **Hardcoded Paths/Values:** Acceptable for local use via `[@Makefile](mdc:Makefile)`; `[@scripts/cli.py](mdc:scripts/cli.py)` provides overrides.
- **Asset File Timeouts:** `[@.github/assets/topics.md](mdc:.github/assets/topics.md)` and `[@.github/assets/languages.md](mdc:.github/assets/languages.md)` may be large; direct inspection might fail, but generation method is known.
- **Testing Strategy:**
  - Unit Tests: Increase coverage for individual functions in all scripts, especially edge cases in Pydantic model validation and data parsing.
  - Integration Tests: Add tests for `[@scripts/cli.py](mdc:scripts/cli.py)` subcommands, verifying that they call underlying script functions correctly and that output files are generated (content validation can be minimal, focusing on existence and basic structure).
  - Snapshot Testing: For visual assets (SVGs, PNGs), consider snapshot testing where feasible to detect unintended visual changes, though this can be complex to maintain.
  - CI: Ensure all tests run on PRs. Optimize CI workflow for speed (e.g., caching dependencies).
- **Documentation Strategy:**
  - `[@docs/](mdc:docs)`: Expand with more detailed usage guides for each script and CLI command.
  - Code Comments/Docstrings: Ensure all functions and classes have clear docstrings.
  - Notepads (`@summary`, `@details`): Regularly review and update to reflect major changes.
  - Consider tools like `sphinx` or `mkdocs` if documentation grows significantly. Perhaps even a `make docs` target to build HTML docs from source.

(For overall project goals, status, and high-level data flows, refer to `@wyattowalsh-readme-summary-notepad.md`)
