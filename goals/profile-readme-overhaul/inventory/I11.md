# I11 — Map daily_snapshots monotonic contract + style registry

**Date:** 2026-08-14  
**Status:** complete

## Evidence

Shared spine: `scripts/art/daily_snapshots.py` `build_daily_snapshots` reconstructs one calendar day sequence from account creation → now (`include_today=True` in `timelapse.py`). Frame t is the cumulative end-of-day world through that day.

Monotonic contract: `validate_snapshot_monotonic_contract` forbids regressions on `MONOTONIC_CUMULATIVE_KEYS` (stars, forks, followers, public_repos, total_commits, …) and on `maturity`. Called after snapshot build and again after `sample_frames`.

Style registry (`scripts/art/timelapse.py` `_STYLE_REGISTRY` / `ALL_STYLES`):

| Key | Module |
|---|---|
| inkgarden | `ink_garden.generate` |
| topo | `topography.generate` |
| genetic | `genetic_landscape.generate` |
| physarum | `physarum.generate` |
| lenia | `lenia.generate` |
| ferrofluid | `ferrofluid.generate` |

Exact-six artifact contract: `scripts/art/artifacts.py` `_validate_canonical_inventory` requires `living-{style}.gif` for those six names only. Those six `living-*.gif` files exist. The img dir also still has legacy `{style}-growth.gif` / `{style}-growth-animated.svg` (outside the exact-six validator when `exact_directory=False`).

## Finding

One daily spine + monotonic contract + exact-six GIFs. Human contact-sheet sign-off: still-frame inspection is signed; published GIFs are not.

## Gap

[A1-frame-inspection.md](./A1-frame-inspection.md) is signed for `inventory/frames/` stills (`t0`–`t2` × six). Residual: no composite strip; stills are synthetic (`timeline=False`), not sampled GIF frames; accretion still weak on inkgarden/physarum t0 and ferrofluid overlap; published `living-*.gif` were not redesigned. See I99.
