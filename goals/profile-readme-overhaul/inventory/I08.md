# I08 — Map word-cloud renderers + starred count parser

**Date:** 2026-08-14  
**Status:** complete

## Evidence

`scripts/word_clouds/generate.py`:

- `parse_frequencies_from_md` → `{name: starred-repo-count}` from Contents + per-`##` list lengths (`scripts.starred_lists` format). Empty sections count `0`. Trailing meta headings ignored.
- `DEFAULT_RENDERER = "typographic"`.
- Registered SVG engines: `wordle`, `clustered`, `typographic`, `shaped`, `metaheuristic-anim`.

CI generate-assets writes exactly:

- `wordcloud_typographic_by_topics.svg`
- `wordcloud_typographic_by_languages.svg`

`tests/test_word_clouds.py`:

- `test_checked_in_starred_lists_parse_as_repo_volume` (python > zig on both lists).
- `test_default_renderer_ships_typographic_filenames`.
- Bake-off: `test_svg_engine_bakeoff_typographic_best_encodes_volume` (see C1).

## Finding

Starred-repo counts drive size. Typographic is the shipped default and the only CI pair.

## Gap

None for the map. Per-renderer volume scores are C1.
