# I16 — Map living-art-modes + AGENTS + workflows docs exact-six copy

**Date:** 2026-08-18  
**Status:** complete  
**Scope:** read-only map of exact-six / all-six / hardcoded-roster copy in the listed agent+docs surfaces. Downstream: S11 (`L-DOCS-MODES`), S12 (`L-DOCS-WF`).  
**Out of scope (fact-docs-contract):** do not redesign `docs/public/showcase/` or `living-art-preview.html` as a second gallery. Those files are named in contract sentences below; S11/S12 only retarget *when* they regenerate, not their layout.

Locks (from `goals/living-art-overhaul/task-graph.json`):

| Node | Lock | Files |
|---|---|---|
| S11 | `L-DOCS-MODES` | `docs/content/docs/scripts/living-art-modes.mdx` |
| S12 | `L-DOCS-WF` | `AGENTS.md`, `docs/content/docs/workflows/index.mdx` |

S11 verify: `no leftover exact-six quota; legends match roster`  
S12 verify: `rg exact-six only in historical openspec`  
Plan W4M `rg`: `exact-six|exact six|all six` over `scripts tests docs/content/docs/scripts/living-art-modes.mdx AGENTS.md .github/workflows/profile-updater.yml` — **does not** include `docs/content/docs/workflows/index.mdx`.

---

## Finding

Today’s docs treat **candidate generators == shipped fleet == six**. Every public contract sentence that says “exact six”, “all six”, “six-style fleet”, “six-way matrix”, or lists the six keys as the only canonical GIFs is a **quota**, not a comment. After K2 the live roster is 4–6 shipped; leftover quota copy will lie.

Two layers must be split in S11/S12:

1. **Quota / CI inventory** — “complete six-style fleet”, “exact-six assembler”, “six matrix children”. These must follow `SHIPPED_STYLE_KEYS` (length 4–6).
2. **Candidate registry** — `_STYLE_REGISTRY` / `ALL_STYLES` / `--only` keys / generator modules. Plan keeps generators; R4 says `--only` stays on **candidates**. Hardcoded six-key CLI lists may remain if they are labeled as candidates, not as the daily published set.

`living-art-modes.mdx` currently has **no README legend copy** (no title / 1–2 sentence metaphor / four-signal mapping). S11 depends on M1 for that text. The knobs table is dialect encoding, not dropdown legends.

`scripts/AGENTS.md` has **zero** exact-six / all-six / living-art roster sentences. `scripts/art/AGENTS.md` has one quota sentence and is **not** on `L-DOCS-WF`. S12’s repo-wide `rg exact-six` verify will fail unless that file is assigned or the verify is narrowed.

---

## Sentence catalog (in-scope files)

Quoted lines are the contract sentences. Nearby tables/lists are hardcoded rosters (same six keys).

### A. `docs/content/docs/scripts/living-art-modes.mdx` → **S11**

| # | Path:line | Kind | Text / roster |
|---|---|---|---|
| A1 | `docs/content/docs/scripts/living-art-modes.mdx:8` | all six + complete-fleet quota | “Once **all six canonical GIFs** exist, generation rebuilds `living-art-manifest.json` and `living-art-preview.html` from that complete fleet.” Also names docs showcase mirror. **Retarget quota to shipped; do not restyle preview/showcase.** |
| A2 | `docs/content/docs/scripts/living-art-modes.mdx:28` | six-way matrix quota | “exports the canonical style registry as a **six-way matrix**.” Today that registry is `LIVING_ART_STYLE_KEYS` == six. After shrink: matrix size = `len(SHIPPED)`. |
| A3 | `docs/content/docs/scripts/living-art-modes.mdx:30-31` | six + exact-six quota | “A read-only assembler merges the **six uniquely named GIF artifacts** and runs the same **exact-six media validator**…” |
| A4 | `docs/content/docs/scripts/living-art-modes.mdx:37` | all six (spine) | “**All six styles** consume one shared calendar spine…” After split, every *registered* style still uses the spine; do not keep the numeral if shipped ≠ six. Prefer “every registered style” / `ALL_STYLES`. |
| A5 | `docs/content/docs/scripts/living-art-modes.mdx:42-44` | candidate identity (not a numeral) | “hands the same sampled list to every registered style in `_STYLE_REGISTRY` / `ALL_STYLES`.” Keep as **candidates** after R3; do not silently equate with shipped. |
| A6 | `docs/content/docs/scripts/living-art-modes.mdx:70-77` | hardcoded 6-row dialect roster | Style keys: `inkgarden`, `topo`, `genetic`, `physarum`, `lenia`, `ferrofluid`. Knobs are stars/commits/followers; repos called out as a shared channel at `:79-81`. **S11:** keep retired keys only if still candidates, or split “candidate dialects” vs “shipped README legends”. Legends do not exist here yet. |
| A7 | `docs/content/docs/scripts/living-art-modes.mdx:85-92` | hardcoded 6-row **published** output matrix | Display names + files: Ink Garden `living-inkgarden.gif`, Topography `living-topo.gif`, Genetic Landscape `living-genetic.gif`, Physarum `living-physarum.gif`, Lenia `living-lenia.gif`, Ferrofluid `living-ferrofluid.gif`. Indexed companions: `living-art-manifest.json`, `living-art-preview.html`. **This is the shipped-file contract.** Retired styles leave this table (S13 assets; S11 copy). No MP4 rows today. |
| A8 | `docs/content/docs/scripts/living-art-modes.mdx:100` | hardcoded 6-key `--only` list + “other five” | “Use `--only inkgarden`, `--only topo`, `--only genetic`, `--only physarum`, `--only lenia`, or `--only ferrofluid`… If the **other five** canonical files are absent…” The six-key list is candidate CLI (R4). “Other five” is a **six-quota** implication: rewrite to “if the rest of the **shipped** canonical files are absent”. |
| A9 | `docs/content/docs/scripts/living-art-modes.mdx:131` | six-style fleet + exactly the six | Constraint: “regenerated only from a **complete six-style canonical fleet**” → Consequence: “Treat **exactly the six canonical GIFs** as the only cross-job authority.” Core S11 quota rewrite. Preview/manifest regeneration stays; gallery is not redesigned. |
| A10 | `docs/content/docs/scripts/living-art-modes.mdx:136` | exact-six assembler | “The **exact-six assembler** cannot succeed, so the finalizer remains skipped…” |
| A11 | `docs/content/docs/scripts/living-art-modes.mdx:148` | six-GIF rollback | “restore or revert the previous tracked **six-GIF fleet** together with both manifest and preview surfaces…” |

Related Cards (`:159-169`) only link Ink Garden, Topography, World State. Not a six-quota sentence; optional S11 hygiene.

**Absent in this file (gaps for S11, not extra quota hits):**

- No per-piece README legends (title / metaphor / repos-stars-commits-followers mapping). Fact-docs-contract requires legends to match the shipped roster; M1 is the source.
- No MP4 / `<video>` companion. After V4, shipped *files* may include `.mp4`; S11 should name those files without turning preview.html into a second gallery.
- Four-signal spine is documented in the knobs table. README intro must **not** list that spine (fact-intro); living-art-modes as engineering SSOT **should** keep the mapping.

### B. Root `AGENTS.md` → **S12**

| # | Path:line | Kind | Text |
|---|---|---|---|
| B1 | `AGENTS.md:94` | six matrix + exact-six fleet | “**six** read-only `generate-event-art` matrix children render isolated canonical GIFs; `assemble-event-art` validates the merged **exact-six** fleet…” **Primary S12 rewrite.** Say matrix follows `LIVING_ART_STYLE_KEYS` / `SHIPPED` (length not hardcoded). |
| B2 | `AGENTS.md:101` | SSOT identity | “Living-art style map (SSOT): `scripts/art/timelapse.py` (`_STYLE_REGISTRY` / `ALL_STYLES`) · human-readable matrix: `living-art-modes.mdx`.” After R1/R3 this is **candidates**. S12 should point shipped CI/README at `SHIPPED_STYLE_KEYS` (planned `scripts/art/roster.py`) and keep `ALL_STYLES` as generators. |
| B3 | `AGENTS.md:167` | index pointer | Sub-file index: living-art-modes = “Mode matrix; pairs with `_STYLE_REGISTRY`.” Same candidate/shipped split as B2. |

Not quota, but stale tree: `AGENTS.md:70-77` lists only `ink_garden.py` and `topography.py` under `art/` (plus timelapse/artifacts/animate). Missing `genetic_landscape.py`, `physarum.py`, `lenia.py`, `ferrofluid.py`. Candidate completeness, not exact-six wording. Optional S12 or R3 hygiene.

Other living-art mentions with no six-quota: `:177` science extras “for living-art CI”.

### C. `scripts/AGENTS.md` — no exact-six copy

`rg` over this file: **no** `six`, `exact-six`, `living-art`, `timelapse`, `inkgarden`, `ferrofluid`, `physarum`, `lenia`, or `genetic`.

Module map (`scripts/AGENTS.md:56-58`) lists `art/ink_garden.py`, `art/animate.py`, `art/topography.py` only. Same incomplete-tree note as B. **S11/S12 do not own this file.** Leave unless I99 adds a docs lock.

### D. `scripts/art/AGENTS.md` — quota hit, **not** on S11/S12 locks

| # | Path:line | Kind | Text |
|---|---|---|---|
| D1 | `scripts/art/AGENTS.md:30-35` | hardcoded 6-module layout | `ink_garden.py`, `topography.py`, `genetic_landscape.py`, `physarum.py`, `lenia.py`, `ferrofluid.py`. Candidate modules; keep after shrink (plan: do not delete generators). |
| D2 | `scripts/art/AGENTS.md:23,57-58` | SSOT identity | “SSOT for `ALL_STYLES`”; “Canonical style names and GIF filenames live in `timelapse.py` (`_STYLE_REGISTRY` / `ALL_STYLES`).” After roster split, GIF *publication* names follow **shipped**, not all candidates. |
| D3 | `scripts/art/AGENTS.md:72-73` | **exact-six quota** | “`artifacts.stage_living_art_fleet()` creates the **exact-six**, media-only workflow handoff.” |

**I99:** D3 is live agent copy. Plan W4M `rg` **will** match `scripts/` → this file. S12 lock does **not** include it. Assign to `L-DOCS-WF`, fold into R3 (`L-TL`), or add an S12b. Otherwise S12’s “`rg exact-six` only in openspec” cannot pass.

### E. `docs/content/docs/workflows/index.mdx` → **S12**

| # | Path:line | Kind | Text |
|---|---|---|---|
| E1 | `docs/content/docs/workflows/index.mdx:19` | job-graph numeral | `generate-event-art **[6 styles]**` |
| E2 | `docs/content/docs/workflows/index.mdx:183-185` | dynamic (keep) | Matrix “derived from `LIVING_ART_STYLE_KEYS`”. This is the correct post-shrink story. Nearby sentences then hardcode six anyway. |
| E3 | `docs/content/docs/workflows/index.mdx:189` | six children quota | “Expands the canonical registry into **six isolated matrix children**…” |
| E4 | `docs/content/docs/workflows/index.mdx:190` | max-parallel six | “`fail-fast: false` and **at most six concurrent runners**.” Pairs with workflow `max-parallel: 6` (S3). After shrink: `max-parallel` may equal `len(SHIPPED)` (plan S3). Do not leave “six” if shipped is 4 or 5. |
| E5 | `docs/content/docs/workflows/index.mdx:197` | six disjoint artifacts | “Downloads and merges the **six disjoint GIF-only artifacts**…” |
| E6 | `docs/content/docs/workflows/index.mdx:208-209` | exact-six fleet | “It prevalidates the downloaded **exact-six living-art fleet** before mutating either destination…” |
| E7 | `docs/content/docs/workflows/index.mdx:235-236` | exact six GIFs + six-GIF showcase | “living art: the **exact six primary GIFs**, their manifest/gallery, and the corresponding **six-GIF docs-showcase mirror** plus its manifest/gallery;” Showcase *mirror* is contract (copy files), not a gallery redesign. |

GIF-only throughout. After video-primary, S12 should mention shipped MP4s only if finalize’s living-art commit group actually includes them (I18 / S3). Do not invent a showcase video gallery.

---

## Hardcoded roster (today’s six)

Used as both candidates and shipped in every in-scope table/list:

| Key | Display name | Canonical GIF (docs) |
|---|---|---|
| `inkgarden` | Ink Garden | `living-inkgarden.gif` |
| `topo` | Topography | `living-topo.gif` |
| `genetic` | Genetic Landscape | `living-genetic.gif` |
| `physarum` | Physarum | `living-physarum.gif` |
| `lenia` | Lenia | `living-lenia.gif` |
| `ferrofluid` | Ferrofluid | `living-ferrofluid.gif` |

No in-scope doc lists `living-*.mp4`.

---

## Implications for S11 (`L-DOCS-MODES`)

S11 runs after S1 + M1. Rewrite `living-art-modes.mdx` so **names, files, and legends** == `bakeoff.json` `accepted` / `SHIPPED_STYLE_KEYS`.

1. **Kill quota numerals** in A1, A2, A3, A9, A10, A11: “all six”, “six-way”, “exact-six”, “exactly the six”, “six-style fleet”, “six-GIF fleet” → “exact shipped”, “complete `SHIPPED` fleet”, “assembler / inventory for `LIVING_ART_STYLE_KEYS`”.
2. **Output matrix (A7)** is the published-file SSOT. Drop retired rows. Add MP4 only if that is the shipped file set (V4), still without redesigning preview.html.
3. **Add legends** from M1 for shipped styles only (title, 1–2s metaphor, four-signal mapping). Keep knobs table as engineering dialect; if a style is retired from daily CI, either move it to a “candidate / not shipped” subsection or delete it from the published matrix. Plan: retired styles leave the daily matrix; generators stay.
4. **`--only` six-key list (A8):** candidate CLI (R4). Keep all candidate keys if labeled as such. Rewrite “other five” to shipped completeness, not `6-1`.
5. **Spine sentence (A4):** drop “All six”; keep one shared calendar spine for every registered style.
6. **Preview / showcase (A1, A9, A11):** keep “regenerate only from a complete shipped fleet”. Do **not** restyle `living-art-preview.html` or docs home showcase (fact).
7. **GIF-only contract:** living-art-modes never mentions video. If README is GIF+MP4 click-through, S11 should state companion MP4 filenames and that preview.html remains GIF-index collateral — not a second README gallery.
8. S11 verify “no leftover exact-six quota” must catch A3/A10 hyphenated `exact-six` and A1 `all six` / A9 `six-style` / A11 `six-GIF` (not only the hyphenated token).

---

## Implications for S12 (`L-DOCS-WF`)

S12 runs after S3 (workflow comments + `max-parallel`). Parallel with S11 (`SHR-DOC`).

1. **`AGENTS.md:94` (B1)** is the only exact-six sentence in root AGENTS. Replace “six read-only … exact-six fleet” with: prepare exports `LIVING_ART_STYLE_KEYS`; matrix children = that list; assembler validates the **shipped** media-only fleet. Do not hardcode length.
2. **`AGENTS.md:101` / `:167` (B2–B3)** must stop implying `_STYLE_REGISTRY` / `ALL_STYLES` is the CI/README roster. Point daily matrix + living-art-modes published table at shipped; keep `ALL_STYLES` as candidate generators.
3. **`workflows/index.mdx`:** E1, E3, E5, E6, E7 are quota. E2 (`LIVING_ART_STYLE_KEYS`) is the sentence to generalize the others toward. E4 `at most six concurrent runners` must track S3 `max-parallel` (may equal `len(SHIPPED)`, not a permanent 6).
4. **Showcase mirror (E7):** retarget “six-GIF docs-showcase mirror” to “shipped GIF (and MP4 if committed) docs-showcase mirror”. Not a gallery redesign.
5. **S12 verify vs locks:** stated verify is `rg exact-six only in historical openspec`. That is **repo-wide** and will still hit:
   - `scripts/art/AGENTS.md:72` (D3) — **unowned by S12**
   - tests / workflow until S3–S9 (S12 only depends on S3; tests S4–S9 are parallel)
   - `living-art-modes.mdx` until S11 (parallel, not a dep)
   - `docs/content/docs/workflows/index.mdx` itself (`exact-six` at `:209`) — S12 must edit this; W4M `rg` **omits** this path
6. **I99 should either** add `scripts/art/AGENTS.md` to `L-DOCS-WF` **or** narrow S12 verify to `AGENTS.md` + `docs/content/docs/workflows/index.mdx`, and extend W4M `rg` to include the workflows index.

---

## Leakage (not S11/S12 files; I99 / later docs)

These are **not** in-scope redesign, but they repeat six-quota or the six-name roster. Plan W4M `rg` misses most of them (except `scripts/art/AGENTS.md` via `scripts/`).

| Path:line | Sentence | Note |
|---|---|---|
| `docs/content/docs/scripts/ink-garden.mdx:61` | “valid, complete **six-style** canonical fleet” | Same quota as A9. No lock. |
| `docs/content/docs/scripts/topography.mdx:70` | “complete **six-style** canonical GIF fleet” | Same. |
| `docs/content/docs/scripts/world-state.mdx:3` | frontmatter: “keep the **six living-art generators** in sync” | Candidate count; still a hardcoded six. |
| `docs/content/docs/scripts/world-state.mdx:12` | “so the **six generators** feel like different lenses…” | Same. `:53` names all six renderers (roster, not quota). |
| `docs/content/docs/cli/generate.mdx:107,132` | `--only` “`inkgarden`, `topo`, `genetic`, `physarum`, `lenia`, or `ferrofluid`” | Candidate CLI list; OK if R4 keeps all keys. No “exact six” wording. |

`docs/content/docs/scripts/meta.json` lists `living-art-modes` only as a page id — no six copy.

`tests/AGENTS.md`: no exact-six hits.

---

## Gap

1. **Orphan exact-six:** `scripts/art/AGENTS.md:72` is live SSOT copy and is outside S11/S12 locks. S12’s openspec-only `rg` cannot succeed without assigning this file or narrowing verify.
2. **W4M `rg` vs S12 files:** workflows index is S12’s main six-quota surface and is **absent** from the plan’s W4M grep path list.
3. **Legends missing** in living-art-modes; S11 cannot “match legends” from current text — needs M1.
4. **Sibling six-quota docs** (`ink-garden.mdx`, `topography.mdx`, `world-state.mdx`) will remain stale unless I99 adds them or accepts leftover “six-style” there.
5. **`scripts/AGENTS.md`:** no exact-six work; incomplete art module map is unrelated to S11/S12 unless I99 wants it.

---

## Suggested S11/S12 edit checklist

**S11 — `living-art-modes.mdx`**

- [ ] A1, A2, A3, A9, A10, A11: quota → shipped completeness (no “six” / “exact-six”)
- [ ] A4: spine language without “All six”
- [ ] A5: label `_STYLE_REGISTRY` / `ALL_STYLES` as candidates
- [ ] A6/A7: published names+files = SHIPPED; legends from M1; candidate knobs optional
- [ ] A8: “other five” → shipped remainder; `--only` keys = candidates if R4
- [ ] Preview/showcase: contract only, no gallery redesign
- [ ] Confirm no leftover `exact-six`, `exact six`, `all six`, `six-style`, `six-GIF`, `six-way`, `other five`

**S12 — `AGENTS.md` + `workflows/index.mdx`**

- [ ] B1: matrix children + assembler follow `LIVING_ART_STYLE_KEYS` / SHIPPED
- [ ] B2–B3: SSOT split candidates vs shipped
- [ ] E1, E3, E5, E6, E7: drop `[6 styles]` / exact-six / exact six / six-GIF
- [ ] E4: max concurrent = S3 `max-parallel` / `len(SHIPPED)`
- [ ] E2 stays as the dynamic source sentence
- [ ] I99: `scripts/art/AGENTS.md:72` D3

---

## Evidence notes

- Fact-docs-contract: living-art-modes + artifact contract match shipped names/files/legends; showcase and `living-art-preview.html` are not a second gallery.
- Fact-ci-roster: daily CI follows shipped 4–6; retired styles leave the daily matrix.
- Fact-candidates: today’s six remain generators until bake-off; S11 must not delete candidate `--only` docs solely because shipped shrinks.
- Until K2, shipped == candidates == six; these sentences are currently true. Shrink is S1 then S11/S12.
