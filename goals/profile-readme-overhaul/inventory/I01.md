# I01 — Map generate-assets banner + finalize allowlist

**Date:** 2026-08-14  
**Status:** complete

## Evidence

- Job `generate-assets` (`.github/workflows/profile-updater.yml` ~368) runs QR + two typographic word clouds, then **Verify pinned light and dark banners**. Comment: “Header banners are pinned to origin/main bytes. Do not regenerate.”
- No `generate banner` / `readme generate banner` in the workflow (`rg` empty). Test `test_generate_assets_skips_banner_generation` asserts the same.
- Generate-assets allowlist is exactly five files: `banner.svg`, `banner-dark.svg`, `qr.png`, `wordcloud_typographic_by_topics.svg`, `wordcloud_typographic_by_languages.svg`. Those paths are uploaded as `profile-assets-${{ github.run_id }}`.
- Finalize **Publish and validate exact profile asset inventory** requires the same five names, including both banners. Commit group `chore(assets): update generated profile assets` includes `:(glob).github/assets/img/banner*.svg`.

## Finding

CI does not regenerate the header pair. Pinned banners are verified present, allowlisted through generate-assets upload, and re-allowlisted in finalize.

## Gap

None for this leaf. Hash pin vs `origin/main` is I02.
