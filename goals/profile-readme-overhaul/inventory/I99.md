# I99 — Inventory merge / lock assignment

**Date:** 2026-08-14  
**Status:** complete (Wave I notes present; residuals recorded, not closed)  
**Branch this session:** `dev` `f8c3a3de7c9c295f1c1a088d8b85883701cfe715` (= `origin/dev`)  
**`origin/main`:** `8ca22c1e8ce24857e471ed62d847bb0951363e5c` (untouched by this goal)

## Wave I notes

I01–I13 are written and complete for their leaf scopes. Do not restyle them except to correct a false claim (I07 href zip; I03/I04/I05/I09/I10 preview gaps).

| Leaf | Title | Status |
|---|---|---|
| I01 | generate-assets banner + finalize allowlist | complete |
| I02 | origin/main ↔ origin/dev banner hashes | complete |
| I03 | production lowlighter matrix vs probe | complete (preview residual) |
| I04 | metrics_svg validate/recover stub markers | complete (extra `x=NaN` residual) |
| I05 | supplemental assets + workflow lists | complete (activity helper / posts residual) |
| I06 | Tech Stack summary / 200+ regexes | complete |
| I07 | skills.yaml url / slug-or-logo | complete (no README href zip) |
| I08 | word-cloud renderers + starred parser | complete |
| I09 | Waka API ranges + heatmap endpoints | complete (Mac-only residual) |
| I10 | blog RSS → SVG card fields | complete (preview still one board) |
| I11 | daily spine + monotonic + exact-six | complete (contact-sheet residual) |
| I12 | komarev ghpvc footer | complete |
| I13 | METRICS_TOKEN boolean gates | complete |

Sibling notes: [C1.md](./C1.md) complete; [A1.md](./A1.md) + [A1-frame-inspection.md](./A1-frame-inspection.md) signed inspection of stills, published GIFs not redesigned; [K1.b1.md](./K1.b1.md)–[K1.b6.md](./K1.b6.md) catalog url+icon batches.

## Lock assignment (from `task-graph.json`)

| Lock | Paths | Owners after I99 |
|---|---|---|
| `L-BAN-ASSET` | `.github/assets/img/banner.svg`, `banner-dark.svg` | BAN (B1 is a no-op: worktree == `origin/main` bytes) |
| `L-WF` | `.github/workflows/profile-updater.yml` | BAN then LL / RM / CAP |
| `L-RS` | `scripts/readme_sections.py` | RM then WAKA then BLOG |
| `L-SM` | `scripts/supplemental_metrics.py` | RM then HAB then SPOT |
| `L-SK` | `scripts/skills.py`, `skills.yaml` | BDG |
| `L-WC` | `scripts/word_clouds/**` | WC |
| `L-ART` | `scripts/art/**` | ART |
| `L-WAKA` | `scripts/wakatime_readme.py`, `scripts/wakatime_svg.py` | WAKA |
| `L-MSVG` | `scripts/metrics_svg.py` | LL |
| `L-README` | `README.md` | generated via finalize; do not hand-edit for ship |
| `L-GIT` | `.git` | CAP only (`G-DEV` / P1 / P2). This setup does not push. |

Same-file writers stay sequential. Independent dirs may fan out.

## Banner pin (I02 re-checked this session)

Worktree SHA-256 (also pinned in `tests/test_profile_workflow.py` / `tests/test_overhaul_fact_ids.py`):

| File | SHA-256 | Matches `origin/main` bytes |
|---|---|---|
| `banner.svg` | `a5e8d08ffb218924a322e423318219af5909f9ff4923891103842a8f7f408649` | yes (when `git show` works; pin tests skip origin compare if it fails) |
| `banner-dark.svg` | `6aaf135ac987e66ddf0594722ed9980c46c374b8a4db09ea05db56cff588f9b7` | yes (same) |

CI does not run `generate banner`. Worktree `on.push.branches` is **`dev` only**. Jobs (including finalize) also require `github.ref == 'refs/heads/dev'`. Finalize errors unless `TARGET_BRANCH == dev` before `HEAD:refs/heads/${TARGET_BRANCH}`. Daily cron still exists (GitHub runs it on the default branch); those runs skip via the `refs/heads/dev` job `if`. **G-DEV residual is the unscoped cron, not an unguarded finalize push.**

## AutomatedVerification map

Facts with `automatedVerification: true` and a fact-id named check (name or docstring):

| Fact | Named check | What it actually locks |
|---|---|---|
| fact-ship-dev | `test_fact_ship_dev_*` | `on.push` is `dev`; finalize refuses `TARGET_BRANCH != dev`; cron still scheduled |
| fact-banner-pin | `test_fact_banner_pin_*` + `test_pinned_banners_match_origin_main_bytes` | worktree hashes vs pinned + `origin/main` when `git show` works |
| fact-lowlighter-maximal | `test_fact_lowlighter_maximal_*` | YAML limits (topics 20, calendar 0, langs 0%, stars 16, people 36); residual test records committed preview is not maximal |
| fact-lowlighter-off | `test_fact_lowlighter_off_*` | production `plugin_music/tweets/activity: no`; no Spotify in `with:` |
| fact-lowlighter-retry | `test_fact_lowlighter_retry_*` | lines/achievements/gists stay off; no isolate-retry artifact |
| fact-lowlighter-audit | `test_fact_lowlighter_audit_*` | committed SVGs not regen-bar stubs; residual test records extra `x="NaN"` |
| fact-habits-both | `test_fact_habits_both_*` | YAML `plugin_habits: yes` + first-party `metrics-habits.svg`; residual test records primary `metrics.svg` has no habits panel |
| fact-habits-split | `test_committed_habits_and_music_svgs_use_designed_layouts` | first-party habits/music designed fields (cannot split vs missing lowlighter habits) |
| fact-remove-feed | `test_fact_remove_feed_*` | no `metrics-activity.svg` in README / supplemental list / finalize |
| fact-spotify | `test_committed_habits_and_music_svgs_use_designed_layouts` | hero + extras; never lowlighter Spotify |
| fact-living-art-spine | `test_fact_living_art_spine_*` | exact-six registry + six `living-*.gif` + monotonic helper |
| fact-no-200-copy | `test_fact_no_200_copy_*` | no `200+` / “View full stack” on committed README |
| fact-tech-details | `test_fact_tech_details_*` | stack in `<details>`; Waka `<img>` visible in `## Metrics`, not inside `<details>` |
| fact-badges-stack | `test_fact_badges_stack_*` | 151 https catalog homepages; residual test records no per-skill README `<a href>` |
| fact-badges-qa | `test_fact_badges_qa_*` | **local** YAML name ↔ board alt zip; **not** github.com/camo of every shield |
| fact-wordclouds | `test_fact_wordclouds_*` | exactly two typographic SVGs; volume parse |
| fact-wordcloud-bakeoff | `test_fact_wordcloud_bakeoff_*` | Spearman winner + palette contrast vs GH light/dark hex (no live GH render) |
| fact-waka-svg | `test_fact_waka_svg_*` | committed SVG has public-safe sections; residual test records Platforms=Mac only |
| fact-waka-privacy | `test_fact_waka_privacy_*` | filter helpers + committed SVG omits banned rows |
| fact-blog | `test_fact_blog_*` | 4–5 visible RSS cards, not `<details>`; residual test records committed one-board wrap |
| fact-views | `test_fact_views_*` | komarev `for-the-badge` / `label=Views` |

`fact-goal`, `fact-living-art-dialects`, `fact-density` stay `automatedVerification: false`.

## Residual gaps (honest; not closed by Wave I)

1. **fact-ship-dev / G-DEV / P2.** Worktree `on.push.branches` is `dev` only (L13–15). Jobs require `github.ref == 'refs/heads/dev'`. Finalize refuses `TARGET_BRANCH != dev` (L1593–1596) before `HEAD:refs/heads/${TARGET_BRANCH}`. Residual: daily cron (L11–12) is still unscoped; a default-branch cron run is skipped by the job `if`, not published. Origin/`main` was not edited this goal.
2. **fact-habits-both / fact-habits-split.** YAML `plugin_habits: yes` on primary, but committed `metrics.svg` has **no** “Recent coding habits” / `habit` panel. Habits appear once (`metrics-habits.svg`). First-party hero chips 30d commits / Active days / Streak / Busiest / Reviews are **0** while Public repos=190.
3. **fact-lowlighter-maximal / audit preview.** `metrics.svg`: “2 Languages” / Python 100% despite threshold `0%`; calendar is a 2026 strip (`viewBox="0,0 795,130"`, text 2026); Notable contributions empty. `metrics.additional.svg`: Featured + people, no Recently starred / Stargazers. `metrics.extra.svg`: followup heading present but bars use `x="NaN"` and 0/0 counts. Extra is not a 401-byte stub.
4. **fact-lowlighter-retry.** `plugin_lines` / `plugin_achievements` / `plugin_gists` stay `no` with no isolate-retry artifact (allowed only if unclean).
5. **fact-waka-svg.** Committed `wakatime.svg` has Languages / Editors / Platforms / Categories / week / year / all-time / heatmap. Platforms row is **Mac only** (no iOS / watchOS). Waka sits in `## Metrics` (not after Word Clouds). Markers are SVG comment only — no `This Week I Spent My Time On` dump.
6. **fact-badges-stack / qa.** README SKILLS are unlinked category boards (`tech-*.svg` rects+text, no `<a>`, no icons). Catalog 151 https urls exist (no Wikipedia). No 151-way YAML `url` ↔ README `<a href>` zip. `test_github_dev_readme_camo_renders_skill_icons` skips when origin/dev HTML has no Camo skill images. `SkillEntry.url` is still Optional and allows `http://`. `_render_badge` homepage wrapping is unused.
7. **fact-blog.** Committed preview is one `blog-posts.svg` wrap (href = first post) + five `title — YYYY-MM-DD` links without hooks. Board item rows omit hooks (featured-only). Worktree generator now emits per-post linked cards; finalize has not republished that surface.
8. **fact-living-art-dialects** (`automatedVerification: false`). `A1-frame-inspection.md` signed unreadability residual. Stills are synthetic `timeline=False`, not sampled GIF frames. Accretion still weak (inkgarden t0 sparse, physarum t0 spore-only, ferrofluid dipole overlap). Published `living-*.gif` were **not** redesigned. Legacy `*-growth.gif` / `*-growth-animated.svg` remain beside the exact-six fleet.
9. **fact-wordcloud-bakeoff.** Winner is typographic via synthetic Spearman. No live github.com light/dark visual QA.
10. **R2 leftover.** `_fetch_recent_activity` / `_render_activity_card` remain in `scripts/supplemental_metrics.py` (~L1271+). No live embed. `metrics-posts.svg` listed for emit but absent from `.github/assets/img`.
11. **facts-review.json** still `accepted: false` vs `facts-result.json` `accepted: true`. Out of this write lease.
12. **`goals/gh-profile-readme-e2e/composition/score_readme_layout.py`** still treats “View full stack” as `teaser_ok`. Out of lease.
13. **task-graph.json** nodes have no completion status. Out of lease.

## Finding

Wave I inventory is merged. Locks above are the owners for Wave 1+. AutomatedVerification facts have fact-id named tests for the **automatable** subset plus residual locks for the preview holes. Those holes (habits panel missing from `metrics.svg`, thin languages / 2026 calendar, missing stars/stargazers, extra `NaN`, Mac-only Waka, unlinked boards, one-board blog, G-DEV) remain for later write nodes — not inventory fiction.
