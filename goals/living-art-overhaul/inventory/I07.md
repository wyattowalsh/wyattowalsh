# I07 — Map test_living_art_media / contracts / e2e rehearsal

**Date:** 2026-08-18  
**Status:** complete  
**Scope:** `tests/test_living_art_media.py`, `tests/test_living_art_contracts.py`, `tests/test_living_art_e2e_rehearsal.py`  
**Locks later:** `L-T-MEDIA` (R5, G2, S4) · `L-T-CON` (S6) · `L-T-E2E` (S7)

Plan node: map exact-six, byte budgets, GIF/MP4 pair rules, 400×400, inventory directory, rehearsal counts. Implications for R5, G2, S4/S6/S7.

## Finding

These three modules pin a **GIF-only exact-six fleet**. Inventory, byte budgets, and 400×400 apply to `living-{style}.gif` only. **No test in this trio mentions MP4.** Production `_export_mp4` is optional and never enters the CLI completeness check, manifest `counts`, or `stage_living_art_fleet` directory. G2 as “pytest this module” will **not** prove GIF+MP4 pairs or measure committed GIF bytes. Until K2, `ALL_STYLES == LIVING_ART_STYLE_KEYS` (six) keeps R5 green if left as equality; after S1 that equality is the shrink bomb. E2E counts already use `len(LIVING_ART_STYLE_KEYS)` (no literal `6`); S7 is small if R2 pointed that name at `SHIPPED`. Media still hardcodes `total_assets == 6` in five places. Dialect parametrize in media/contracts covers **generators**, not the shipped README roster — S4/S6 must not collapse those to 4.

---

## SSOT the tests import

From `scripts/art/artifacts.py` (tested, not owned by I07):

| Constant | Value | Line |
|---|---|---|
| `LIVING_ART_BYTE_BUDGETS` | six GIF names → per-file byte caps | `30:37:scripts/art/artifacts.py` |
| `LIVING_ART_TOTAL_BYTE_BUDGET` | `sum(...)` = **27_000_000** | `38:38:scripts/art/artifacts.py` |
| `LIVING_ART_CANONICAL_DIMENSIONS` | `(400, 400)` | `39:39:scripts/art/artifacts.py` |
| `LIVING_ART_CANONICAL_LOOP` | `0` | `40:40:scripts/art/artifacts.py` |
| `LIVING_ART_MIN_FRAME_COUNT` | `2` | `41:41:scripts/art/artifacts.py` |
| `LIVING_ART_MAX_FRAME_COUNT` | `DEFAULT_PUBLISHED_MAX_FRAMES` = **120** (`daily_snapshots.py:35`) | `42:42:scripts/art/artifacts.py` |
| `LIVING_ART_MIN_RUNTIME_MS` | **24_000** (`timelapse.py:51`) | `43:43:scripts/art/artifacts.py` |
| `LIVING_ART_STYLE_KEYS` | `("inkgarden", "topo", "genetic", "physarum", "lenia", "ferrofluid")` | `46:54:scripts/art/artifacts.py` |

Budget table (GIF only):

| Name | Budget (bytes) |
|---|---:|
| `living-ferrofluid.gif` | 3_800_000 |
| `living-genetic.gif` | 2_400_000 |
| `living-inkgarden.gif` | 7_200_000 |
| `living-lenia.gif` | 1_200_000 |
| `living-physarum.gif` | 2_400_000 |
| `living-topo.gif` | 10_000_000 |
| **total** | **27_000_000** |

Canonical filenames come from **`sorted(LIVING_ART_BYTE_BUDGETS)`**, not from `LIVING_ART_STYLE_KEYS` order (`_canonical_living_art_names` at `291:293:scripts/art/artifacts.py`). Style-key order (README/CLI) is labels-dict order; budget-key order is lexicographic by filename.

`ALL_STYLES` is `list(_STYLE_REGISTRY.keys())` — same six names today (`58:67:scripts/art/timelapse.py`). After ROS, that list is **candidates**; `LIVING_ART_STYLE_KEYS` becomes **shipped** (R2).

SVG generators still draw **800×800** (`6:7:scripts/art/shared/constants.py`). Published raster size is a separate 400×400 contract.

---

## File map

### `tests/test_living_art_media.py` (1674 lines)

**42** `test_*` functions. Mixes four concerns in one lock (`L-T-MEDIA`):

1. **Animate SVG/GIF helpers** (not the published fleet)
2. **Artifact stage/publish/manifest/byte budgets** (exact-six GIF inventory)
3. **Accretion dialect / knob tests** (all six generators; overlap I08 / A1 / D\*2)
4. **On-disk fact tests** (committed `living-*.gif` names + multi-frame)

#### Helpers

| Helper | Role | Line |
|---|---|---|
| `_write_test_gif` | Pillow GIF; default size **(4, 3)**; durations `(12_000, 12_000)`; `loop=0` | `28:58:tests/test_living_art_media.py` |
| `_write_canonical_fleet` | Writes **every** `LIVING_ART_BYTE_BUDGETS` name at **400×400** | `61:68:tests/test_living_art_media.py` |
| `_file_snapshot` | Rollback assertions | `71:74:tests/test_living_art_media.py` |

Canonical fleet iterates **budget keys**, not style keys. If S2 drops a budget row, fleet helpers shrink automatically; if a GIF is added without a budget, stage tests will not write it.

#### Exact-six (literal `6` + name-set)

| Assertion | What it pins | Line |
|---|---|---|
| `counts == {"timelapse_gif": 6}` | Manifest channel count | `283:285:tests/test_living_art_media.py` |
| `total_assets == 6` | Manifest length | `286:286:tests/test_living_art_media.py` |
| `actual_styles == set(LIVING_ART_STYLE_KEYS)` | Style set (not order) | `291:292:tests/test_living_art_media.py` |
| public `counts == {"timelapse_gif": 6}` | Docs mirror | `342:342:tests/test_living_art_media.py` |
| stable-payload fixture `total_assets: 6` | Drift detector baseline | `592:592:tests/test_living_art_media.py` |
| `source_manifest["total_assets"] == 6` | Stage | `631:631:tests/test_living_art_media.py` |
| `{path.name for path in stage_dir.iterdir()} == set(LIVING_ART_BYTE_BUDGETS)` | **Exact directory** = six GIFs, nothing else | `632:632:tests/test_living_art_media.py` |
| primary/public `total_assets == 6` | Publish both surfaces | `880:881:tests/test_living_art_media.py` |
| `len(set(families.values())) == 6` | Six distinct dialect families (generators) | `1424:1424:tests/test_living_art_media.py` |
| `ALL_STYLES == [six names]` | Candidate registry identity | `1327:1334:tests/test_living_art_media.py` |
| `tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS` | **Shipped ≡ candidates** (R5 target) | `1650:1650:tests/test_living_art_media.py` |
| `living == expected` glob of `living-*.gif` | Committed img dir GIF set | `1667:1669:tests/test_living_art_media.py` |
| `set(LIVING_ART_BYTE_BUDGETS) == expected` | Budgets ↔ filenames | `1670:1670:tests/test_living_art_media.py` |

Named exact-six tests:

- `test_stage_living_art_fleet_is_exact_six_and_media_only` (`614:635:tests/test_living_art_media.py`) — stage listing equals budget names; **no** `living-art-manifest.json` / `living-art-preview.html`; files are regular (not symlinks). Banner + stale companions in **source** are allowed (`exact_directory=False` on producer).
- `test_fact_living_art_exact_six_excludes_legacy_growth_collateral` (`1664:1673:tests/test_living_art_media.py`) — committed `.github/assets/img/living-*.gif` **equals** `{living-{k}.gif for k in LIVING_ART_STYLE_KEYS}`. `*-growth.gif` / `*-growth-animated.svg` must be disjoint from that set (legacy names stay off the fleet).

Loops `for style in LIVING_ART_STYLE_KEYS` (SVG animate outputs, sync, budgets, facts): `142:147`, `270:271`, `308:309`, `320:321`, `344:345`, `350:351`, `404:405`, `419:422`, `454:455`, `480:481`, `1656:1661`, `1668:1668`. After R2 these follow **shipped**. Dialect parametrize (`1337:1339`) is a **hardcoded six-tuple**, not `LIVING_ART_STYLE_KEYS`.

#### Byte budgets

Happy path: `test_living_art_byte_budgets_accept_reviewed_fleet` (`349:355`) writes 400×400 synthetics then `validate_living_art_byte_budgets(manifest)`.

Rejects (`test_living_art_media_contract_rejects_invalid_metadata`, `358:413`, target `living-lenia.gif`):

| Injected metadata | Expected diagnostic |
|---|---|
| `200×200` | `dimensions=200x200 required=400x400` |
| `loop=1` / `loop=None` | `loop=… required=0` |
| 1 frame | `frames=1 required=2..120` |
| 121 frames | `frames=121 required=2..120` |
| `duration_ms=23990` | `required>=24000` |
| duration `0` at index 0 | `nonpositive frame durations at indexes=[0]` |

Also: missing GIF Netscape loop (`416:432`); unbudgeted names (`435:450`) — `living-junk.gif` fails **manifest scan** (`Unsupported living-art GIF`); `living-topo-dark.gif` **matches** `_TIMELAPSE_RE` then fails budget as `unexpected canonical asset` when `budgets={}`; over-budget reports `{name}: observed=101 budget=100` and `canonical total:` (`453:474`); sync refuses to write companions if budgets are 1 byte (`477:496`); empty `budgets={}` is authoritative (`499:514`); malformed manifests (`517:562`); PNG bytes under a `.gif` name (`565:572`).

**Gap:** every budget/dimension test uses **tmp_path synthetics**. No test here calls `validate_living_art_byte_budgets` on `.github/assets/img`. On-disk facts only check `is_file()`, `st_size > 0`, and `n_frames > 1` (`1656:1661`).

#### 400×400

| Use | Line |
|---|---|
| Canonical fleet helper | `66:66` |
| Reviewed-fleet + contract inject | `351`, `363`, `405`, `422` |
| Duplicate-name fixture | `534:542` |
| PNG-not-GIF | `566:567` |
| Publish invalid-stage variants | `755:777` (`399×400` is the dimension fail) |
| E2E helper (other file) | always 400×400 even when CLI `--size 96` |

Several sync tests **patch** `LIVING_ART_CANONICAL_DIMENSIONS` to `(4, 3)` so default `_write_test_gif` (4×3) passes (`273:276`, `327:330`, `462:465`, `487`). Production default remains 400×400.

#### Inventory directory (`_validate_canonical_inventory`)

Production (`308:343:scripts/art/artifacts.py`):

- `exact_directory=True` (stage, persisted stage, downloaded stage): **every** directory entry must be one of the six GIF names. Extra files fail `unexpected=`.
- `exact_directory=False` (producer source, primary checkout img): only `living-*.gif` names are compared; `banner.svg` is ignored.

Tests:

| Case | Behavior | Line |
|---|---|---|
| Stage media-only | listing == `set(LIVING_ART_BYTE_BUDGETS)`; no JSON/HTML | `631:635` |
| Missing `living-topo.gif` before stage | `missing=.*living-topo.gif`; stage dir untouched | `702:714` |
| Unexpected `living-unknown.gif` on stage | publish raises; destinations unchanged | `754:755` |
| Publish copies GIFs; deletes unmanaged `living-old.gif`; keeps `banner.svg` / `inkgarden-growth.gif` | `842:901` | |
| `_is_managed_surface_name` is GIF + companions only | `418:422:scripts/art/artifacts.py` | |

**MP4 is not inventory.** `living-*.mp4` would survive `exact_directory=False` (not `.gif`) and would **fail** `exact_directory=True` (unexpected name) if dropped into the **stage** dir. Commit img can hold MP4s next to GIFs without failing `test_fact_living_art_exact_six` (glob is `living-*.gif` only).

#### GIF / MP4 pair rules (this module: **none**)

Zero matches for `mp4` / `.mp4`. Production:

```531:535:scripts/art/timelapse.py
        # Assemble GIF (exact-six contract). Optional MP4 sibling never fails the GIF.
        gif_path = out_dir / f"living-{style}.gif"
        _assemble_gif(valid_frames, valid_durations, gif_path)
        outputs.append(gif_path)
        _export_mp4(gif_path)
```

`_export_mp4` (`331:381:scripts/art/timelapse.py`): sibling `.mp4`, yuv420p, CRF 23, `+faststart`; skip if ffmpeg missing/fails/empty. CLI expected names are GIF-only (`161:161:scripts/cli/generate/_common.py`). Manifest `counts` has only `timelapse_gif` (`55:57`, `150:152:scripts/art/artifacts.py`).

#### Other media tests (not fleet, but same file / S4 lock)

- Animate SVG: writes `{style}-growth-animated.svg` for **each** `LIVING_ART_STYLE_KEYS` (`142:147`); topo `timeline=False` (`150:192`, `195:266`).
- Daily spine: `ALL_STYLES` hardcoded six (`1327:1334`); `render_timelapse` source must mention `build_daily_snapshots` / `sample_frames` / `validate_snapshot_monotonic_contract` (`1650:1654`).
- Dialect parametrize six styles (`1337:1394`); distinct families (`1397:1424`); leased knobs (`1453:1537`); early-spine ink/phys/ferro (`1577:1613`). These belong to **candidate generators**. Shrinking them to shipped would drop retired-style dialect coverage while generators still exist (plan: do not delete generators).

---

### `tests/test_living_art_contracts.py` (304 lines)

**13** tests. Payload validation + registry zip + README/docs GIF hrefs. **No budgets, no 400×400, no inventory dir, no MP4, no literal `6`.**

| Test | Pin | Line |
|---|---|---|
| live metrics/history Pydantic | fetch shapes | `92:172` |
| `render_timelapse` rejects bad metrics | `styles=list(LIVING_ART_STYLE_KEYS)` | `175:183` |
| contributions_daily coerced | styles=`["topo"]` | `186:221` |
| `test_all_style_registries_have_same_keys` | `set(ALL_STYLES) == set(LIVING_ART_STYLE_KEYS)` **and** `animate` has all six modules | `224:241` |
| `test_all_generators_accept_plain_seed_strings` | parametrize **six** `(style_key, module)` pairs | `244:287` |
| `test_readme_living_art_section_uses_canonical_timelapse_gifs` | README contains `.github/assets/img/living-{style}.gif` for **every** `LIVING_ART_STYLE_KEYS`; forbids `{style}-growth.gif` | `289:294` |
| `test_docs_homepage_uses_mirrored_canonical_timelapses` | `docs/app/(home)/page.tsx` has `/showcase/living-{style}.gif` for every key + preview/manifest; no `growth.gif` | `297:304` |

Hardcoded six-way maps (candidates): `232:239`, `244:253`, `269:276`.

`render_timelapse(..., size=64)` in reject/coerce tests is **not** the 400×400 publish contract.

README/docs loops will follow **shipped** once `LIVING_ART_STYLE_KEYS` is `SHIPPED`. Registry equality will **fail** after S1 unless rewritten (S6). Generator seed parametrize should stay on **candidates**.

---

### `tests/test_living_art_e2e_rehearsal.py` (345 lines)

**3** tests. CLI rehearsal with stubbed `render_timelapse`. GIF helper is always **400×400**, 2 frames, 12s+12s, loop 0 (`92:105`).

#### Rehearsal counts

`test_living_art_cli_rehearsal_generates_all_preview_surfaces` (`108:198`):

1. Invokes **`generate living-art`** (full roster, no `--only`) **and** **`generate timelapse`** (`--max-frames 6 --size 96`).
2. Stub **ignores** `styles` / `size` / `max_frames` and writes `living-{style}.gif` for **every** `LIVING_ART_STYLE_KEYS` (`133:137`).
3. Expected img files: **N GIFs + manifest + gallery** where `N = len(LIVING_ART_STYLE_KEYS)` (`180:187`).
4. `manifest["total_assets"] == len(LIVING_ART_STYLE_KEYS)` (`192`).
5. `manifest["counts"]["timelapse_gif"] == len(LIVING_ART_STYLE_KEYS)` (`193`).
6. Docs showcase: same N GIFs + manifest + gallery (`195:198`).

**No literal `6`.** Today N=6 so 8 files per surface. After shrink N∈[4,6] if this constant is shipped.

Index refresh: CLI only calls `_refresh_living_art_artifacts` when `active_styles == tuple(LIVING_ART_STYLE_KEYS)` (`210:216:scripts/cli/generate/_common.py`). Full rehearsal therefore expects companions; `--only` must not.

`--only` tests (`201:273`, `276:345`):

| Command | Style forwarded | Index | Size captured |
|---|---|---|---|
| `generate living-art --only topo --max-frames 6 --size 96 --workers 2` | `["topo"]` | **no** manifest | `96` |
| `generate timelapse --only inkgarden …` | `["inkgarden"]` | n/a (timelapse path) | `96` |

Stub default `size: int = 400` (`127`, `220`, `295`) matches `render_timelapse` default (`395:395:scripts/art/timelapse.py`). `--size 96` is asserted only when captured; the written GIF is still 400×400.

No MP4. No byte-budget call. No stage/publish.

After R4, `--only` validates **candidates**; default generate uses **shipped**. These `--only` keys remain valid as long as `topo` / `inkgarden` stay in the candidate registry. Full rehearsal stub uses `LIVING_ART_STYLE_KEYS` — if that name is shipped, the stub will not write retired candidate GIFs (correct for daily CI). It will also **not** prove candidate-only generation.

---

## GIF / MP4 pair rules (cross-file)

| Surface | GIF | MP4 |
|---|---|---|
| `render_timelapse` return list | required | not returned |
| `_export_mp4` | — | best-effort sibling |
| CLI completeness (`expected_output_names`) | `living-{style}.gif` | not checked |
| `LIVING_ART_BYTE_BUDGETS` / inventory / stage | six `.gif` | not a canonical asset |
| Manifest `counts` | `timelapse_gif` only | no channel |
| Managed surface / rollback | `living-*.gif` + json/html | unmanaged |
| These three test modules | exhaustive | **absent** |
| Plan G1 / video-primary | six pairs | README `<video>` or GIF+click (M6/V4, not this trio) |

400×400 is even, so ffmpeg `scale=trunc(iw/2)*2` is a no-op on the canonical raster.

---

## Implications

### R5 — Relax `ALL_STYLES == LIVING_ART_STYLE_KEYS` only where shipped==candidates

**Lock:** `L-T-MEDIA`. **Files in graph:** `test_living_art_media.py`, `test_art_shared_package.py`, `test_overhaul_fact_ids.py` — **not** contracts (S6).

In this trio, the equality lives at:

```1650:1650:tests/test_living_art_media.py
    assert tuple(ALL_STYLES) == LIVING_ART_STYLE_KEYS
```

Until K2, shipped==candidates==six, so **keeping equality is green** (plan W1M). R5 should still rewrite it to an explicit gated form, e.g. `ALL_STYLES` (candidates) vs `LIVING_ART_STYLE_KEYS` (shipped), equal **iff** those tuples match — otherwise S1 cannot land without a red media test.

Do **not** treat `1327:1334` as the same assertion: that list is **candidate identity** (six generators). It should stay six while `_STYLE_REGISTRY` stays six (R3). Sibling copies: `tests/test_art_shared_package.py:90-91`, `tests/test_overhaul_fact_ids.py:185-186` (R5/S10).

`test_living_art_contracts.py:231` is the same equality under **S6**, not R5. If R5 forgets contracts, ROS still looks green until SHR.

### G2 — Validate regenerated fleet against byte budgets and inventory

**Lock:** `L-T-MEDIA`. **Verify:** `pytest -q tests/test_living_art_media.py`.

What that command **does** today:

- Synthetic 400×400 GIFs vs current `LIVING_ART_BYTE_BUDGETS`
- Stage/publish exact-six directory rules
- Committed `living-*.gif` **names** + `n_frames > 1`

What it **does not** do:

- Open real `.github/assets/img/living-*.gif` for 400×400, loop=0, 2..120 frames, ≥24s, or byte caps
- Require `living-*.mp4` siblings (G1 pairs are invisible here)
- Fail if regenerated GIFs exceed 27 MB total

G2 either needs a **committed-fleet** `validate_living_art_byte_budgets(build_living_art_manifest(img_dir))` (and optionally MP4 presence) in this module, or a non-pytest check. Budget bumps belong in `artifacts.py` (`L-ARTF`, I03/S2), not only in tests. Repo-growth / pack-size stays out of this goal (`facts.md` out of scope); G2 must stay inside existing caps, not raise them.

If GEN produces GIFs that fail the media contract, **these tests stay green** until someone points the validator at disk. That is the G2 hole.

### S4 — Rewrite exact-six media tests to exact-shipped

**Lock:** `L-T-MEDIA`. After S2 (budgets/inventory already shipped-only).

Must change (shipped length N ∈ [4,6]):

- Literal `6` at `284`, `286`, `342`, `592`, `631`, `880`, `881`
- Test/doc names: `exact_six` (`614`, `1664:1665`)
- `test_fact_living_art_exact_six_*` glob = shipped GIF names only (retired `living-*.gif` fail until S13)
- Loops over `LIVING_ART_STYLE_KEYS` if that name is already SHIPPED (R2) — mostly automatic

Must **not** blindly shrink:

- `ALL_STYLES == [six names]` (`1327`) — candidates
- `@parametrize` six dialects (`1337`) and `len(families)==6` (`1424`) — generator coverage
- Animate `all_generators` six-pack is in `animate.py`, not this test’s parametrize, but SVG tests loop `LIVING_ART_STYLE_KEYS` (`142`) — after shrink those tests would stop requiring retired `{style}-growth-animated.svg` unless they switch to candidates

Prefer `len(SHIPPED_STYLE_KEYS)` / `set(LIVING_ART_BYTE_BUDGETS)` over a new literal N.

### S6 — Rewrite living-art contracts tests to shipped roster

**Lock:** `L-T-CON`.

Split two zips that are one test today (`224:241`):

| Zip | After shrink |
|---|---|
| `set(ALL_STYLES) == set(LIVING_ART_STYLE_KEYS)` | **false** if candidates ⊃ shipped |
| `hasattr(animate, module)` for each key | **candidates** (generators remain) |
| README `living-{style}.gif` | **shipped** |
| docs homepage `/showcase/living-{style}.gif` | **shipped** (plan: do not redesign homepage as a second gallery, but names/files must match shipped — `facts.md` docs-contract is `living-art-modes` + artifact contract; this test still pins the homepage) |
| generator seed parametrize | **candidates** |

`render_timelapse(..., styles=list(LIVING_ART_STYLE_KEYS))` (`180`) should stay shipped. `--only`-like `styles=["topo"]` stays valid as a candidate.

### S7 — Rewrite e2e rehearsal counts to `len(SHIPPED)`

**Lock:** `L-T-E2E`.

Counts are **already** `len(LIVING_ART_STYLE_KEYS)` (`192:193`, loops `181`, `134`, `195`). If R2 aliases that name to SHIPPED, S7 is: import `SHIPPED_STYLE_KEYS` explicitly, keep `--only` on **candidates**, and make the full-rehearsal stub honor `styles=` so a future default-shipped list cannot silently write extra GIFs if someone passes a subset.

Still missing after S7 unless added: MP4 pair count, 400×400 of stub vs `--size 96` (stub lies), byte budgets.

---

## Lock / sequencing

`L-T-MEDIA` is shared: **R5 → (later) G2 → (after K2) S4**, never parallel. S6/S7 share SHR-T with S4 but different files/locks — parallel with S4 after S2.

Media file also holds accretion tests that D\*2 / `L-T-ACC` will touch via `test_art_shared_package.py`; S4 should not fight dialect greening by deleting those cases.

---

## Gap (for I99)

1. **No GIF+MP4 pair contract** in media / contracts / e2e. G1 pairs and video-primary README are untested here. Optional ffmpeg skip is the live rule.
2. **G2 pytest hole:** committed fleet bytes/400×400/runtime are not validated by this module.
3. **Literal six** concentrated in media stage/manifest tests; e2e already length-based.
4. **Registry equality** duplicated across media (R5), contracts (S6), `test_overhaul_fact_ids.py` / `test_art_shared_package.py` (R5/S10). Must stay consistent or ROS/SHR will flap.
5. **Dark GIF** `living-*-dark.gif` is already rejected as unbudgeted (`435:450`) — matches fact-one-look (no dual pairs unless bake-off).
6. **exact_directory=True** on stage forbids extras; retired GIFs left in checkout img fail S4 fact glob and producer `living-*.gif` inventory — S13 required (plan risk). MP4 leftovers do not fail the GIF glob.
7. Out of scope: `openspec/changes/prevent-living-art-repo-growth` (I19/S15). Live tests + `LIVING_ART_BYTE_BUDGETS` remain the SSOT for G2 caps.
