# I19 — Confirm openspec prevent-living-art-repo-growth stays untouched

**Date:** 2026-08-18  
**Status:** complete  
**Lock:** none (read). This leaf must not write under `openspec/changes/prevent-living-art-repo-growth/`.  
**Later gate:** **S15** (type `read`, deps `S12`, lock none). Task-graph verify: `git diff -- openspec/changes/prevent-living-art-repo-growth empty`.  
**Fact:** `fact-out-scope` — media budgets / pack size stay a separate change (`facts.md`; grill “Not this goal”). Automated verification is **false**; S15 is the human/git proof.

This change is **historical/stale on purpose**. After the bake-off shrink, live SSOT is artifacts + tests + `living-art-modes` + the workflow — **not** this OpenSpec folder. Do not edit, archive, or “fix” its exact-six / fan-in wording as part of living-art-overhaul.

---

## Tree (7 tracked files, 340 lines)

All under `openspec/changes/prevent-living-art-repo-growth/`. There is **no** `openspec/specs/living-art-delivery/` archive copy. The change is still sitting in `changes/` (created 2026-08-12, schema `spec-driven`). This goal must not `openspec archive` it either — archiving would mutate the tree.

| Path | Lines | Role |
|---|---|---|
| `.openspec.yaml` | 2 | `schema: spec-driven` / `created: 2026-08-12` (no six wording) |
| `README.md` | 5 | One-paragraph summary: six-style matrix + exact-six fan-in |
| `proposal.md` | 38 | Why / what changes / capabilities |
| `design.md` | 76 | Goals, **Non-Goals: fewer styles**, fan-in decisions |
| `tasks.md` | 39 | Checklist (3.x exact-six **done**; 2.x encoder + 4.x Blob **open**) |
| `baseline.md` | 54 | 2026-08-12 pack-size measurement + six-GIF fleet bytes |
| `specs/living-art-delivery/spec.md` | 126 | ADDED requirements: budgets, six-GIF handoff, fail-closed fan-in |

No untracked files in this directory.

---

## Exact-six / fan-in wording (inventory)

Quoted phrases are the live text in this change. They encode a **six-name GIF fleet** and a **six-shard matrix + assembler**. That is the 2026-08-12/13 growth-prevention contract, not the overhaul’s 4–6 shipped roster.

### `README.md`

| Line | Phrase |
|---|---|
| 4–5 | “shared-input **six-style** workflow matrix whose **exact-six fan-in** remains subordinate to one git writer” |

### `proposal.md`

| Line | Phrase |
|---|---|
| 11 | “tune each of the **six styles** independently” |
| 13–16 | “Transfer only the **exact six canonical primary GIFs** between jobs” then sole writer revalidates / regenerates manifests |
| 18–20 | “render the **six styles** in isolated matrix jobs … merge … through an **exact-six fan-in** gate” |
| 23 | “daily **six-style** behavior unless separately approved” |
| 38 | Impact: “**six style** generators, workflow handoff/finalization…” |

### `design.md` (includes the freeze)

| Line | Phrase |
|---|---|
| 3 | “The **six living-art GIFs** change daily” (pack-growth context) |
| **9** | **Non-Goals: `fewer styles`**, cadence changes, history rewrite, LFS, … |
| 24–27 | Decision 8: “only the **six canonical primary GIFs** as cross-job authority” |
| 34–39 | Decision 11: matrix from `LIVING_ART_STYLE_KEYS`; “merges **all six** artifacts” then `stage_living_art_fleet()` |
| 54–57 | Risk: “**six-runner** matrix” → “Cap the matrix at **six** jobs” … “fail the **fan-in** unless every non-experimental shard succeeds” |
| 58–60 | “require the assembler's **exact-six** inventory and media validation” |
| 66–70 | Migration: “**exact-six** media-only handoff” then “**six** isolated style shards, and a validated **exact-six** assembler” |

### `tasks.md` (checked items that landed; leave checked)

| Line | Item |
|---|---|
| 5 | `[x] 1.3` “descriptors for **all six** canonical styles” |
| 20 | `[x] 3.6` “**exact-six** primary GIF artifact” |
| 24 | `[x] 3.10` “**six-way** isolated matrix, and an **exact-six** read-only assembler” |
| 39 | `[x] 5.4` “Rehearse matrix artifact **fan-in**” |

Unchecked (still this change’s unfinished work; **not** this goal):

- `2.1`–`2.3` encoder / Pareto tuning  
- `4.1`–`4.5` optional immutable Blob pilot  

### `specs/living-art-delivery/spec.md`

| Line | Requirement / scenario |
|---|---|
| 44–47 | “transfer **exactly the six** canonical primary GIFs and no derived manifest…” |
| 59 | regenerate from “the validated **six-GIF** fleet” |
| **65–69** | **Requirement: Parallel generation has one input snapshot and fail-closed fan-in** — merge through the **exact-six** staging validator |
| 71–73 | Scenario **Six successful style shards** → assembler receives **six disjoint** canonical GIF names |
| 80–81 | “**WHEN** the **six** matrix children render one scheduled update” |
| 84 | “canonical **six-style** defaults” when `budgets=None` |
| 97 | Optimization SHALL “preserve **all six** registered styles” unless a **separate approved change** says otherwise |

### `baseline.md`

| Line | Phrase |
|---|---|
| 31–32 | “The **six** current repository assets total 20,665,377 bytes” (measurement, not a product quota) |
| 45 | “fresh-output **exact-six fan-in**” justified by sequential timeout failure |

`.openspec.yaml` has no six / fan-in wording.

---

## Why this is stale on purpose

1. **Grill / facts.** `prevent-living-art-repo-growth` is **out of scope**. It owns media budgets / pack size, not roster shrink or README presentation (`grill-notes.md` “Not this goal”; `facts.md` last bullet; `fact-out-scope`).
2. **This change’s own Non-Goal.** `design.md:9` lists **`fewer styles`**. Rewriting it to “exact shipped (4–6)” would be a different OpenSpec change, not a drive-by edit.
3. **Proposal already allows a later approval.** `proposal.md:23` keeps “daily six-style behavior **unless separately approved**.” The overhaul grill + `bakeoff.json` `accepted` **is** that separate product approval. Live code/docs/tests absorb it. This folder stays the record of how exact-six fan-in was introduced.
4. **Spec escape hatch.** `spec.md:97` already says preserve all six styles **unless a separate approved change says otherwise**. The overhaul is that change for **live** surfaces. Do not amend this spec in-place.
5. **Plan v3.** “stays stale on purpose. Out of scope. Do not edit it. Live contract + tests + `living-art-modes` are the SSOT after shrink.” Research row: leave it; **S15 asserts no diff there**. Risk: “Historical exact-six remains… Live code must not wait on that change.”
6. **Still an open change.** Encoder (2.x) and Blob (4.x) tasks are unchecked. This goal must not complete, archive, or retarget those either.

Until K2, shipped == candidates == six, so live exact-six tests stay green **without** touching OpenSpec. After S1–S14, live wording moves to exact-**shipped**; OpenSpec remains six.

---

## Live SSOT that WILL change (not this folder)

Plan Wave SHR (`S1`–`S14`) plus earlier roster split. These are the surfaces S15 is proving we updated **instead of** OpenSpec.

| SHR / ROS | Lock | File(s) | What “exact-six” means today | After shrink |
|---|---|---|---|---|
| R1 / S1 | `L-ROS` | `scripts/art/roster.py` (new) | n/a today | `SHIPPED_STYLE_KEYS = bakeoff accepted` |
| R2 / S2 | `L-ARTF` | `scripts/art/artifacts.py` | `LIVING_ART_STYLE_KEYS` (6 keys); `LIVING_ART_BYTE_BUDGETS` (6 GIF names); `_validate_canonical_inventory` docstring “exact six”; `stage_living_art_fleet` / `publish_living_art_fleet` “exact-six” | Drop retired budgets; inventory = shipped names; rename “exact six” → “exact shipped” |
| R3 | `L-TL` | `scripts/art/timelapse.py` | `_STYLE_REGISTRY` / `ALL_STYLES`; comment L531 “exact-six contract” | Registry stays **candidates**; comment must not freeze shipped=6 |
| R4 | `L-CLI` | `scripts/cli/generate/_common.py` | `--only` and default generate both use `LIVING_ART_STYLE_KEYS` | `--only` = candidates; default generate = shipped |
| S3 | `L-WF` | `.github/workflows/profile-updater.yml` | `max-parallel: 6` (~1046); step names “Stage **exact-six** living-art artifact” / “Summarize **exact-six**…” (~1180, ~1205); comment “Keep **exact-six** stage GIF-only” (~1194) | Comments + `max-parallel` may equal `len(shipped)`; step names must stay in lockstep with S5 `STAGE_STEP` |
| S4 | `L-T-MEDIA` | `tests/test_living_art_media.py` | `test_stage_living_art_fleet_is_exact_six_and_media_only`; `test_fact_living_art_exact_six_excludes_legacy_growth_collateral`; `total_assets == 6` | exact-shipped |
| S5 | `L-T-HAND` | `tests/test_living_art_artifact_handoff.py` | `STAGE_STEP = "Stage exact-six…"`; `test_exact_six_gif_artifact_round_trip_…`; `_fan_in_matrix_style_artifacts` “six immutable matrix uploads” | I05 owns the assertion map |
| S6 | `L-T-CON` | `tests/test_living_art_contracts.py` | Loops `LIVING_ART_STYLE_KEYS` | shipped roster |
| S7 | `L-T-E2E` | `tests/test_living_art_e2e_rehearsal.py` | Counts follow `LIVING_ART_STYLE_KEYS` (the `max_frames == 6` hits here are **frame caps in fixtures**, not fleet size — do not “fix” those as roster) | `len(SHIPPED)` where the test is counting styles |
| S8 | `L-T-CLI` | `tests/test_cli.py` | Loops `LIVING_ART_STYLE_KEYS` | shipped vs candidate split |
| S9 | `L-T-WF` | `tests/test_profile_workflow.py` | `test_living_art_uses_dynamic_six_way_matrix…` asserts `max-parallel == 6`; `test_living_art_workflow_uses_exact_six_primary_only_handoff` | I05 / S3 lockstep |
| S10 | `L-T-IDS` | `tests/test_overhaul_fact_ids.py`, `tests/test_art_shared_package.py` | `ALL_STYLES ==` six-tuple; `len(families) == 6`; `test_fact_living_art_spine_exact_six_and_monotonic_helper` | candidates vs shipped; do not keep `ALL_STYLES == LIVING_ART_STYLE_KEYS` after split unless R5 still holds |
| S11 | `L-DOCS-MODES` | `docs/content/docs/scripts/living-art-modes.mdx` | “all six canonical GIFs”; “six-way matrix”; “exact-six media validator”; table “complete six-style canonical fleet” / “exact-six assembler” | shipped names, files, legends |
| S12 | `L-DOCS-WF` | `AGENTS.md` (~94 “six … exact-six fleet”); `docs/content/docs/workflows/index.mdx` (~209 “exact-six living-art fleet”; ~235–236 “exact six primary GIFs” / “six-GIF docs-showcase”) | S12 verify: `rg exact-six` only in historical OpenSpec **for those two files** | matrix follows `SHIPPED`, not “six” |
| S13 | `L-ASSETS` | `.github/assets/img/living-*` | six GIF (+ MP4) stems | remove retired |
| S14 | `L-README` | `README.md` Living Art | six wrap tiles today | regenerate from `SHIPPED` |

### Extra live copy **not** named on S12

| File | Line | Note for I99 / SHR |
|---|---|---|
| `scripts/art/AGENTS.md` | 72 | “`stage_living_art_fleet()` creates the **exact-six**, media-only workflow handoff.” Not in S12 `files[]`. Update with S2 or add to S12. |
| `scripts/art/daily_snapshots.py` | 7 | Module docstring “**all six** living-art styles consume…” — still true for **candidates**; reword only if it is read as a shipped-quota. |
| `scripts/art/timelapse.py` | 531 | Comment “Assemble GIF (**exact-six** contract).” R3 / S2. |
| `scripts/art/artifacts.py` | 28–29, 314, 935, 999 | Comment “six-style baseline”; inventory/stage/publish docstrings. **S2 owns these.** |

Plan Wave SHR verify `rg` (after S4–S12) is scoped to `scripts`, `tests`, `living-art-modes.mdx`, `AGENTS.md`, `profile-updater.yml`. It **will still match** this OpenSpec tree if someone `rg`s the whole repo — that is expected. S12’s “rg exact-six only in historical openspec” is the docs-lane check, not a license to rewrite OpenSpec so the repo-wide grep goes quiet.

---

## Git status (read-only; I19 baseline)

Recorded 2026-08-18 on branch **`dev`**. No mutations.

| Check | Result |
|---|---|
| `HEAD` / `origin/dev` | `2c21731be4716df7d376714db6ea6d7395a55881` (identical) |
| `origin/main` | `4a48b347ba275cc3b4f2525d959e82746c2590f6` (different commit; **do not update main**) |
| `git status --porcelain=v1 -uall -- openspec/changes/prevent-living-art-repo-growth` | **empty** |
| `git diff HEAD --` that path | **empty** |
| `git diff --cached --` that path | **empty** |
| `git diff origin/dev --` that path | **empty** |
| `git diff ceb6861d8… HEAD --` that path | **empty** (no later commit touched the tree) |

Commits that **created / last edited** this tree (both 2026-08-13, before this goal):

| Commit | Date | Subject | Touch |
|---|---|---|---|
| `886214500f151391f716304ff470591317de46e7` | 2026-08-13 17:38:15 -0400 | `feat(art): harden living-art publication` | add all 7 files (286 lines) |
| `ceb6861d8be691d0da4c2009a05c4897e6ca6726` | 2026-08-13 20:48:57 -0400 | `fix(workflow): harden profile generation pipeline` | +57/−3 (README, baseline, design, proposal, spec, tasks). **Last touch.** |

Conversation-start `git status` also did not list this path.

### Blob fingerprints at `HEAD` (S15 may re-`ls-tree`)

```
100644 blob 5081c9876368646770b3a309a95e065fa34c74bb	.openspec.yaml
100644 blob 9aae6a3218f1006ca65b95919f56c33a7a473b56	README.md
100644 blob aa1f128a4c75dfbb9d5bdcc874d36a8cf72574aa	baseline.md
100644 blob 15b0ca26f7a1f4639576ada36ce9c0a0cfa1831f	design.md
100644 blob fd32d8e7b7b388e0d263633b01334329af7de2b8	proposal.md
100644 blob 9bfe6ef98f11f48972ca21dbc3ba4e0184dc1c78	specs/living-art-delivery/spec.md
100644 blob 2c5887f448abbe81da8942f5d4d7017321998ad2	tasks.md
```

---

## How S15 will prove the tree was not edited

S15 is **read-only**. Deps: **S12** (docs already rewritten). Lock: none. It must **not** “make the grep clean” by editing OpenSpec.

Task-graph literal is necessary but **not sufficient**: `git diff -- path` is **unstaged vs the index**. A staged edit, or a **commit during the goal** that rewrites OpenSpec, would still show an empty unstaged diff.

### Required (all must be empty / print nothing)

Run from repo root after S12, before W4M:

```bash
# 1. Task-graph literal (unstaged)
git diff -- openspec/changes/prevent-living-art-repo-growth

# 2. Staged
git diff --cached -- openspec/changes/prevent-living-art-repo-growth

# 3. Working tree + index vs HEAD (covers 1+2)
git diff HEAD -- openspec/changes/prevent-living-art-repo-growth

# 4. Untracked / deleted / renamed (porcelain empty)
git status --porcelain=v1 -uall -- openspec/changes/prevent-living-art-repo-growth
```

### Required: no goal-era commits on that path

`git diff` vs `HEAD` is empty even if someone **committed** an OpenSpec rewrite. Compare to this I19 baseline:

```bash
# No commits since I19 HEAD that touch the tree
git log --oneline 2c21731be4716df7d376714db6ea6d7395a55881..HEAD \
  -- openspec/changes/prevent-living-art-repo-growth

# Still identical to last historical touch
git diff ceb6861d8be691d0da4c2009a05c4897e6ca6726 HEAD \
  -- openspec/changes/prevent-living-art-repo-growth
```

Both must be empty. If `HEAD` moved on `dev` for unrelated work, that is fine **as long as those two pathspecs stay empty**.

### Optional fingerprint (catches rewrite-same-size tricks)

```bash
git ls-tree -r HEAD -- openspec/changes/prevent-living-art-repo-growth
```

Blob IDs must match the table above.

### Do **not** do on S15

- Edit, archive, or reformat files under `openspec/changes/prevent-living-art-repo-growth/`.
- Run `openspec archive prevent-living-art-repo-growth`.
- “Update the spec to 4–6” so repo-wide `rg exact-six` is quiet. After shrink, **OpenSpec is the remaining historical hit** — that is the intended leftover.
- Treat unchecked 2.x / 4.x tasks as in-scope.

S15 evidence for `fact-out-scope`: attach the empty diff + empty `git log …..HEAD -- <path>` in the goal close-out (C2 still must not touch `main`).

---

## Finding

`openspec/changes/prevent-living-art-repo-growth` is a **clean, last-touched 2026-08-13** record of exact-six GIF handoff and fail-closed six-shard fan-in. Its design **non-goal is fewer styles**. Living-art-overhaul must leave every byte of that tree alone; S1–S14 retarget live artifacts, tests, `living-art-modes`, workflow, and AGENTS copy. S15 proves `git diff` **and** no new commits on that path vs I19 `HEAD` `2c21731be`.

## Gap

`scripts/art/AGENTS.md:72` still says exact-six and is **not** in S12 `files[]`. I99 / S2 / S12 should pick an owner so S12’s “rg exact-six only in historical openspec” is not failed by that file after docs SHR. Do **not** solve it by editing OpenSpec.
