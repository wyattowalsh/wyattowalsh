# I07 — Map SkillEntry.url coverage in skills.yaml

**Date:** 2026-08-14  
**Status:** complete

## Evidence

Counted via `load_skills()` + `iter_skills` (`tests/test_skills.py`):

| Check | Count |
|---|---|
| Catalog skills | **151** |
| Homepage `url` present (https) | **151 / 151** |
| Missing url | 0 |
| Missing both `slug` and `logo_path` | 0 |
| With `slug` | 123 |
| With `logo_path` | 100 |
| With both | 72 |

`tests/test_skills.py` `test_skills_yaml_badges_have_https_hrefs_and_camo_safe_urls` requires every skill `.url` to be non-empty `https://`, matching rendered `<a href>`, Camo-safe shields `src`, length ≤ `MAX_SHIELDS_BADGE_URL_LENGTH`.

## Finding

Every skill has a homepage URL. Every skill has `slug` or `logo_path` (or both). Ready for K1 icon-fitness audit.

## Gap

None for URL / slug-or-logo coverage. Icon fitness / retro style is K1, not I07.
